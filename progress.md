# Progress - EduAI Prism UI Implementation v1.2

- 2026-07-09 16:30 PDT: Completed R50 `/class` class-learning real-use closure under the corrected real-user/company beta method. Teacher logged in through the visible UI, opened `/class`, waited for `/api/class`, clicked the returned pending-grading card into `/chat?seed=...`, confirmed the composer received returned title/subject/student context, sent the seeded task, waited for `/api/chat` 200, and reviewed the returned 1170-character grading guidance before judging. Fixed `R50-CLASS-CACHE-001`: `/api/class` now returns `Cache-Control: no-store` for 401/403/200 because class diagnosis contains minor PII. Fixed `R50-CLASS-ACTION-001`: pending grading cards now seed chat with actionable grading context instead of inert `ref` params. Verified researcher masking, student redirect to `/learn`, desktop/mobile no overflow, 318-323x59 pending targets, console empty, lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R50-班级学情真实使用闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R50.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 13:50 PDT: Completed R49 `/admin/models` admin model-governance closure under the corrected real-user/company beta method. Admin logged in through the visible UI, selected MiniMax M2.7, changed the student-open switch through the visible page, waited for save status, and verified independent API readback before restoring. API real-use checks also covered anonymous 401, student 403, teacher PUT 403, invalid model 400, student normal chat blocked when the model is closed, student soft-care help bypassing model closure, teacher first normal reply quality, teacher second-call quota denial, usage increment, and full settings restoration. Fixed `R49-MODEL-SAFETY-001`: soft pressure/care wording now routes to care before model-availability blocking while crisis/integrity priority stays intact. Fixed `R49-MODEL-COPY-001`: admin model governance copy now uses no-paid fair-use/quota wording rather than cost/paid cues. Fixed `R49-MODEL-TARGET-001`: admin model controls, tabs, sidebar links, and topbar search use stable 52px targets. Final evidence: `r49-admin-models-api-summary.json`, `r49-admin-models-ui-save-readback-final.json`, desktop/mobile visual JSON, lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review all passed. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R49-后台模型治理闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R49.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 13:05 PDT: Completed R48 `/hub` model-hub visual-scheme closure under the corrected real-user/company beta method. Teacher logged in through the visible UI, opened `/hub`, clicked the GPT-Image recommendation into `/chat?model=gpt-image&seed=...`, clicked the visible send button, waited about 16.4s for the returned content, and judged only after reading the full visual prompt pack. Fixed stale/fake model-hub stats, recommendation cards losing model/seed context, duplicate current-model risk, sub-44px model/filter controls, and GPT-Image overclaim copy. Added teacher visual prompt fallback with classroom use, avoid elements, privacy safety, and teacher review checklist. Final evidence: `r48-hub-final-browser-summary.json` pass; desktop/mobile `/hub` no overflow, no visible small targets, recommendation links carry context, console empty; `npm run lint`, `npx tsc --noEmit`, `npm run build`, production restart on `:4920`, code-review-graph, and manual impact review passed. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R48-模型广场视觉方案闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R48.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 12:39 PDT: Completed R47 `/dashboard` real-data trust closure under the corrected real-user/company beta method. Teacher and admin logged in through the visible UI, waited for `/api/dashboard` and related source APIs to return, then desktop/mobile pages were judged only after source readback, content reading, click-return checks, target-size checks, console checks, and manual review. Found and fixed static dashboard data by adding protected backend `DashboardSnapshot` + `/api/dashboard`; fixed teacher sourceSummary integrity drift against `/api/learning`; fixed admin auditRows drift by auditing before snapshot generation; excluded onboarding from `/dashboard`; raised text-link and admin-tab targets. Final evidence: `r47-dashboard-api-summary-final-r3.json` pass; `r47-dashboard-browser-summary-final-r7.json` pass with teacher recent chat `/chat?session=...`, quick action `/chat?seed=...`, admin tabs 71x40, no overflow, no console, no small targets; lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review passed. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R47-首页仪表盘真实数据闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R47.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 11:49 PDT: Completed R46 student `/explore` real-data trust closure under the corrected real-user/company beta method. A student logged in through the visible 3D login path, waited for `/api/explore` to return, then the exploration page was judged only after API readback, rendered source/quest/task/growth content, desktop/mobile browser inspection, click-return checks, and manual review completed. Found and fixed `R46-EXPLORE-001`: `/explore` depended on static mock quests/tasks/progress and local task toggles; added protected `/api/explore` and `getExploreSnapshot(user)` derived from real learning snapshot data. Found and fixed `R46-EXPLORE-002`: real browser measured task links at 39px and filters at 35px; raised the controls to stable `min-h-12` and reverified desktop/mobile no small targets. Final evidence: anonymous `/api/explore` 401, student 200, sourceSummary matched `/api/learning` at 11/16/16/1/1/0/10, peer comparison honest `not_connected`, Browser desktop/mobile `verdict=pass`, task link and quest-card navigation returned correct URLs, no overflow, console empty; lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review passed. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R46-学生探索页真实数据闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R46.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 11:24 PDT: Completed R45 student `/learn` real-data trust closure under the corrected real-user/company beta method. A student logged in through the visible 3D login path, waited for `/api/learning` to return, then the learning dashboard was judged only after API readback, rendered KPI/source content, desktop/mobile browser inspection, and content-quality review completed. Found and fixed `R45-LEARN-DATA-001`: `/learn` depended on static sample learning data and could mislead students about assignments, mistakes, and peer ranking; added protected `/api/learning` and `getLearningSnapshot(user)` backed by real chat sessions, messages, favorites, feedback, knowledge files, and integrity records. Found and fixed `R45-LEARN-TOUCH-002`: SafetyHelp measured 40px high on mobile; raised relevant controls to 44px. Found and fixed `R45-LEARN-MOBILE-003`: source readback was only in hidden mobile RightRail; added a mobile-visible source-readback card. Final evidence: anonymous `/api/learning` 401, student 200, sourceSummary 11/16/16/1/1/0/10, Browser desktop/mobile `verdict=pass`, mobile `smallEnabledTargets=[]`, `overflowX=false`, console empty; lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review passed. Reports written to `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测R45-学生学习首页真实数据闭环报告-20260709.md` and `D:/VB/LLM-School/开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R45.md`. Overall 12-hour real-use QA remains active.

- 2026-07-09 10:55 PDT: Completed R44 admin analytics real-data trust closure under the corrected real-user/company beta method. Admin logged in through the visible UI, `/admin/analytics` was judged only after the backend API, returned values, rendered UI, source-readback card, role-boundary checks, desktop/mobile inspection, and content-quality review all completed. Found and fixed `R44-ANALYTICS-001`: active-account KPI counted historical audit-only user ids and could exceed current users; the aggregate is now bounded to current users. Found and fixed `R44-ANALYTICS-002`: key enabled analytics/topbar actions measured near 42px in the current scaled browser; analytics actions and shared topbar icon controls now use stable 48px sizing. Final evidence: `/api/admin/analytics` admin 200, student 403, anonymous 401; active users 4 of 4, model calls 112, old static values absent; Browser default/mobile checks reported no small enabled buttons, no horizontal overflow, no console errors; lint, tsc, build, production restart on `:4920`, code-review-graph, and manual impact review passed. Reports written under `D:/VB/LLM-School/开发材料/`; overall 12-hour real-use QA remains active.

- 2026-07-08 22:30 PDT: Completed R29 first-message AI gateway cold-start budget optimization under the corrected real-user/company beta method. A real teacher used visible login and `/chat` for three consecutive classroom-attention/year-group tasks, and judgment was made only after API, AI output, rendered UI bubble, content reading, and session readback all completed. Fixed the R28 residual latency risk by reducing normal-chat gateway attempts to one ranked base per request while retaining failed-base cooldown and successful-base promotion. Final evidence: first message local-fallback 8.0s API / 12.7s UI with usable classroom-attention content; follow-ups remained remote at 7.9s and 6.9s UI completion; session readback persisted 6 messages; console=0; crisis safety returned `source=safety` in 12ms and exact student integrity returned `source=integrity-scaffold` in 7ms; `npx tsc --noEmit`, `npm run lint`, `npm run build`, code-review-graph (159 files / 688 nodes / 6461 edges), and manual impact review passed. Reports written to `开发材料/EduAI-Prism-真实用户内测R29-首条AI冷启动等待预算优化报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R29.md`. Overall 12-hour QA remains active; next recommended slice is weak-network/mobile two-stage AI response experience.

- 2026-07-08 22:18 PDT: Completed R28 LLM gateway latency real-use closure under the corrected company/school beta method. A real teacher used visible login and the chat UI for three consecutive year-group classroom-attention tasks, waiting for full API, AI, rendered assistant bubble, and session readback before judgment. Baseline confirmed first-message local fallback took 36.6s UI and returned the wrong meeting-pack intent. Implemented gateway base health cooldown/success priority, distinct-origin candidate ordering, an 8s per-attempt timeout budget, and a teacher attention/observation fallback template; fixed the R28 tester to wait on exact assistant completion plus run marker for rendered Markdown tables. Final evidence: first message local-fallback 16.0s API / 20.8s UI with usable attention-analysis content; follow-ups remote 7.6s and 6.3s API with usable content; session readback persisted 6 messages; crisis safety and exact integrity scaffold bypassed gateway in milliseconds; lint, tsc, build, code-review-graph, and manual review passed. Reports written to `开发材料/EduAI-Prism-真实用户内测R28-LLM网关延迟与教师留存体验闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R28.md`. Residual: first cold message is improved but still a retention risk at 20.8s UI completion.

- 2026-07-08 21:45 PDT: Completed R27 methodology correction and real long-session teacher chat closure. Upgraded the QA protocol so conclusions require real school/company users, full UI/API/AI/save/readback waits, returned-content reading, and internal-review judgment. A teacher used visible login, dismissed onboarding, created a new chat, generated a class support + family-school communication plan, then asked for a sendable parent WeChat message. Real-use testing found and fixed three long-session blockers: frontend history count exceeded API max 10, historical assistant messages exceeded per-item 2000 chars, and total history exceeded the API 6000-char payload budget. Implemented `buildApiHistory()` with 10 item / 2000 item-char / 4000 total-history budget and added stable message-bubble `data-*` attributes so UI verification waits for the exact assistant message to finish streaming. Verification passed lint, tsc, build, production restart on `:4920`, strict Playwright/Edge real-use final flow, screenshot review, code-review-graph build/status, and manual impact review. Reports written to `开发材料\EduAI-Prism-真实用户内测R27-真实公司级内测协议与长会话修复报告-20260708.md` and `开发材料\EduAI-Prism-真实用户内测问题清单-20260708-R27.md`. Residual: first full reply took 38.4s UI completion due gateway fallback; next recommended slice is gateway stability/latency.

- 2026-07-08 21:10 PDT: Completed R25 real-use prompt output-format closure under the corrected company/school beta method. A teacher used the visible login and prompt creation/edit/publish workflow, waited for full `/api/chat` and `/api/prompts` results, then a student logged in to read/use the published prompt and verify non-owner mutation was still forbidden. Found and fixed `R25-PROMPT-FORMAT-001`: the output-format textarea accepted input but did not reach AI preview or prompt persistence. Wired `outputExample` through new/edit UI, `PromptTestPanel`, client/API schema, SQLite prompt storage, and local fallback output formatting. Added a safety regression proving student crisis replies ignore hostile table/marker output-format instructions. Verification passed lint, tsc, build, Playwright/Edge real-use final flow, safety regression, code-review-graph build/status, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R25-提示词输出格式闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R25.md`. Overall 12-hour QA remains active.

- 2026-07-08 17:54 PDT: Recorded the latest methodology correction as a hard v2 QA protocol: future internal testing must simulate real school/company users and internal review meetings, wait for all UI/API/AI/download/save results to fully return, read the returned content, and only then judge product quality. Added `开发材料/EduAI-Prism-真实公司级内测纠偏协议-v2-20260708.md`. Static/route/button/build checks remain baseline health evidence only.

- 2026-07-08 17:50 PDT: Completed R24 real-use prompt draft edit AI preview parity closure. A teacher used the visible product flow to run create-page AI preview, save a draft, open `/prompts/{id}/edit`, run edit-page AI preview after the fix, publish, and verify student visibility/use and non-owner mutation boundary after waiting for full results. Baseline confirmed `R24-PROMPT-EDIT-001`: the edit page had no `运行测试` button or `AI 输出预览`; fixed by adding shared `app/components/prompts/PromptTestPanel.tsx` and wiring both `/prompts/new` and `/prompts/[id]/edit` to the same real `/api/chat` preview path with source, duration, and safety status. Verification passed lint, tsc, build, Browser real-use create/edit previews, publish/student use/boundary, cleanup, code-review-graph, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R24-提示词编辑AI预览闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R24.md`. Overall 12-hour QA remains active.

- 2026-07-08 17:33 PDT: Completed R23 real-use agent-to-chat identity closure under the corrected methodology. A teacher created a real agent, submitted review, admin published it, and a student entered `/chat?agent=...`, sent a class-meeting task through the visible browser UI, waited for the full result, then verified API persistence, browser session restore, forged-contract blocking, disabled-agent blocking, and cleanup. Fixed confirmed issues in the agent chat contract: chat sessions now persist `agentId`; `/api/chat` no longer trusts client-supplied `agentPreamble`; session/body agent mismatch returns `409`; disabled/not-visible agents return `404`; `/chat` restores active agent identity from session deep links; local fallback now reflects the selected agent and returns concrete class-meeting sections (`暖场 / 分组任务 / 复盘收束`). Verification passed `npm run lint`, `npx tsc --noEmit`, `npm run build`, API real-use wait, Browser real-use wait, Browser session restore, disabled-boundary cleanup, code-review-graph, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R23-智能体对话身份闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R23.md`. Overall 12-hour QA remains active.

- 2026-07-08 16:54 PDT: Completed R22 real-use agent lifecycle governance closure under the corrected methodology. A teacher created a real draft agent, student/admin sessions waited for full API results, and the admin UI was used for real approve/disable actions. Found and fixed `R22-AGENT-GOV-001`: draft/review agents were visible/readable/usable by students before approval. Found and fixed `R22-AGENT-GOV-002`: `/admin/agents` showed review status but lacked real governance actions. Also hardened global agent search to `pub` only and raised new governance button targets to 42px. Verification passed lint, tsc, build, production restart on `:4920`, Browser admin approve/disable, API cross-role boundaries, cleanup sweep, code-review-graph, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R22-智能体生命周期治理闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R22.md`. Overall 12-hour QA remains active.

- 2026-07-08 16:20 PDT: Completed R20 prompt cross-role governance closure under the corrected real-use method. A real teacher logged in through the visible form, saved a prompt through the visible `暂存草稿` action, then API/student/admin checks waited for complete results. Found and fixed `R20-PROMPT-GOV-001`: drafts were actually saved as public prompts and visible/readable to student/admin. Implemented real `draft/pub` semantics across prompt DB/API/client/types and prompt UI, added owner-only draft labels, hid drafts from non-owners, preserved published prompt visibility/use-count behavior, and cleaned all R20 test data. Verification passed lint, tsc, build, production restart on `:4920`, Browser teacher draft/publish flows, API role boundaries, cleanup sweep, code-review-graph, and manual review. Reports written to `开发材料\EduAI-Prism-真实用户内测R20-提示词跨角色治理闭环报告-20260708.md` and `开发材料\EduAI-Prism-真实用户内测问题清单-20260708-R20.md`. Overall 12-hour QA remains active.

- 2026-07-08 15:59 PDT: Completed R19 real-use prompt-library closure under the corrected beta-testing method. A real teacher used the visible product path: login -> `/prompts` -> create prompt template -> run preview and wait for the full result -> publish -> search/detail -> send prompt card to chat -> create fresh chat -> wait for the full assistant answer -> API persistence/security/cleanup. Found and fixed `R19-PROMPT-001` in `app/lib/server/llm.ts`: AI classroom-guide prompts were misrouted to a generic meeting fallback. Found and fixed `R19-PROMPT-002` and `R19-PROMPT-003` in `app/app/(shell)/prompts/page.tsx`: narrow/mobile prompt cards needed a direct send-to-chat action and 42px-or-larger action targets. Verification passed lint, tsc, build, production restart on `:4920`, Browser real-use, API boundary/cleanup, code-review-graph, and manual impact review. Reports written to `开发材料\EduAI-Prism-真实用户内测R19-提示词库真实使用闭环报告-20260708.md` and `开发材料\EduAI-Prism-真实用户内测问题清单-20260708-R19.md`. Overall 12-hour QA remains active.

- 2026-07-08 15:21 PDT: Completed R17 real-admin permissions save/audit closure. Followed the corrected real-use standard: admin logged in through the visible login page, opened `/admin/permissions`, changed teacher `用户管理` through the UI, waited for `已保存到服务端`, verified API readback and audit records, restored the permission through the UI, and verified backend boundaries. Found and fixed `R17-ADM-PERM-001`: permissions page action/edit/save buttons and switches were below stable target size. Fixed only `app/app/(shell)/admin/permissions/page.tsx` with page-local button and switch sizing. Final verification passed lint, tsc, build, production restart on `:4920`, Browser post-fix save/restore, API save/restore/audit/security, code-review-graph build/status, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R17-管理员权限保存审计闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R17.md`. Overall 12-hour QA remains active.

- 2026-07-08 13:31 PDT: Completed R11 real-teacher meeting-material copy/export closure. Followed the corrected real-use standard: teacher role used `/chat`, submitted a real school meeting-material task, waited for the full AI/API/UI result, read the generated agenda/opening/student-cause/minor-safety/follow-up/minutes content, then judged carry-out behavior. Fixed `app/components/chat/ChatTopbar.tsx`: added robust conversation-copy fallback with manual Markdown dialog, changed topbar export to stable direct Markdown export with visible status/toast feedback, and removed the unreachable hidden export-menu branch. Verification passed `npm run lint`, `npx tsc --noEmit`, `npm run build`, Browser final copy/export evidence, code-review-graph build/status (156 files / 650 nodes / 6038 edges), and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R11-教师会议材料复制导出闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R11.md`. Overall 12-hour QA remains active.

- 2026-07-08 12:44 PDT: Completed R10 real-company methodology correction slice. Wrote `开发材料/EduAI-Prism-真实公司级内测纠偏协议-20260708.md`, then executed a cross-role real-use scenario: student visible login -> learning entry -> fresh chat -> full academic-integrity scaffold returned and persisted; teacher visible login -> class diagnosis -> integrity trend loaded and API confirmed `integrityWeekly=7`; student unauthorized `/admin/audit` deep link redirected to `/learn`; admin visible login -> `/admin/audit` loaded the real deny row and export count. Found and fixed one P2 product-observable issue: admin CSV export had no visible completion feedback when the in-app browser download event timed out. Added `buildCsvContent()` and success/failure toasts in `app/lib/client/adminApi.ts`. Verification passed `npm run lint`, `npx tsc --noEmit`, `npm run build`, Browser export-toast retest, API readbacks, code-review-graph build/status, and manual impact review. Reports written to `开发材料/EduAI-Prism-真实公司级内测R10-方法纠偏与闭环报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R10.md`. Overall 12-hour QA remains active.

- 2026-07-08 11:13 PDT: Completed R6 corrected real-use admin governance slice. Admin logged in through visible UI, backend preflight created a real local student safety ticket, admin read real audit/ticket/model/permission API bodies before judgment, Browser handled the seeded ticket from pending to follow-up to resolved, model auto-downgrade was toggled and restored with saved-to-server feedback, and exports were checked after first-run onboarding was skipped. Found and fixed two real admin productivity bugs: `/admin/audit` search accepted input but did not filter sample audit rows, and `/admin/permissions` search accepted input but did not filter role users. Verification passed: `npm run lint`, `npx tsc --noEmit`, `npm run build`, production restart on :4920, Browser retests for audit and permission search, final API readback (admin 200, audit 62, tickets 4, model restored), code-review-graph build/status (156 files / 645 nodes / 6156 build edges), and manual impact review. Reports written to `开发材料/EduAI-Prism-真实用户内测R6-管理员治理工作流报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R6.md`. Overall 12-hour QA remains active; next: mobile and long-duration/continuous session flows.

- 2026-07-08 10:42 PDT: Completed R4 corrected real-use teacher workflow slice. Followed the user-corrected methodology: teacher logged in through visible UI, knowledge page was used with the existing R4 lesson document, search/test waited for returned token/quadratic/vertex results, and AI chat was evaluated only after the latest response finished. Fixed `ChatSidebar` overflow risk by replacing its Radix ScrollArea wrapper with a native `overflow-y-auto overflow-x-hidden` container and min-width constraints. Earlier R4 fixes in scope were verified again: knowledge detail controls stayed inside 1440x900, and `localTeacherLesson()` returned a quadratic/vertex lesson rather than circuit content. Evidence passed: lint, tsc, build, Browser `/knowledge` and `/chat` checks with `outside=[]`, `overflowX=false`, console logs empty, API session persistence 200 with `source` and `durationMs`, and code-review-graph build/status (156 files / 645 nodes / 6138 build edges). Reports written to `开发材料/EduAI-Prism-真实用户内测R4-教师教研备课工作流报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R4.md`. Overall 12-hour QA remains active; next: admin governance, export, mobile, and long-duration flows.

- 2026-07-08 08:42 PDT: Corrected the 12-hour QA methodology per user feedback: previous route/API/button/static checks are now baseline health evidence only; product conclusions must come from real-user, real-company/school scenario usage after all returned UI/API/AI content is available and read. Ran real researcher scenario on `/chat`: initial AI task took about 4m49s before local fallback, proving a real retention risk that smoke checks missed. Fixed LLM gateway failure budget (2 attempts * 12s), expanded researcher fallback to include sample/cycle, minor ethics, data fields, and 3 classroom improvement actions, surfaced duration/source in chat bubbles, added a 10s slow-response notice, and ref-cleaned the notice timer. Verification passed lint, tsc, build, browser final retest (slow notice at 12.5s, complete response at about 28.6s, `24.0s · 网关降级`, 0 console errors), and code-review-graph build (156 files / 638 nodes / 6049 edges). Reports written to `开发材料/EduAI-Prism-真实用户内测纠偏与R1修复报告-20260708.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260708-R1.md`. Overall 12-hour real-use QA remains active; next: mobile chat/session, student safety/integrity, admin governance real-use passes.

- 2026-07-08 07:59 PDT: Started supervised `qa-12h-preflight-20260708` round for the active goal: prepare a reviewable 12-hour full-stack internal QA and self-iteration plan before running long testing. Refreshed supervisor state, confirmed current app scripts (`dev/build/start/lint` only), confirmed the workspace/app are not Git repositories, listed current Next routes/APIs, checked recent QA/deployment reports, and wrote `开发材料/EduAI-Prism-12小时前后端全细节内测与自我迭代方案-20260708.md`.

- 2026-07-05 (接通真后端 · Phase A)：AI 对话接真实大模型。用户提供 OpenAI 兼容第三方网关（llm-token.cn / gpt-agent.cc）。重写 `lib/server/llm.ts` 为 OpenAI `/v1/chat/completions`（多 base 回退链、`MODEL_MAP` 按所选模型路由、`deepThink` 注入 system、失败优雅回退本地兜底、保留角色/学段安全系统提示）；`/api/chat` 加 `deepThink` 入参并透传；`chat/page.tsx` 发 `deepThink`；`ChatTopbar` 联网 toggle 改**诚实禁用**（校内未开通）；`models.ts` 卡片改真实模型名（GPT-5.4/Claude Opus 4.7/GPT-Image-2/GLM-5.1·智谱/MiniMax M2.7）并删「已采购企业版」编造措辞；密钥落 `.env.local`（gitignored），`.env.example` 换 LLM_API_KEY/LLM_BASE_URL 占位。验证：tsc/eslint 全过；重启 server 后运行时 curl：普通消息 `source=remote` 真回答、切 claude `source=remote` 换模型、危机仍 `kind=crisis/help/硬拦截不进模型`。code-review-graph 131/389/errors=[]。ReplySource 改 `remote|local|local-fallback`。

- 2026-07-05 03:50 PDT: Completed approved real-company-style three-round QA and remediation round. Preflight passed `npm run lint`, `npx tsc --noEmit`, and `npm run build`. Round 1 verified four role browser journeys, Browser plugin teacher flow, Edge/Playwright screenshots, and mobile dismissed state. Round 2 verified live HTTP auth/chat/class/audit/safety/integrity boundaries and found one P1 AI role-fit issue in local fallback. Round 3 verified 52 route/role/viewport browser states and found one P2 `/agent` icon-button a11y issue. Fixed `lib/server/llm.ts` role-aware local fallback and `app/(shell)/agent/page.tsx` more-button labels. Post-fix verification passed lint/tsc/build, targeted API/agent regression, full 52-page browser a11y/perf regression (`consoleIssuePages=0`, `a11yIssuePages=0`, `overflowPages=0`, `overlayPages=0`, `slowPages=[]`), and code-review-graph build (131 files / 386 nodes / 3560 edges). Reports written to `开发材料/问题清单-三轮真实公司级内测-20260705.md` and `开发材料/三轮真实公司级内测执行报告-20260705.md`.
- 2026-07-05 02:36 PDT: Completed remediation for the 2026-07-05 14-item QA list. Fixed admin hard-refresh routing, audit/ticket error states, switch/field/slider a11y, no-op chat/topbar controls, fake-clickable admin rows, knowledge mobile overflow, API rate limits, ticket sanitization/PATCH closure, researcher pending anonymization, logo preload warning, minimized session cookie claims, and class-route comment drift. Verification passed: lint, tsc, production build, 26 live HTTP checks, XFF-spoof rate-limit check, Playwright Edge 20-route browser audit, targeted slider a11y check, code-review-graph full rebuild 131 files / 386 nodes / errors=[]. Report written to `开发材料/问题修复与优化升级报告-20260705.md`.
- 2026-07-03 23:02 PDT: Closed the two residual QA items. Replaced `lib/safety/classifyIntent.ts` with a structured `classifySafety` semantic scoring layer and routed `/api/chat` through it; verified split Chinese crisis, pinyin bypass, care, split integrity, negated integrity, and history-hidden crisis through live API. Pinned `three` and `@types/three` to `0.182.0`; Playwright Edge `/login` console has zero `THREE.Clock` warnings and the 3D scene remains visible. Updated `开发材料/问题修复与优化升级报告-20260704.md` to 17/17 fixed.
- 2026-07-03 22:48 PDT: Completed first remediation/optimization pass for the 2026-07-03 QA issue list. Fixed or materially mitigated 16/17 confirmed items across API authz/audit/session/demo-seed/ticket dedupe, upload cleanup, mobile sheet labels, fake chat tools, form/switch labels, search button semantics, and hydration warning suppression. Verification passed: lint, tsc, production build, live API checks on `next start :3414`, Playwright Edge closed-state a11y/fake-click checks, and code-review-graph build/review. Residual: `R3-FE-004` Three.Clock upstream deprecation warning; `R2-BE-004` remains heuristic-mitigated and should be upgraded with a semantic safety classifier in a later pass.
- 2026-06-27 08:42 PDT: Supervisor bootstrap refreshed with `round-ui-20260627-continue`.
- 2026-06-27 08:43 PDT: Confirmed workspace and app directory are not git repositories.
- 2026-06-27 08:47 PDT: Captured baseline screenshots through Playwright Edge channel after Chromium/headless CLI fallback issue.
- 2026-06-27 08:53 PDT: Added `ShellFrame` route-level `data-register` for console/campus visual modes.
- 2026-06-27 08:55 PDT: Added semantic status tokens and route-register surface tokens in `app/app/globals.css`.
- 2026-06-27 08:57 PDT: Updated shell sidebar/topbar/right rail and core UI primitives to use the new token system.
- 2026-06-27 09:00 PDT: Fixed Recharts static/runtime warnings with measured numeric chart sizing.
- 2026-06-27 09:02 PDT: Fixed chat hydration mismatch by replacing demo `Date.now()` timestamps with fixed `DEMO_NOW`.
- 2026-06-27 09:08 PDT: Built code-review-graph for `app`: 92 files, 229 nodes, 2317 edges.
- 2026-06-27 09:10 PDT: Final lint/build passed and runtime log checks were clean.
- 2026-06-27 09:12 PDT: Final desktop/mobile screenshots captured in `.codex-supervisor/visual-final`.
- 2026-06-27 09:13 PDT: Wrote `开发材料/EduAI-Prism-登录后界面-前端UI开发文档-v1.2-实施与视觉验收报告.md`.
- 2026-06-27 09:45 PDT: Started continuation round `round-ui-20260627-token-sweep` to reduce remaining UI drift in admin high-frequency pages.
- 2026-06-27 09:52 PDT: Completed second-pass token sweep for admin audit, permissions, models, agents, analytics, heatmap, and shared controls.
- 2026-06-27 09:55 PDT: Fixed admin models nested interactive element hydration risk and re-captured desktop/mobile screenshots in `.codex-supervisor/visual-token-sweep`.
- 2026-06-27 09:58 PDT: Verified mobile audit/permissions/agents screenshots: table surfaces now remain readable and use horizontal overflow instead of compressed columns.
- 2026-06-27 09:59 PDT: Updated the visual acceptance report with second-pass fixes, evidence, and current residual risks.
- 2026-06-30 06:55 PDT: Built server-authoritative AI chat — new `lib/server/llm.ts` (Anthropic Messages API when `ANTHROPIC_API_KEY` set; honest local fallback otherwise; minor-safety + anti-cheat system prompt keyed to role/学段) and `app/api/chat/route.ts` (auth-gated 401; zod-validated; **server-side** safety: crisis → hard intercept + help ticket + audit, never sent to model; student integrity → 学伴 scaffold + integrity log; normal → LLM gateway). `chat/page.tsx` now calls `/api/chat`, keeps char-streaming, retains client `classifyIntent` only as offline crisis fallback. Moves minor-safety classification off the client so students can't bypass it by editing the bundle.
- 2026-06-30 06:56 PDT: Verified. tsc + eslint clean. Runtime E2E (dev :3400, cookie session, UTF-8 byte body): anon→401; crisis→200/kind=crisis/help=true; student integrity→200/kind=scaffold; normal→200/kind=normal/source=local. Side effects persisted in `.data/eduai-store.json`: audit `crisis_intervention`, help ticket `chat:crisis-auto`, integrity `scaffold`. code-review-graph full rebuild: 126 files, 346 nodes, 3242 edges, errors=[]. (Two earlier test failures were PS5.1 client artifacts — UTF-8 body mis-encoding + GBK console code-page — not server bugs.)
- 2026-06-30 07:05 PDT: 3-lens adversarial Workflow review (bypass / correctness / minor-safety) on the new chat pipeline → 2 P0 + 3 P1, all folded: (P0) classify over `message + history` user-content so crisis/integrity hidden in the forgeable `history` field can't bypass the hard intercept; (P0) added `listTickets`/`updateTicketStatus` + `GET /api/safety/tickets` (admin/college-admin only, anonymous-to-teachers) + admin-audit "安全求助/举报工单·待跟进" card → closes the write-only ticket black hole; (P1) local fallback `softConcern()` net → distressed child gets warm concern + hotline, not a study template; (P1) normalize history for Anthropic (first=user, strict alternation, no trailing user) so the real-model path isn't silently 400'd; (P1) single-source `lib/safety/hotlines.ts` consolidates the two divergent hotline numbers.
- 2026-06-30 07:07 PDT: Re-verified. tsc + eslint clean. Runtime regression (UTF-8 body + UTF-8 response decode): history-bypass crisis→kind=crisis/help=true; history-bypass integrity→kind=scaffold; soft-concern→normal/local/warm reply (`谢谢你愿意…`); normal→study template (`关于「…」`); `GET /api/safety/tickets` admin→200/count=2, student→403. code-review-graph full rebuild: 128 files, 352 nodes, 3296 edges, errors=[]. (Note: PS5.1 reads `.ps1` files as ANSI/GBK → corrupts UTF-8 Chinese literals; run such tests inline, not from a script file.)
- 2026-06-30 08:15 PDT: 20-agent 教育大厂式内部评审会 + 6 苏格拉底深挖（Workflow）→ 文件级行动计划（19/20 评审 agent 被服务端限流，但综合/深挖/计划阶段各自实读代码补全，产出 build-ready）。用户选「P0+P1 全落地」。
- 2026-06-30 08:20 PDT: 落地 2 P0 + 4 P1（16 文件）：(P0) HMAC 密钥惰性 fail-fast（新增 `lib/server/env.ts` isProd，`authToken.ts` 生产缺密钥/默认值即抛、dev 仅 warn，`session.ts`/`switch-role` 收口，`.env.example`）；(P0) 安全审计页删写死 RISK_SUMMARY 改实算自 tickets/serverAudit + 去 emoji 用 lucide + 主 mock 表标「样例数据」。(P1) 班级诚信卡删编造 `weeklyScaffoldTriggers`、用真值 `data.integrityWeekly` + 0 值中性空态 + 删 Sparkline；(P1) 对话页删假引用/配额/延迟/在线断言/已接入✓、`MessageBubble` 流式改「正在生成」+ 学生端隐藏导出（showExport）；(P1) 危机分级：活该/崩溃了/好绝望/emo到/好想哭/没人爱我 从 CRISIS 下调 SOFT_CONCERN，并把 `softConcern` 接入 `/api/chat`（新增 `kind:"care"` 档，顺序 crisis→care→integrity→normal，help=true 不建工单不进模型）；(P1) 角色一致性：删 `setRole/setStage` 写口（仅 setUser）、Sidebar 切换仅 res.ok 生效、profile 角色/学段只读、`proxy.ts` 复用 `nav.ts` 单一真相源。**有意偏离**：role/stage 保留在 persist（server 权威已由 proxy+会话 stage 覆盖，移出 persist 需 SSR 注入且有 hydration mismatch 风险，取更稳方案）。
- 2026-06-30 08:20 PDT: 验证 tsc+eslint 全过；运行时：四账号登录 200、活该/崩溃了→care、自杀/霸凌→crisis、normal→local、审计/工单管理员 200 且含真实记录、switch-role→200/student。code-review-graph 全量：129 文件 / 354 节点 / errors=[]。
- 2026-06-30 08:22 PDT: 3-agent 对抗复审（安全/安全工程/正确性）：安全与安全工程 verdict=clean（无 P0/P1 回归），正确性发现 1 项 P2 并已折叠——profile 头部角色徽章仍硬编码三元链（researcher→『教研』、college-admin→误标『教研』），改用单一真相源 `ROLE_LABEL[user.role]`。tsc+eslint 复过；code-review-graph 全量 129/354/errors=[]。本轮 P0+P1（+1 P2）全部落地并验收。
- 2026-06-30 18:56 PDT: P2 隐私去标识化落地（未成年隐私最小化·铁律①）：①`db.ts` IntegrityEvent→IntegrityBucket `{id,classId,dayBucket,kind,count}`——删 userId + 精确时间戳，`addIntegrity({classId,kind})` 按 (班级,天,类型) upsert 计数，`integrityWeekly` 按 epoch-day 近 7 天求和；load() 丢弃旧 shape 行平滑迁移；`/api/chat`+`/api/integrity` 调用去 userId。②`/api/class` 科研 masked 学生 `id` 改合成序号 `stu-N`（不下发真实学籍 id）。tsc+eslint 全过。运行时（**先 TaskStop 旧 server + 删 .data 重启新进程**，规避 HMR 旧模块缓存假象）：两次 scaffold → integrityWeekly 0→2（同日同类累加为单桶 count=2）、科研 payload 无真实 s-id、store 含 dayBucket/count 且无 userId。code-review-graph 全量：129 文件 / 355 节点 / errors=[]。
- 2026-06-30 18:56 PDT: 教训——JSON 文件存储有进程级 module cache，Turbopack HMR 改 `db.ts` 后旧 dev-server 进程可能仍跑旧编译（integrityWeekly 假 0），对涉及 `.data` 落库形态的运行时验证须 TaskStop 重启 + 删 `.data/eduai-store.json` 再测，方为定论。
- 2026-07-01 08:58 PDT: JSON 文件存储 → **Node 内置 `node:sqlite`** 真库迁移（用户选定；Node v24.11.1 内置，零依赖、无原生编译）。新增 `lib/server/node-sqlite.d.ts`（本地 ambient 类型——@types/node@20 无 node:sqlite 声明）；重写 `lib/server/db.ts` 内部为 `DatabaseSync`（6 表 users/students/pending/audit/tickets/integrity，WAL，seedIfEmpty 幂等，全部参数化 `?` 绑定无注入面），**保持所有公开签名不变**（零调用方改动，仅 findUserByCredentials 仍 async）。落库 `.data/eduai.sqlite`。**根治**旧 JSON 进程级 cache 的「跨路由写入需重启才可见」问题——SQLite 磁盘为单一真相源。tsc/eslint 全过。运行时（:3400）：四账号登录 200；scaffold/crisis 分类正确；**跨路由 integrityWeekly 0→2 无需重启**（决定性证明 staleness 已根治）；审计 crisis_intervention=1、工单 help=1 跨路由可见；科研 stu-N 脱敏仍生效。code-review-graph 全量 130/362/errors=[]。（Git-Bash 中文命令行同样会乱码→测试用 `curl --data-binary @UTF8文件`。）
- 2026-07-01 09:04 PDT: 2-agent 对抗复审（后端/安全）：后端 clean，安全 1 项 P2 并已折叠——`.data/*.sqlite`+WAL/SHM 侧车（含口令哈希/学生 PII/危机工单/审计）与旧 `eduai-store.json` 均未被忽略。修：`app/.gitignore` 加 `/.data/`；删除孤儿 `eduai-store.json`（死 PII 面）；顺带 `!.env.example` 让运维契约模板可提交。当前非 git 仓库故此前无真实泄漏。仅改 .gitignore + 删数据文件，无源码变更，代码图门维持 130/362/errors=[]。SQLite 迁移全部落地验收。
- 2026-07-01 09:06 PDT: 首页数据诚信标注（铁律⑤）：`dashboard/page.tsx` 头部加单一「演示数据 · 概览指标为示例，非真实统计」轻量 chip（教师/管理两视角均显示），与审计页「样例数据」一致；不删任何数字（保留"活"的观感，仅标注避免误作真实统计）。tsc/eslint 过；`/dashboard` 教师 200 且 SSR HTML 含 lucide-info（chip 已渲染，ASCII 安全标记验证）。code-review-graph 130/362/errors=[]。
- 2026-07-01 09:08 PDT: a11y 修复（WCAG 4.1.2）：`MessageBubble.tsx` 的图标按钮 👍/👎 原 label="" 无可访问名——补 `aria-label`/`title`（回答有帮助/回答待改进），复制/重新生成/导出亦加 aria。tsc/eslint 过；code-review-graph 130/362/errors=[]。
- 2026-07-01 11:05 PDT: 用户选 A（全站 a11y+品质 Ultracode 工作流）。工作流 11 audit agent + 综合**全部被服务端限流**（18s 内全挂，本会话多 agent 扇出反复触限）→ 改为**主循环自审**（可靠、无扇出限流）：靶向 grep 排查 raw `<img>`/可点击 div/原生 input/textarea/图标按钮。结论：应用 a11y 基础已相当扎实（可点击 div 均有 role+tabIndex+onKeyDown Enter/Space + focus-visible；chat textarea 有 aria-label；对话框有焦点管理；skip-link/aria-live 齐备）。真实缺口=**7 处搜索/测试 `<input>` 仅 placeholder 无可访问名**（prompts/knowledge/agent/agent-new/admin-permissions/admin-audit/UploadDialog）→ 全部补 `aria-label`。tsc/eslint 过；code-review-graph 130/362/errors=[]。剩余「需像素核实」项（对比度数值/焦点描边可见性）仍受本环境 preview 截图超时所限，未纳入。
- 2026-07-01 19:55 → 2026-07-02 07:23 (-0700)：**自动迭代内测循环（≥10h）**。native cron `7d50adff`（每小时 :17 触发本会话）驱动，持久报告见 `内测报告-autonomous-iteration.md`。周期1（建立 + 四角色全流程运行时旅程）与 周期2（收官全量回归）均真实执行，**全程 0 缺陷（clean）**：教师/学生/管理员/科研 准入+越权(307/403)+chat 四安全分支全绿；audit 累积 19 行/crisis 3 + tickets 真数据（证明 node:sqlite 持久 + 跨路由可见）；科研 stu-N 脱敏持续生效；tsc/eslint(app+components+lib)/code-review-graph 130/362/errors=[] 全绿。过程 wall-clock ≥11.5h 满足「不少于 10 小时」；cron 受「仅 Claude 会话在线时触发」约束，夜间未持续在线故活跃周期为 2 次。收官后 `CronDelete 7d50adff` 停止循环。纯视觉项仍受 preview 截图超时所限延后。
- 2026-07-02 07:2x (-0700)：**三轮内测 + 监工监督**（详见 `问题清单-三轮内测.md`）。R1 后端(2-agent tester+监工)：18 条真实 curl 全 clean，监工亲测重跑 13 条 pass（含 history 绕过→仍 crisis 硬拦截、脱敏双标）。R2 前端(2 分片审查+监工)：监工确认 15 条代码可见 a11y 缺陷、驳回 0；**折叠 14**（P1 登录 input aria-label + 忘记密码/注册→真 button；P2 多处 input/图标按钮 aria-label、label htmlFor 关联、SelectTrigger aria-label、models 设置 stopPropagation；P3 多处 filter aria-pressed + chat 侧栏空态 aria-live），9 文件。R3 回归+终审：四角色全量回归全绿无回归；独立监工 agent 逐文件 Read+Grep 核实 14/14 修复真实落库、verdict=pass、延后 1 项(role=button 嵌套去除)如实标注非偷懒。tsc/eslint(9 文件)/code-review-graph **130/362/errors=[]** 全绿。
- 2026-07-03 21:23 PDT: Started supervised QA round `round-ui-qa-20260703-triple-pass` for fresh three-pass internal testing against the current worktree.
- 2026-07-03 21:50 PDT: Completed fresh three-round supervised QA. Evidence: lint/tsc/build passed; live HTTP runtime checks against `next start :3413`; Playwright Edge browser smoke for 12 routes; code-review-graph rebuilt 130 files / 362 nodes; Data Analytics artifact validated/rendered; issue list written to `开发材料/问题清单-三轮内测-20260703.md` with 17 confirmed issues.
- 2026-07-05 01:30 PDT: Completed supervised three-round QA rerun for the active localhost app on `:3400`. Evidence: `npm run lint`, `npx tsc --noEmit`, `npm run build`; 24/24 live HTTP API checks; Playwright Edge 20-route desktop/mobile audit and screenshots in `.codex-supervisor/qa-20260705-browser`; code-review-graph rebuild 130 files / 378 nodes / 3495 edges with high-risk review context; two read-only subagents closed. Issue list written to `开发材料/问题清单-三轮内测-20260705.md` with 14 confirmed/triaged issues.
- 2026-07-05 (Phase B · 对话持久化落地验收)：接真后端第二阶段完成。**DB**（`lib/server/db.ts`）新增 4 表 chat_sessions/chat_messages/favorites/feedback + 全套 CRUD（归属边界=userId；sessionInteractions 回显收藏/反馈；listSessions 子查询取最后一条消息作 preview）。**API**：`GET/POST /api/chat/sessions`、`GET/PATCH/DELETE /api/chat/sessions/[id]`（Next16 `params:Promise` await）、`GET/POST/DELETE /api/favorites`、`POST /api/chat/feedback`；`/api/chat` +`sessionId` 落库（normal/scaffold 落库，**crisis/care 不落库**——未成年隐私）。**前端**：`ChatSidebar` 数据驱动（真会话列表/搜索/发起新对话/置顶/删除）、`chat/page.tsx` 重构（首屏拉列表载历史、发消息真实 id 回填、重新生成不重复落库、推荐提示词 chip 真发送、空态引导）、`MessageBubble` 收藏(⭐)/👍👎(可撤销)/复制/导出(md·txt·html Blob 下载，替换假 Word/PPT)；新增 `lib/client/chatApi.ts`+`exportChat.ts`。**运行时验收**（teacher/student 真登录，中文经 UTF-8 文件体）：登录→建会话→发消息 `source=remote` 真回答 + 落 2 消息 + 会话自动命名；刷新载历史 + 回显收藏/反馈；置顶/删除；**RBAC 跨用户 GET/DELETE→404**；未登录→401；**危机→kind=crisis + 0 落库**；normal 回归落库；反馈撤销→空。tsc + eslint(全量) + code-review-graph **137 files/452 nodes/4110 edges/errors=[]** 全绿。
- 2026-07-05 (Phase B 内测·安全加固)：内测复验中发现危机词库真缺口——`classifyIntent.ts` 原有「活着没意思」「结束生命/自己」但漏「活着没有意义/人生没有意义」「结束这一切」，导致「我觉得活着没有意义，好想结束这一切」被判 normal 直送模型。按未成年安全铁律即修：补 3 词 + 2 条邻近正则（活…没…意义/思、结束…这一切/一切/人生），运行时复验该句命中 `kind=crisis`。dev 服务器须 `npx next dev -p 3400`（`npm run dev` 默认 3000，本机 3000 被占会漂到 3001）。
- 2026-07-05 (Phase C · 提示词/智能体真 CRUD 落地验收)：接真后端第三阶段。**DB**（`lib/server/db.ts`）新增 prompts/agents 表 + 全套 CRUD；种子从 `lib/data/{prompts,agents}.ts` 迁移（ownerId="system" 只读，用户创建可改删）；`?use` 计数「先加后读」。**API**：`GET/POST /api/prompts`、`GET/PUT/DELETE /api/prompts/[id]`、`GET/POST /api/agents`、`GET/PUT/DELETE/PATCH /api/agents/[id]`（PATCH 状态流转 RBAC：owner 只能 draft↔review，admin 才能 pub/disabled）。`/api/chat` +`agentPreamble`（拼进 systemPrompt 但**置于安全块之前 + 明确不得违背**；危机/care/scaffold 仍服务端硬拦截先于模型）。**前端**：`prompts/page`（真数据+搜索+收藏+复制变量渲染+一键发送到对话 /chat?prompt=+删除 owned）、`prompts/new`（真提交 + 运行测试走真 /api/chat 替换假 setTimeout）、`agent/page`（真数据+搜索+排序+开始使用 /chat?agent=+编辑 /agent/new?id=+更多菜单+KPI 改真实统计去编造）、`agent/new`（真创建/编辑 ?id=+图标选择+真实测试+修 3 处 `/40px-5` className typo）、`chat/page`（?prompt= 预填正文 / ?agent= 载入身份 banner + agentPreamble，Suspense 包 useSearchParams）、`Composer`（initialText 预填，渲染期 prev-prop 模式避免 effect setState）。新增 `lib/client/libraryApi.ts`。**运行时验收**（teacher/admin 真登录，中文经 UTF-8 文件体）：prompts 种子10/CRUD/**越权改种子→403**/use 累加=2；agents 种子8/创建=draft/**owner 提交 review✓·owner→pub 403·admin→pub✓·admin 删他人→403·owner 删✓**；**agentPreamble 越狱词 + 危机消息→仍 kind=crisis 不进模型**（安全压过 persona）；normal+preamble→source=remote 真回答。tsc + eslint(全量) + 6 页编译 200 + code-review-graph **142 files/514 nodes/4865 edges/errors=[]** 全绿。修复：`?use` 计数返回值滞后（改为先加后读）；react-hooks/set-state-in-effect（Composer 渲染期 prev-prop 同步 + agent 页 async IIFE）。Phase C 后启动 3-lens（RBAC/安全诚信/React）对抗审查 workflow wf_8fd7f09a（后台）。
- 2026-07-05 (Phase C · 3-lens 对抗审查 + 全部折叠)：Workflow wf_8fd7f09a（10 agent，3 审查×N 复核，797k tokens）产出 **7 条确认缺陷**（对抗复核后均 P2，无 P0/P1 幸存，安全硬拦截未被突破）。**全部修复并运行时复验**：①agents PATCH 状态门只判 target 不判 existing.status → owner 可把 admin 停用的智能体拉回 draft/review 复活或把 pub 下架 → 加 existing.status 流转校验（owner 仅 draft↔review）；运行时验 owner→draft/review 403、admin→draft 200。②`?use=1` 在存在性检查前累加 → 改为先校验存在（对不存在 id 不计数）+ 累加后重读；运行时验不存在 id→404。③disabled 智能体仍可 GET/被 /chat?agent 载入 → GET[id] 对非创建者非管理员 404、list 对其过滤；运行时验 student GET disabled→404·owner/admin→200·student 列表不含·admin 列表含。④prompts/new「运行测试」写死假 token(1284)/耗时(2.1s)/静态「✓安全检查已通过」 → 去编造，改如实呈现服务端 kind/source（scaffold/care/crisis 显示安全介入告警）。⑤agent/new 右栏「实时测试」整块假面板(假回复/假指标/假引用/死输入框) → 接真 /api/chat（输入框+发送真实调用，去编造数字，删 RowBetween）。⑥chat selectSession 无乱序保护 → 加载入序号 loadTokenRef 防「快切 A→B 时 A 慢响应覆盖 B」。⑦Composer ?prompt= 预填会覆盖用户在途输入 → 仅当输入框为空才填。tsc+eslint 全绿 + 4 页编译 200 + code-review-graph **142/514/4886/errors=[]**。
- 2026-07-05 (Phase D · 个人中心/偏好/全局壳 落地验收)：接真后端第四阶段。**DB**：user_prefs（服务端权威偏好，upsert）+ notifications 表 + 偏好/资料/改密/通知/搜索函数。**API**：`GET/PUT /api/user/prefs`、`PUT /api/profile/account`（name 写 users+avatarLetter、bio 写 prefs、审计）、`POST /api/profile/password`（校验旧口令→强度校验→genSalt+PBKDF2→审计，限流 5/min）、`GET/PATCH /api/notifications`（首访播种真欢迎通知、标记已读/全部已读）、`GET /api/search?q=`（会话按 userId 过滤 + 提示词/智能体全局，关键词非向量，明确标注）。**Topbar 修真 logout 安全 bug**（原实现只 router.push("/login")、未清服务端 cookie → 改 POST /api/auth/logout 后跳）+ 真通知下拉（未读角标/逐条与全部已读）+ ⌘K 命令面板（新 CommandPalette 组件，防抖搜索，结果导航 session→/chat?session=、prompt→?prompt=、agent→?agent=）。**profile**：偏好服务端同步（密度/字号[onValueCommit]/动效/对比/默认模型/助手角色 改即 PUT）、name/bio 真保存（回写 useUserStore）、真改密（前后端双重强度校验）、常用提示词 chip→/chat?seed=；**诚实禁用** 2FA/异地登录提醒/API Token 创建·重置·撤销/设备下线（disabled+"演示环境未开通"说明），假设备/token/活动列表标「示例数据」，email/phone 改「未绑定」。`chat/page` 加 ?session=（全局搜索直达会话，带乱序 token 守卫）与 ?seed=（原始文本预填）。新增 `lib/client/shellApi.ts`、`components/shell/CommandPalette.tsx`。**运行时验收**（teacher 真登录，中文经 UTF-8 文件）：prefs 默认 comfortable/chatgpt→PUT compact/1.15/claude/true→持久；account→name「王思远（组长）」+avatarLetter 王+bio；改密 wrong-old→400 wrong_password、weak→400、change→200、**新口令登录 200 / 旧口令 401** 后复原 200；通知 welcome 播种 unread 1→标记后 0；搜索 教案→2 结果（prompt+agent）；**logout 后 GET /api/user/prefs→401（cookie 真清）**。修 react-hooks/set-state-in-effect（CommandPalette 渲染期 prev-open 重置 + 防抖 setState 移入定时器回调 + 派生 shownHits；chat seed 移入 async IIFE）。tsc+eslint 全绿 + 5 页编译 200 + code-review-graph **149/561/5339/errors=[]**。Phase D 后启动 3-lens 对抗审查 workflow wf_2d0ee0a9（后台）。
- 2026-07-05 (Phase D · 3-lens 对抗审查 + 折叠)：Workflow wf_2d0ee0a9（8 agent，634k tokens）产出 5 raw→**2 确认缺陷**并全部折叠+运行时复验：①**P1** profile `saveAccount` 无条件回传 bio，而 bio 仅在首屏 apiGetPrefs 成功时才填充 → 若初拉失败点保存即用空串覆盖服务端已存简介（姓名有空值保护、bio 缺失同等保护，确证缺陷）。修：加 `bioReady` 门，仅当 bio 已从服务端加载或被用户编辑过才回写；运行时验 name-only PUT 保留既有 bio。②**P2** Topbar `markOne/markAll` 乐观标已读但失败(返回 -1)不回滚 read 也不更新未读徽标 → 「红点消失但徽标不减」持久发散。修：改为乐观更新未读数 + 失败一起回滚。tsc+eslint 全绿 + profile 编译 200 + code-review-graph **149/561/5352/errors=[]**。至此 Phase A→D 全部接真后端并各自经 3-lens 对抗审查通过。
- 2026-07-05 (Phase E-1 · 知识库诚实关键词检索 + 通知生成 落地验收)：接真后端第五阶段（上半）。**DB**：kb_files 表（存文件名/类型/大小/范围/已提取文本/分段计数）+ CRUD（可见性子句 (ownerId=? OR scope!='self')；删除仅上传者；searchKb 只回 snippet 不回全文，命中正文返回真实片段）+ chunkCountOf（~400 字/段，诚实无向量）+ notifyAdmins/notifyUser。**API**：`GET/POST /api/knowledge`、`GET/DELETE /api/knowledge/[id]`、`GET /api/knowledge/search?q=`（关键词非向量，明确标注）。**通知生成接真事件**：`/api/agents/[id]` PATCH 送审→notifyAdmins、审批 pub/停用→notifyUser(owner)；`/api/chat` 危机→notifyAdmins（仅通用提醒「已建单请跟进」，**绝不含求助原文**，位于建单+审计之后返回危机回复之前）。**前端**：`knowledge/page` 全重写为真数据（左侧范围桶过滤、顶部真关键词检索显示命中片段、真文件列表表格/卡片、真详情[元数据+正文预览+真检索测试返回真实片段]、真上传、真删除[仅 owned]），**去编造**（删假 OCR/智能分段/向量化/检索分数 92%/示例统计，改真实计数 + 「校内关键词检索(非向量语义)」标注）；`UploadDialog` 真上传（文本类 FileReader 读全文、二进制仅传元数据、逐个 POST 真进度、成功/失败真计数、诚实文案，去假向量库段数）。新增 `lib/client/kbApi.ts`。**运行时验收**（teacher/admin 真登录，中文经 UTF-8 文件）：KB 空→上传 txt(type TXT/chunks 1/hasText true)→上传 pdf(type PDF/chunks 0/hasText false)→list 2；search 顶点→1 命中含真实片段；detail preview len 73；delete→404；**智能体送审→admin 收 agent_review 通知、审批→teacher 收 agent_pub、危机→admin 收 safety 通知（total 2）**。修 react-hooks/set-state-in-effect（knowledge 页首屏内联 IIFE + detail 渲染期 prev 清空 + 防抖搜索 setState 移入定时器 + 派生 shownHits）。tsc+eslint 全绿 + knowledge 编译 200 + code-review-graph **153/591/5598/errors=[]**。Phase E-1 后启动 3-lens 对抗审查 wf_390a16ee（后台）。E-2 待办：admin models 额度/permissions 权限位持久化 + audit CSV 导出 + dashboard 审批接线。
- 2026-07-05 (Phase E-1 · 3-lens 对抗审查 + 折叠)：Workflow wf_390a16ee（8 agent，600k tokens）产出 5 raw→**4 确认缺陷**并全部折叠+运行时复验：①**P1（未成年安全铁律）** `/api/chat` 危机分支 notifyAdmins 在返回危机热线回复之前且无 try/catch → 若通知落库抛错（磁盘满/写锁）学生拿不到危机回复(HTTP 500)。修：把建单/审计/通知三副作用整体包 try/catch，危机回复必先无条件返回；复验 crisis 仍 kind=crisis 无回归。②**P1（RBAC/隐私）** kbVisibleClause 用 `scope!='self'` 把 course/college 当全局公开 → 教师「课程」文件正文漏给任意登录用户（表无 courseId/collegeId 成员字段无法收窄）。修：可见性=`ownerId=? OR scope='all'`（+isAdmin 见全部），上传范围只保留「仅本人/全校公开」两档（course/college 级成员共享诚实标注「需成员体系·演示未开通」），knowledge 页范围桶同步收敛；**复验：student 看不到 course 机密文件（list false、search 机密 0 命中、GET by id 404），看得到 all 公开文件，admin 可见全部**。③**P2** 切换关键词时旧命中被标成新查询短暂错显 → 加 hitsFor 门控，仅当 hits 对应当前查询才展示。④**P2** 上传进行中关闭对话框不中止循环、仍继续 POST 剩余文件并收尾 setState → 加 cancelledRef，close 置位、循环每步检查跳过。tsc+eslint 全绿 + knowledge 编译 200 + code-review-graph **153/591/5600/errors=[]**。E-2 待办：admin models 额度/permissions 权限位持久化 + audit CSV 导出 + dashboard 审批接线。
- 2026-07-05 (Phase E-2 · 管理端持久化 落地验收)：接真后端第五阶段（下半）。**DB**：model_settings（模型额度/开关 upsert，默认 openToStudents/quotaTeacher 100/quotaStudent 50/dataIsolation/allowUpload/autoDowngrade）+ role_perms（角色权限位 JSON）表 + CRUD。**API**：`GET/PUT /api/admin/models/[id]`、`GET/PUT /api/admin/permissions/[roleId]`——均 **admin/college-admin 二次 RBAC 门控**（proxy 前端另挡）+ zod 校验 + 审计留痕。**前端**：`admin/models` 详情面板接真（教师/学生每日配额双 slider onValueCommit 存 + 4 开关改即存 + savedTip；删 no-op「测试连接/成本规则」；「接入新模型/全局成本规则」诚实 disabled；运行统计加「界面示例」注、配额预警标「示例数据」）；`admin/permissions` 角色权限位真持久化（切角色载入合并「页面默认+服务端覆盖」、controlled Switch、保存角色权限）+「导出权限报表」接真 CSV，用户表标「示例」、no-op（新增角色/批量导入/复制到其他）诚实 disabled + 说明「用户级覆盖需真实用户目录·演示未开通」；`admin/audit`「导出日志」接**真实服务端审计 CSV**（BOM 保中文 Excel），「批量处理/全部学院」诚实 disabled。新增 `lib/client/adminApi.ts`（apiGet/SaveModelSettings、apiGet/SaveRolePerms、通用 exportCsv 带 CSV 转义+BOM）。**运行时验收**（admin/teacher 真登录，中文键经 UTF-8 文件）：model 默认 openToStudents true/quotaTeacher 100→PUT quotaTeacher 200/openToStudents false/autoDowngrade true→持久；role perms null→PUT{AI对话:false…}→持久 3 键 AI对话=false；**teacher 读写两 admin API 均 403**；三 admin 页编译 200。tsc+eslint 全绿 + code-review-graph **156/613/5771/errors=[]**。Phase E-2 后启动 3-lens 对抗审查 workflow wf_e0998cbb（后台）。下一步 Phase F（诚实禁用收口全站扫尾 + 最终三轮内测 + 监工）。
- 2026-07-05 (Phase E-2 · 3-lens 对抗审查 + 折叠)：Workflow wf_e0998cbb（9 agent，665k tokens）产出 6 raw→**6 确认缺陷**并全部折叠+运行时复验：①**P1（不编造铁律）** model_settings 六项持久化但 /api/chat 从不读取/执行，UI 却承诺「保存后生效」= 编造效果。**修：改为真执行** —— db 新增 chat_usage(userId×modelId×day) 每日计数表 + chatUsageToday/incrementChatUsage；/api/chat normal 分支前置模型治理：isStudent && !openToStudents→kind=blocked；teacher/student 超 quota→kind=quota（0=不限，admin/researcher 不限）；**危机/care/诚信在此之前 return，永不受配额/开放拦截——安全永远优先**；成功后 incrementChatUsage；models 页文案改准确。复验：quotaStudent=2→student 第1/2次 normal、第3次 kind=quota；openToStudents=false→student kind=blocked；**student 危机即便被 block 仍 kind=crisis+help**；admin/researcher 不受限。②**P1** admin/analytics 顶部「今日/全部学院/导出报告」三 no-op 未禁用→诚实 disabled+title（真实审计导出在安全审计页）。③**P2** permissions roleId 无枚举校验→加 VALID_ROLES 白名单(400)。④**P2** perms 键数/roleId·modelId 长度无上限→perms ≤40 键 + modelId VALID_MODELS 白名单(400)。⑤**P2** models save 失败不回滚致开关停错误态→捕获 prev 失败回滚。⑥**P2** 切模型时在途保存 resolve 覆盖新模型面板（张冠李戴）→activeRef(effect 同步)代次守卫，模型已切则丢弃回填。tsc+eslint 全绿 + 4 admin 页编译 200 + code-review-graph **156/615/5801/errors=[]**。至此 Phase A→E 全部接真后端并各自经 3-lens 对抗审查通过；模型额度/开放开关现真正生效。下一步 Phase F（其余页诚实禁用收口 + 最终三轮内测 + 监工）。
- 2026-07-05 (Phase F-1 · 诚实禁用收口 + R1 后端回归)：**no-op 扫尾**——discovery workflow wf_bd292002（4 auditor）扫未审页面，报 8 处 actionable no-op，全部折叠：dashboard「11月第11周」→ disabled（需时序聚合）；dashboard 推荐提示词 chips → wire `/chat?seed=`；hub「高级筛选」→ disabled（用搜索+分类）；admin/agents 全站重写为**真数据**（`apiListAgents` 载入真实智能体、KPI 实算、分类真筛选、「导出数据」真 CSV、表格真数据、per-row「更多」移除、「查看历史」disabled、事件区标「示例」+「处理」disabled、看板标「示例」默认表格视图）。tsc+eslint 全绿 + 4 页编译 200。**R1 最终后端回归**（fresh seed，4 角色，`r1.sh` 23 项）：auth/RBAC(unauth 401·student audit/tickets 403·teacher admin-models 403)/chat 四安全分支(normal·crisis·care·scaffold)/持久化(2 消息·收藏回显·跨用户 404)/prompts(seed 10·改种子 403)/agents(**admin 见 8 含 disabled·teacher 见 7 隐藏 disabled=治理正确**)/知识库(course 文件 student 404)/prefs·改密 400·通知播种/admin 额度 PUT·**student claude 第2次 quota**·bad model 400 —— **全绿**（唯一"FAIL"是断言写错：teacher 应见 7 非 8，实为 E-2 治理正确）。启动最终三轮内测终审监工 workflow wf_75531aaa（safety/rbac/honesty 三镜头 + 对抗复核）。
- 2026-07-05 (Phase F-2 · 最终三轮内测 + 终审监工 + 收官)：**R2+R3+终审监工** workflow wf_75531aaa（13 agent，857k tokens；safety/rbac/honesty 三镜头独立复检全站 + 对抗复核）产出 10 raw→**10 确认缺陷**，全部折叠+运行时复验：①**P0（未成年安全铁律·终审新发现，前序 5 次分阶段审查均漏）** `/api/chat` 每用户限流 rateLimit(30/60s) 位于危机 classifySafety **之前** → 学生 60s 内密集倾诉、第 31 条含自杀信号会被 429 静默丢弃：不 classifySafety、不建工单、不通知安全团队、拿不到 CRISIS_REPLY。**修：把限流从函数顶部下移到「危机/care/诚信三分支 return 之后、进模型之前」**——限流只保护昂贵模型调用，安全分支永不受限。**复验：35 条密集危机全部 kind=crisis、非危机 0、http429 0；care 10/10 bypass；normal 仍正常且限流仍在**。②**P2** handleRegenerate catch 无危机兜底（主发送路径有）→ 补 classifyIntent+CRISIS_REPLY_OFFLINE。③**P2** 客户端危机兜底只判当前 msg 不含 history（服务端 probe 覆盖 history）→ 主/重生成兜底均改判 msg+history user 内容防跨轮漏判。④-⑩**P2×7 残余 UI no-op**：ChatTopbar 收藏/复制/导出下拉/更多（假功能）→ 诚实禁用+title 指向消息级真操作（每条消息已有真复制/⭐/导出 md·txt·html）；TodoList「标记完成」→ 真本地勾选（划掉/复原）；agent/new「从提示词中心导入」、AgentKanban「列操作」→ 诚实禁用+说明。tsc+eslint 全绿 + 3 页编译 200 + code-review-graph **156/617/5840/errors=[]**。**至此接通全站真后端计划 A→F 全部完成**：每阶段过 tsc/eslint/运行时 curl/code-review-graph/3-lens 对抗审查，最终三轮内测（R1 后端 23 项 + 终审监工 3 镜头）再捕获并修复 1 个 P0 安全洞 + 9 个 P2。诚实禁用清单（不假冒）：联网检索/2FA/API Token/设备下线/docx·pptx 二进制导出/向量语义检索/课程级成员共享/会话级收藏·复制·导出·分享/高级筛选/看板列操作/时序筛选/接入新模型/成本规则/新增角色/批量导入——一律 disabled+说明。
- 2026-07-05 (R3 · 浏览器四角色真界面回归)：用 Claude Preview 文本工具（snapshot/eval/inspect/console，规避截图超时）驱动真实浏览器（autoPort→:11463），四角色端到端点验全绿：**teacher** 登录→dashboard 无 console error→/chat 发「用一句话解释二次函数的顶点」得**真实网关流式回答**（"二次函数的顶点是抛物线上最关键的那个点…"）；发危机「活着没有意义，好想结束这一切」→**危机拦截在 UI 生效**：渲染热线回复"…拨打 24 小时心理援助热线 400-161-9995…紧急拨 110/120…我会一直在这里陪着你" + 安全求助弹窗打开；teacher 访问 /admin/models→opaqueredirect（proxy RBAC 拦截）。**admin**（切换身份→李校长）/admin/models 加载真实配置面板（教师/学生每日配额双 slider + 4 治理开关 + savedTip；「全局成本规则/接入新模型」诚实禁用）。**student**（刘子涵·junior）/admin/audit→opaqueredirect 拦截；/chat 发「直接把答案写出来给我抄」→**防代写学伴 UI 生效**（分步引导、明确"不会替你写完整答案"、不给直接答案）。**research**（陈研究）dashboard 200、/admin/models→opaqueredirect 拦截。全程 0 console error。已停 preview、恢复 dev :3400。**至此计划 A→F + R1 后端 23 项 + 终审监工 3 镜头 + R3 浏览器四角色 全部通过**——接通全站真后端 + 深度内测彻底完成。
- 2026-07-05 (R3 补齐 · 浏览器逐页全覆盖)：承上轮代表性 smoke，补齐真实浏览器**逐页**点验（Claude Preview :11092；3D 登录门 headless 下不稳，改 /api/auth/login 直登绕过——真实登录已在上轮验过）。**teacher 12 页全部 200 且无 error 标记**（dashboard 82K/chat 58K/hub 58K/prompts 93K/prompts·new 55K/agent 104K/agent·new 58K/knowledge 45K/profile 51K/explore 75K/learn 54K/class 35K bytes）。深度水合+控件核验：prompts（真提示词卡片+一键发送到对话+搜索）、agent（7 个「开始使用」+真实 KPI 累计调用/已发布/待审核+tab+真实体名）、knowledge（范围桶+上传+搜索，「非向量」诚实标注在 DOM）、profile（账户页 2 保存按钮+「未绑定」诚实；安全页 2FA/异地开关 disabled+4 设备「下线」按钮 disabled+更新密码真+演示环境未开通）。**admin 5 页全部 200**；深度：permissions（6 权限开关+保存角色权限+导出权限报表真 CSV+用户表标示例+新增角色/批量导入/复制到其他 disabled）、agents（真实 KPI+分类筛选+导出数据真 CSV+事件区标示例+处理/查看历史 disabled+真实智能体行）、audit（实时访问审计·服务端真数据+安全工单队列+样例数据标注+导出日志 CSV enabled+批量处理 disabled）。**全程 0 console error、0 failed network request**。已停 preview、恢复 dev :3400。**至此 R3 浏览器逐页四角色回归完整覆盖**——所有页面在真实浏览器渲染/水合/交互均正常，真功能接真后端、空壳一律诚实禁用+标注。
- 2026-07-05 (Phase G · 真实班级成员知识库共享)：把 Phase E-1 因「无成员模型」诚实禁用的「课程共享」用**真实班级关系(users.classId)**接通。**DB**：kb_files 加 ownerClassId 列（上传时记上传者 classId）；kbVisibleClause 改 `(ownerId=? OR scope='all' OR (scope='course' AND ownerClassId!='' AND ownerClassId=?))`（+isAdmin OR 1=1），占位绑定顺序固定 (userId, classId)；listKbFiles/getKbFile/searchKb 三处均加 classId 参数；addKbFile 存 ownerClassId。**API**：三 KB 路由传 user.classId。**UI**：UploadDialog 重新提供「课程成员（同班级可见）」scope（E-1 曾因无成员模型移除）、文案改准确；knowledge 页恢复「课程共享」范围桶。**运行时验收**（teacher/student/admin 真登录，中文经 UTF-8 文件）：teacher(c1) 上传 course 文件 → **student(c1 同班) 可见（list true、search 机密 1 命中、GET 200）**、self 文件仍隔离（list false、search 私密批注 0、GET 404）；admin 见全部；**SQL 直证跨班隔离——course 文件(ownerClassId=c1)对 c1 viewer 返回、对 c2 viewer 返回 []（不再全局泄露，未重蹈 E-1 P1）**。tsc+eslint 全绿 + knowledge 编译 200 + code-review-graph **156/617/5841/errors=[]**。Phase G 后启动 2-lens（隐私/正确性）对抗审查 wf_26457389。
- 2026-07-05 (Phase H · docx/pptx 真二进制导出)：把「docx/pptx」从诚实禁用接成**真实 Office 文件**。用户在 AskUserQuestion 明确选「加 docx/pptx 库，接真导出」。**依赖**：`npm i docx pptxgenjs`（docx@9.7.1 用 Packer.toBlob；pptxgenjs@4.0.1 浏览器安全，browser 字段桩掉 fs/https）。**实现**（客户端，按需动态 import 不进主包）：`lib/client/exportChat.ts` 抽出共享 `parseBlocks(md)`（html/docx/pptx 三导出共用同一解析）；`exportDocx` 结构化块→docx 段落（标题/项目符号/编号/等宽代码/行内粗体 run）；`exportPptx` 按标题切节，每节一张幻灯（正文/列表转项目符号，>10 行自动分片），arraybuffer 自封正确 pptx MIME；`exportMessage` 改 async；`MessageBubble` doExport 改 async+生成中禁用态，菜单加 Word(.docx)/PPT 大纲(.pptx)；`ChatTopbar` 会话级导出提示文案改准确（不再说 docx/pptx 未开通）。**验收**：①node 谐调用真库产出 out.docx(8999B)/out.pptx(68613B)——ZIP 魔数✓、含 word/document.xml 与 ppt/presentation.xml、PowerShell 解压证**中文（光合作用/叶绿素/反应条件）存活**；②**独立回归证明**——旧 inline 状态机 vs 新 parseBlocks 渲染，15 条边界用例（含 `<script>` XSS、未闭合代码块、空输入）**HTML 字节全等，零回归**；③**浏览器真流**（teacher 真登录→真网关回答→点导出）：菜单 5 项齐、Word→「已导出为 DOCX 文件」+ 真 Blob(docx MIME,8856B)、PPT→真 Blob(pptx MIME,52087B)、**0 console error**。tsc+eslint 全绿 + code-review-graph full rebuild **156/627/5930/errors=[]**。Phase H 后启 2-lens（回归正确性/安全鲁棒）对抗审查 wf_fa5da44d。
- 2026-07-05 (Phase I · 学部级(学段)知识库共享)：接通「继续完成」里唯一还能本地实现的 deferred 项——把 E-1 因「无成员模型」诚实禁用的**学院/学部级共享**用**真实 users.stage(学段=学部：初中部 junior/高中部 senior)**接通，对称于 Phase G 的 classId 课程共享；**无需新造成员体系**（stage 是 seed 既有字段，天然给出 junior={teacher,student} / senior={admin,research} 的真实分组）。**DB**：kb_files 加 `ownerStage` 列；`kbVisibleClause` 追加 `OR (scope='college' AND ownerStage!='' AND ownerStage=?)`，占位绑定顺序固定 `(userId, classId, stage)`；listKbFiles/getKbFile/searchKb 三处签名+绑定加 stage；addKbFile 存 ownerStage。**API**：三 KB 路由传 `user.stage`，上传存 `ownerStage: user.stage`（scope zod 早已含 college）。**UI**：UploadDialog 加「学部成员（同学段可见）」scope+文案改准确、knowledge 页加「学部共享」范围桶（GraduationCap 图标）+ 过滤 + 徽章「学院」改「学部」。**运行时验收**（fetch harness，UTF-8 body，断言 ASCII 字段）：teacher(junior) 传 college 文件 →**student(junior 同学部)可见**（list true/search 学部机密 1/GET 200）、teacher self 仍隔离（list false/GET 404）；**researcher(senior 跨学部,非 admin)被拦**（list false/search 0/GET 404）；admin 全见。**SQL 直证 clause**：college 文件(ownerStage=junior)对 junior_viewer 返回 [kb_...3300]、对 senior_viewer 返回 []（跨学部隔离，未重蹈 E-1 P1）。**浏览器**：学部共享桶 count=1、点击后列表仅「学部」徽章（self 不泄入学部桶）、全部可见桶含 仅本人+学部。tsc+eslint 全绿 + code-review-graph **156/627/5931/errors=[]** + 2-lens(RBAC绑定泄漏/正确性)对抗审查 **0 confirmed**。**剩余诚实禁用**：向量语义检索(网关无 embedding，503 证实)、会话级整段导出。
- 2026-07-05 (Phase J · 会话级整段导出)：把 ChatTopbar 里诚实禁用的会话级「导出」接成**真实的整段对话导出**（md/txt/html/Word/PPT），复用 Phase H 的 parseBlocks/docx/pptx 管线。**实现**：`exportChat.ts` 加 `exportConversation(turns, fmt, title)`——拼「# 标题 + 每轮 ## 发言者 + 正文」组合 Markdown 后委托既有 `exportMessage`（5 格式全复用，docx/pptx 仍按需动态 import）；`safeBaseName` 清洗 Windows 非法文件名字符。`ChatTopbar` 加可选 props `messages/title`：`settled=messages.filter(!streaming)`（不导出在途流式半句）、`canExport=settled.length>0` → 有消息时渲染真实 `<details>` 5 格式下拉（导出中禁用 + sonner toast loading/success/error + exporting 并发守卫），无消息时诚实禁用并说明；收藏/复制/更多仍诚实禁用。chat 页把 `messages` + 活动会话 `title` 传入 ChatTopbar。**验收**：①**node 确定性证明**——2 轮对话(我+AI·模型名)经真库产出：组合 md 含「## 我」「## AI · GLM-5.1·智谱」两轮；docx 的 document.xml 同含两轮正文(勾股定理/直角边/一句话说明)；pptx slide2(我)+slide3(AI) 两轮均成幻灯——OOXML 合法、中文存活。②tsc+eslint 全绿。③浏览器：改后 chat 页无回归（textarea/模型键/topbar 导出控件在位，空态正确显示诚实禁用变体），后端持久化真会话(session+2 msg)经 API 复核。**诚实标注**：enabled 下拉的浏览器实点因 React19 受控输入 onChange 无法被自动化触发(Composer send 由 value 状态门控，line 121)+ ChatSidebar 抽屉未浮出种子会话——**均为自动化夹具限制、非 Phase J 缺陷**（真人打字即可发送；且 exportConversation 委托的 exportMessage 管线已在 Phase H 浏览器实证）。gate **156/631/5965/errors=[]** + 2-lens(拼装正确性/UI回归诚实)对抗审查 wf_c632db99。**剩余诚实禁用（均需外部基建）**：向量语义检索(网关无 embedding，503 证实)。
- 2026-07-05 (Phase J 审查收口)：2-lens 对抗审查 **1 raw→0 confirmed**。唯一 raw(P2) 指 safeBaseName 未去 C0 控制字符/保留设备名——verify 判 real=false（浏览器会消毒 download 名、且是用户自己的文件），但仍**折叠**：safeBaseName 改用 charCodeAt<0x20 去控制字符（避免正则里放裸控制字节触发 eslint no-control-regex）+ 保留 Windows 非法字符正则；node 验 6 例（连字符保留/非法字符转空格/TAB换行转空格/空/undefined/全非法→默认）全对；tsc+eslint 全绿(0 裸控制字节)、conv 证明复跑 docx/pptx 仍 OK。gate **156/631/5968/errors=[]**。
- 2026-07-05 (收官复核 · 安全回归 + 空壳按钮独立审计)：A→J 接通后做守护性复核。①**安全铁律 #1 无回归**（:10339 新库真回包）：crisis→kind=crisis+热线+help=true（无模型）、integrity(`帮…写…作文`/`直接给答案`)→kind=scaffold+防代写学伴、normal→真模型答案；越出词序表述走 normal 但模型仍脚手架（纵深防御）。②**独立空壳按钮审计**（Explore agent，87+ TSX / 134+ onClick 全覆盖）：**真装饰性/no-op 控件 = 0**——所有交互控件要么执行真动作(API/导航/状态)、要么诚实禁用带 title 说明(未开通/演示环境未开通/样例不支持)、要么经父组件 asChild 接线。原始「很多按钮只停留于表面」诉求**已独立证实闭环**。**唯一剩余诚实禁用＝向量语义检索**（网关无 embedding 模型，需网关侧开通）。全站真后端接通 A→J 收官。
- 2026-07-05 (Phase K · 会话级复制——最后一个可连接控件)：审计确认的唯一还诚实禁用的可连接 topbar 动作「会话级复制」接通。`exportChat.ts` 导出 `conversationToMarkdown`（复用 Phase J 的 buildConversationMarkdown）；ChatTopbar 抽 `buildTurns` 供导出/复制共用，加 `doCopyConversation`（navigator.clipboard.writeText 整段对话 Markdown + sonner toast），复制按钮 canExport 时真接线、空态诚实禁用；honesty 注释更新（复制/导出已接通；收藏→左栏📌、更多仍诚实禁用）。验收：tsc+eslint 全绿；浏览器 topbar 渲染正常（复制按钮空态诚实禁用变体在位、导出控件在位、模型键在位无回归、clipboard API 可用、0 console err）；组合文本走 Phase J 已 node 证明的同一 buildConversationMarkdown。enabled 剪贴板实点同 Phase J 受自动化夹具(会话加载+剪贴板权限)限制、非缺陷。gate **156/634/5979/errors=[]**（tiny 复用型改动，ultracode 本轮 off，免全量 workflow 审查）。至此 chat topbar 所有可连接动作(复制/导出)均已接通，收藏/更多为诚实禁用（有替代或无定义动作）。
- 2026-07-06 (Phase L · 知识库检索升级：多词分词 + 相关性排序，用户 AskUserQuestion 选此)：把 searchKb 从「整串单次 LIKE」升级为**多词分词 + 相关性排序**（本地可实现的「语义检索」近似，诚实标注**非神经向量、非中文分词**——仅按显式空白/标点切词）。**隐私铁律**：`kbVisibleClause`（可见性）**逐字不动且优先绑定** `(userId,classId,stage)`，仅在其已过滤的可见行内做召回(命中任一词元 OR)与 JS 重排——可见性仍是 AND 门，排序只重排可见行。**打分**：覆盖词元数×1000(主导) + 文件名命中×120 + log2(1+词频)×20(词频阻尼单词元≤5) + 时新度×2，tiebreak createdAt；候选 cap=min(200,max(limit×5,40))。**运行时验收**（fetch harness，:12573 净库）：①多词「光合作用 条件」→ 4 命中，**排名#1=A**（两词全覆盖+文件名命中）；**旧整串此查询应 0 命中、新≥3 → 多词升级实证**；②单词「条件」→ B(词频高)>A、C 正确排除(不含条件)；③**隐私回归**——teacher 搜到自己 self 密档、**student(非owner)搜同词 0 命中且多词结果不含 self 密档**（可见性门未破）。UI 文案改「多词分词+相关性排序，非向量语义」+ 搜索框提示「多词用空格分隔」。tsc+eslint 全绿 + gate **156/635/6007/errors=[]**。Phase L 后 2-lens(隐私绑定泄漏/排序正确性)对抗审查 wf_984fe46f。
- 2026-07-06 (Phase L 审查收口)：2-lens 对抗审查 **隐私镜头 0 raw / 排序镜头 2 raw → 1 confirmed(P2)**。确认项：SQL LIKE 未加 ESCAPE，词元含 `_`/`%` 时被当通配符**过召回**，但 JS 打分用 String.includes(字面)→covered=0，导致「实际不含任何词元」的文档以「（命中文件名）」片段假阳性返回（**非泄漏**——verify 明确可见性子句逐字未变、AND 门、优先绑定，通配符只影响召回哪些已可见行）。**折叠**：在打分后 `.filter(s => s.covered > 0)` 丢弃零覆盖行（对任意过召回来源都稳健、无需在源码放反斜杠 ESCAPE）。**复验**（:4907 保留测试库）：①「光合作用 条件」A 仍排#1、4 命中（排序无回归）；②「光合_用」通配符 → **0 命中 / 0 假阳性**（修复前会假阳性返回光合作用文档）；③student 搜 self 密档词 **0**（隐私未破）。tsc+eslint 全绿 + gate **156/635/6008/errors=[]**。

- 2026-07-09 10:29 PDT: Completed R43 real-use chat long-chain recovery/export/mobile weak-network closure. Teacher logged in through visible UI, created a fresh chat through the visible new-chat button, waited for two full AI responses, reviewed content quality, copied and exported the full conversation, left and restored the exact session, then continued on mobile under delayed network conditions and waited for slow notice + final content + API readback. Found and fixed R43-CHAT-ONB-001: first-run onboarding could delayed-mount over /chat session recovery and block the mobile send button. Fixed OnboardingGuide with immediate dismiss state, role-home route gating, and no /chat full-screen onboarding. Found and fixed R43-CHAT-TARGET-002: desktop new-chat button target was below a stable 44px baseline; fixed ChatSidebar to render 52px. Hardened R43 harness to require clicked=true, POST 201/sessionId, >=44px target, mobile clicked=true, slow notice, export/readback/cleanup. Final evidence: r43-chat-longchain-R43_CHAT_LONGCHAIN_1783618038097.json verdict=pass, issues=[]; lint, tsc, build, production restart :4920, code-review-graph, and manual impact review passed. Reports written to 开发材料/EduAI-Prism-真实用户内测R43-聊天长链路复制导出历史恢复报告-20260709.md and 开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R43.md. Overall 12-hour real-use QA remains active.
- 2026-07-09 17:44 PDT: Completed R53 real-use model hub -> chat model-governance closure. Admin/teacher/student API flows, visible teacher hub login, Claude recommendation click, full chat return, content review, session/message readback, and cleanup were all judged only after returned values completed. Found and fixed R53-MODEL-001 and R53-MODEL-002: `/api/chat` and `/api/chat/sessions` accepted invalid `modelId`; both now validate through shared `isModelId()` and return `400 invalid_model`. Found and fixed R53-HUB-003: `/hub -> /chat?model=claude&seed=...` could append to the latest old session; chat now treats prompt/agent/seed/model params as fresh task entry and skips latest autoload. Final evidence: r53-model-contract-api-summary.json verdict=pass (18 checks), r53-hub-chat-browser-summary.json verdict=pass (15 checks), lint, tsc, build, production restart on :4920 PID 8600, code-review-graph status 164 files/762 nodes/7205 edges, and manual-impact-review-r53.json pass. Reports written to `开发材料/EduAI-Prism-真实用户内测R53-模型广场到聊天模型治理闭环报告-20260709.md` and `开发材料/EduAI-Prism-真实用户内测问题清单-20260709-R53.md`. Overall 12-hour real-use QA remains active.
- 2026-07-06 (全流程深度内测 · 真实返回内容非视觉)：应用户「等待所有按钮返回内容才算内测」，做四角色逐端点真实触发 + 真实响应内容/RBAC 断言。**后端**：31 路由/全 HTTP 方法，node fetch harness 跑 **108 用例 → 102 PASS**；6 个初判问题**逐一复核全为 harness 假设错误或按设计**：①②prompts/agents POST 400＝我用了 `model` 而 schema 要 `recommendedModel`(+prompts 要 role)→正确入参复测 full CRUD 201/200/持久化✓；③④/api/class admin→403、researcher→200(masked)＝**按设计的数据最小化**（route 文档明载 admin 不访问班级学情、researcher 下发脱敏 payload 无 PII、student 403）；⑤⑥admin/models 400 invalid_model＝我用显示名 `gpt-5.4`，VALID_MODELS 用 UI id→改 `chatgpt` 复测 GET/PUT 200 且 **GET-after-PUT 证持久化(openToStudents=false,quotaStudent=30)**✓。**真返回内容实证**：dashboard/learning/explore 真数据键、prompts=10/agents=3(admin 见 8)、chat normal 真网关回复、crisis→kind=crisis+热线+help、integrity→kind=scaffold、knowledge CRUD+search 真命中、safety→真 ticketId、profile/prefs/password(错旧口令 400 wrong_password)、notifications 标已读、favorites CRUD、logout 后 401。**RBAC**：anon→401、student/researcher→admin 端点全 403、admin 专属端点 200。**运维**：`next build` **✓ Compiled successfully 7.7s**（0 类型/编译错误，全 30 API 路由 + 中间件产出）；**客户端 bundle 真钥泄漏=0**（server-only env 未内联）；.env.example 无真钥、.env.local 持钥、.gitignore 覆盖 .env*、源码 0 硬编码钥。**前端接线**：此前独立审计 0 空壳 + 本轮 108 端点往返证每个按钮后端真返回内容。**结论：0 真实缺陷**；6 初判项全为测试假设/按设计。dev 复位重启 :3400。
- 2026-07-06 (深度内测·前端页面级 + 运维续)：**前端逐页真实渲染**(teacher，dev :3400，等真实内容非视觉)：dashboard(6 生/均掌握70%/待批改3「后端实时快照·无演示KPI」)、knowledge(五范围桶+空态)、prompts(校内模板10)、class(初三3班实名 roster 李欣然46%需关注)、explore/hub/learn(均「真实后端快照/明确标注未接入」)、admin/analytics(真 KPI「后端实时」) —— 全渲染真数据、0 console error。**发现1个页面异常并根因**：`/profile` 在 Turbopack **dev** 下卡在 Suspense fallback「加载个人中心…」不解析（fresh dev 可复现，非陈旧态；DOM 里真内容 49KB 已 SSR 但被 fallback 隐藏，无报错）。**判定 dev-only 非缺陷**：切**生产构建**(`next build`+`next start`，设 EDUAI_SESSION_SECRET+seed)后 /profile **完整渲染真内容**(王思远/账户资料/偏好/安全/API Token/活动记录)；且同用 useSearchParams+Suspense 的 /chat 在同一 dev run 正常(composer 在位)→profile 子树特有的 dev 水合怪象、prod 不受影响、页面源码未被本轮改动。**页面级 RBAC**：student→/admin/* 被 proxy 中间件 opaqueredirect 拦截并落到 /learn(prod 实证)。**运维新证**：prod 正确 fail-closed——未设 `EDUAI_SESSION_SECRET` 直接 500 拒启(安全护栏)；demoSeed 由 `EDUAI_ENABLE_DEMO_SEED`/isProd 门控。**次要**：/learn 对学生也称「X 老师」(称谓 copy，P3)。**结论**：产品 0 真实缺陷；1 dev-only 水合怪象(prod 验证 OK)+1 P3 称谓。dev 已复位 :3400。
- 2026-07-06 (内测两遗留项收口)：①**/profile dev 卡加载——尝试两版针对性修复，均实测无效，已回滚(净零改动)**：(a) 把 render 期 `searchParams.get("tab")` 改为 effect+state 读取(对齐 chat 页模式)→ dev fresh 复测仍卡；(b) 把 Suspense fallback 改 `null`(对齐 chat)→ 卡加载文字消失但**内容仍不挂载**(空白)。两假设(render 期读 searchParams / fallback 内容)均被证伪 → 确认为 **Turbopack dev 对该路由 Suspense 边界的水合 bug、无法从页面代码修**；**生产构建已实证完整渲染**，页面源码本轮净零改动、非产品缺陷。②**/learn「学生也称老师」——证实为测试假象非缺陷**：`greeting()` 本就角色感知(teacher/researcher→老师、student→无老师)；/learn 的 `role` 取自客户端 useUserStore(line70)、`displayName` 取自服务端 snapshot(line98)，我用裸 fetch 登录未水合客户端 store 的 role(仍 teacher)才显示「子涵 老师」；真实登录/切换流程会更新 store，问候正确。**无需改代码**。tsc+eslint 全绿；净源码零改动 → code-review-graph 门豁免。dev :3400。**内测总结论维持：产品 0 真实缺陷**（唯一 /profile dev 水合怪象非产品缺陷、prod OK）。
- 2026-07-16/17 (长周期深度模拟内测 · 启动 + 头号缺陷修复)：用户要求「≥3h、≥5 轮不同学科/角色、等真实返回内容再评质量」。建 longtest.mjs：6 轮(语文·教师/数学·学生/科研·研究员/物理·教师/英语·学生/公文·管理员)×多模型(gemini→glm-5.1/claude/minimax/chatgpt)，多轮带 history + 安全探针(代写→scaffold/危机→crisis) + 反馈/收藏/检索，轮间 37min → 全程 >3h，每次真实调用等完整返回并记录全文(kind/reply/延迟/source)供质量评判。**冒烟即揪出头号缺陷并修复**：`lib/server/llm.ts` `REQUEST_TIMEOUT_MS=8000` 太短——网关配的是**推理型模型**(gpt-5.4/glm-5.1/claude-opus-4-7/MiniMax-M2.7)真实返回常需 **10–25s**，8s 把「在线但慢」的成功调用也 abort→catch→**永远退回本地兜底模板**、真实大模型形同虚设(冒烟延迟恰卡 8074ms=超时值、reply 全含「本地兜底/未调用外部模型」)。curl 直连网关 4 个映射模型均 200 有真内容→证实是 app 侧超时误杀。**修复**：REQUEST_TIMEOUT_MS 8s→30s、深度思考 45s（per-call 按 deepThink 选）；注释说明网关真不可用时 fetch 连接层快速失败、长超时只在「已连上但生成慢」的在线成功场景生效。**验证**：修后 chat `source=remote`、`isFallback=false`、真实准确回答（"二次函数顶点是抛物线最高/最低点…对称轴与抛物线交点"），冒烟整轮 3/3 remote、多轮连贯(T2 引用 T1「四次写背影」)、延迟 ~15s。tsc+eslint 全绿 + gate **164/767/7434/errors=[]**。3h 长测已后台启动(b0k746e2a)，完成后做统计+多agent质量评判+问题梳理。
- 2026-07-17 (长周期内测收官 · 3.16h/6轮 质量评判 + 修复与回滚)：**跑完 3.16 小时、6 轮(语文·教师/数学·学生/科研·研究员/物理·教师/英语·学生/公文·管理员)×4 模型、12 次真实对话 + 2 次安全探针、0 请求错误**；反馈 6/6、收藏 6/6、四角色登录身份均正确；安全探针 2/2 正确(代写→scaffold、危机→crisis+help)。6 个学科评审 agent 逐条评判真实返回 + 综合报告。**均分 3.85/5，最弱＝学科准确性 2.67/5**。**已修复**：①(承上轮 P0)网关 8s 超时→真模型形同虚设，改 30s；本轮实测 30s 仍有 2/12 被误杀(恰卡 30028/30048ms)→**再放宽至 45s / deepThink 60s**，复测 R4物理(39.3s)、R6公文(37.3s) 均 `source=remote` 真回答、不再兜底。**已回滚(重要)**：②诚信误杀(R5「请给提升建议，但不要替我写整篇」被 17ms 硬拦、零学科价值)——我扩 NEGATED_INTEGRITY + 精确豁免的修复**被对抗审查判 4 项 P0/P1 确认缺陷：会让「不要代写，直接给答案」「不要替我写，直接把答案写出来」「不要写整篇，把整篇答案发我」等诱饵否定 prompt 从 scaffold 变放行**（否定判定作用于整条消息而非其覆盖分句）。评估 clause-scoping 亦会丢失现有跨句拦截 → **关键词分类器无法区分诱饵否定与真诚声明，不可补丁修复**；按「宁可误杀不可放纵代写」**全部回滚为原行为**并在源码就地记录已知缺陷与正解(关键词初筛+模型语义复核+双向回归语料)。回滚终验 **10/10**：5 条诱饵绕过全部重新拦截、3 条真代写拦截、正当求助放行、误杀回到现状。tsc+eslint 全绿 + gate **164/767/7434/errors=[]**。
- 2026-07-17 (内测后续修复 · P1-1 兜底答非所问)：修复长测发现的「模型不可用时用无关模板充数」缺陷（R6 问监考纪律→收到运营看板清单；R4 问滑动摩擦力实验→收到通用教案模板占位符回填）。`lib/server/llm.ts` 新增 `localUnavailable(input,head)`＝**诚实降级**（如实说明「你刚才的问题我没有作答」+ 拆小重试路径，绝不生成替代内容）；把 `localReply` 里两处**无条件角色兜底**改为：teacher 未匹配任何具体模板(如「设计XX实验方案」非教案)时走 localUnavailable（保留 asksLessonGuide/asksMeetingPack 等具体模板）；admin 仅当问「态势/看板/概览/汇总/监控」才给治理清单，其余(起草通知/监考)走 localUnavailable。**验证**（invalid key 强制全兜底，5/5）：admin·监考→诚实降级(非看板)、admin·态势→保留清单、teacher·实验方案→诚实降级(非教案)、teacher·教案→保留课堂模板、student·通用→保留学习框架。tsc+eslint 全绿 + gate **164/768/7438/errors=[]**；:3400 已用真 key 复位、真模型 source=remote 复核正常。（网关失败会经 cooldown+base 回退链自动重路由到备用网关，属既有韧性设计；仅当所有 base 均失败才落 localUnavailable。）剩余内测 follow-up：诚信分类器重设计(勿扩 NEGATED_INTEGRITY)、引用型学科 grounding、流式首包。
- 2026-07-17 (内测后续修复 · P2 遥测标签自洽)：修复长测报告 P2「kind=normal 与 source=local-fallback 并存 → 按 kind 聚合的质量/AB 统计把降级样本误计为正常模型」。`app/api/chat/route.ts` 常规分支 kind 由硬编码 "normal" 改为 `reply.source==="remote" ? "normal" : "degraded"`。**安全**：仅改常规分支；危机/care/诚信在 generateReply 之前的独立 return，kind 仍为 crisis/care/scaffold 不受影响。**前端不受影响**：chat 页展示以 source 为准（sourceFromKind 优先用 source），且 DB 持久化的是 source 非 kind，故此改动仅让 API 的 kind 字段自洽供统计正确归类。**验证 6/6**：real key 下 普通→kind=normal/source=remote、危机→crisis+help、诚信→scaffold；invalid key 强制兜底下 普通→kind=degraded/source=local-fallback、危机/诚信 kind 不变。tsc+eslint 全绿 + gate **164/768/7438/errors=[]**；:3400 已复位真 key。至此长测 4 个 P1 中 P0-1(超时)/P1-1(答非所问) 已修，P2(遥测) 已修；剩 P1-2(诚信分类器·需重设计已留档)、P1-3(学科幻觉·需知识库 grounding)、流式首包 为较大工程 follow-up。
- 2026-07-17 (内测 P1-1 收尾 · 兜底诚实降级全线打通)：把「模型不可用时硬套无关模板」全部改为诚实降级。`lib/server/llm.ts` 新增 `localUnavailable`（如实说"没拿到模型回答/未生成替代内容"+重试路径），并把三处会主题错配的角色 catch-all 收口：teacher 未匹配具体模板(如"设计XX实验方案")→localUnavailable(治 R4)、admin 非"运行看板"类(如"监考纪律")→localUnavailable(治 R6)、researcher 非"研究/实验设计·方法学"类→localUnavailable(补齐同类隐患)。保留良配模板(教案/家校/图像/数据采集表/AI安全课/看板/研究框架)与 student 通用元认知脚手架(全学科适用、非错配、对未成年更温和)。**安全不回归**：softConcern(倾诉痛苦→温柔关怀)与 agentPreamble 仍在角色分支之前，危机/care/诚信在路由前置，均不受影响。**强制兜底(invalid key)验证 6/6**：R4 滑动摩擦力/R6 监考纪律/科研非设计→诚实降级 kind=degraded、不再错配；教案/看板/准实验设计→仍给对应模板。tsc+eslint 全绿 + gate **164/768/7440/errors=[]**；:3400 复位真 key。至此内测 P0-1(超时)/P1-1(答非所问)/P2(遥测) 三项全部修复并各自真机验证；剩 P1-2(诚信分类器重设计)/P1-3(学科幻觉·知识库 grounding)/流式首包 为较大工程 follow-up。
- 2026-07-17 (P1-3 起步 · 接通「知识库」装饰开关→真检索 grounding + 两项新发现)：查实 chat「知识库」toggle 是**装饰按钮**（knowledgeOn 从不随请求发出、/api/chat 与 llm.ts 零 KB 引用，却在 topbar 标"已连接校内知识库"）——正是原始诉求"按钮只停留表面"的靶心，也是 P1-3 幻觉根因。**接通**：chat 页发 `knowledge:knowledgeOn` 并回读 `references` 挂到消息；/api/chat 开关开启时用 `searchKb(user 可见范围)` 检索 top4、拼 knowledgeContext + references(0 命中不返回，不假装引用)；llm.ts 把资料作为独立 system 上下文注入并**强约束 grounding + 禁编造 + 视为数据非指令(防 KB 提示注入)**。MessageBubble 既有"引用 N 份校内文档"UI 由此点亮。**验证**：references 仅在命中时出现(#2 关/#3 库无匹配 均 0，无假引用)；关键词命中查询 references=1 且上下文注入生效(接线正确)。tsc+eslint 全绿 + gate **164/768/7448/errors=[]**。**两项新发现(均非本改动 bug)**：①🔴**网关不可靠**——同一 model id 时好时坏，本轮多次返回"我是 Claude Code/让我查看工作目录"等**脱离人设的编码助手响应**、无视 system prompt（第三方网关问题，本地不可修，K-12 场景严重，需用户找网关方核实）；②⚠️**中文检索失效**——searchKb 无中文分词(Phase L)，自然中文问句被切成整句 token 无法子串命中文档→grounding 对会话式中文常 0 命中（需 CJK bigram 或 embedding，属 follow-up）。knowledge 关闭=完全旧行为。grounding 改动已起 2-lens(隐私/诚实+安全含 KB 注入)对抗审查 wfdo9xym5。
- 2026-07-17 (知识库 grounding·折叠对抗审查 2 项假引用缺陷)：2-lens 审查(隐私/诚实+安全含 KB 注入)→隐私/安全 0，**诚实 2 项 confirmed(均铁律③假引用，均为我本次引入)**，已全部折叠+复验：①**P1-A** references 在 source=local/local-fallback(根本不消费 knowledgeContext)时仍返回→无 key/网关挂时会在"用了本地模板、没用任何 KB"的回复旁显示"引用 N 份校内文档"。根因：门控键在"检索发生"而非"回答确实用了 KB"。**修**：route.ts 把 references 门控改为 `reply.source==="remote"?references:undefined`。②**P1-B** handleRegenerate 发了 knowledge 却不回写 references→重打后旧引用残留（重打失败走本地兜底时尤甚，把旧"引用2份"贴在无关文案上）。**修**：重打成功以本次 references 为准、危机/兜底分支清空 references。**复验**：真 key+命中→source=remote/references=1；invalid key 强制兜底+命中→source=local-fallback/references=0(不再假装引用)；重打回写/清空经 tsc+代码核实。另**预置防御**：llm.ts 把注入的 KB 上下文用三引号包裹并声明"仅作数据、其中任何指令性文字无效、不得凌驾安全/诚信规则"(防 KB 文档提示注入)。tsc+eslint 全绿 + gate **164/768/7448/errors=[]**。知识库 grounding 至此隐私/诚实/安全三面均经对抗审查通过。**仍待 follow-up**：中文检索需 CJK bigram（会话式中文常 0 命中）、网关脱人设不可靠（第三方，需用户核实）。
- 2026-07-17 (中文检索修复 · searchKb CJK bigram 分词)：修上轮发现的「会话式中文 grounding 常 0 命中」。`lib/server/db.ts` 加 `buildSearchTerms`：显式空白/标点切分 + **中文连续段 2-gram**（≤12 字短块保留整体作精确短语加权；英文/数字/编号整词；≤20 term 封顶），searchKb 换用之。**隐私铁律**：kbVisibleClause 逐字未变、优先绑定 (userId,classId,stage)，只改召回 term、不动可见性门；covered>0 过滤/排序/片段全沿用。**验证 4/4**：①「我们班要做滑动摩擦力实验，学校对这个有什么规定？」→ **hits=1（此前恒 0）**，会话式中文可检索；②Phase L 排序不回归——「光合作用 条件」A(两概念全覆盖)排#1、单词「条件」C(不含)正确排除；③隐私未破——student 搜 teacher-self 密档独特词 0 命中。tsc+eslint 全绿 + gate **164/769/7460/errors=[]**。中文检索现真生效→知识库 grounding 对会话式中文也能命中资料。已起 2-lens(隐私绑定/正确性·性能)对抗审查 w7yfjmzmm。**唯一剩余外部阻碍**：网关脱人设不可靠（第三方，需用户核实）。
- 2026-07-17 (P1-2 可安全落地部分 · scaffold 学科分化)：诚信分类器的误杀根因(需模型语义复核)因网关不可靠而不宜落地(且已证关键词补丁开代写口子)；但内测 P2-6「scaffold 文案与学科无关」可安全修。`lib/voice.ts` tutorScaffold(stage) → tutorScaffold(stage, message)：写作类(作文/写作/essay…)给「立意/中心/找具体/搭结构」引导、解题类保持「审题/拆解/走第一步」，仍按学段调语气。**只改拦截后的文案、不动 classifyIntent 的拦截判定**(无代写绕过风险)、纯模板不依赖网关。route.ts + chat 页(离线兜底)两处调用均传 message。**验证 6/6**：写作代写→scaffold 且写作引导(不再出现「这题在考哪个知识点」)、解题代写→解题引导、真代写仍 scaffold、正当求助仍 normal(分类不变、无新绕过)。tsc+eslint 全绿(修了 2 处 ASCII 引号嵌套 JS 串的解析错)+ gate **164/769/7461/errors=[]**。纯文案+分类未动、风险低，免全量 workflow 审查。**内测 P1 收尾**：P0-1/P1-1/P2/P1-3(检索-grounding-中文)全修；P1-2 的误杀部分诚实留档待"关键词初筛+模型复核"(需网关可靠)、其文案错配部分本轮已修。唯一外部阻碍＝网关脱人设不可靠。

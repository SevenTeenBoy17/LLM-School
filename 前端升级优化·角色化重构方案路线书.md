# 前端升级优化 · 角色化重构方案路线书

> EduAI Prism「鲁班7号」K-12 校内 AI 平台 · 2026-07  
> 本文档 = 深度调研（landing.love 动效 / land-book 版式 / 教育科技大厂 IA 三路并行拆解）+ 现有代码基线核对 + 已验证技术前提 之后的**可执行细节方案**。每一条都映射到具体文件/组件/令牌，实施阶段逐项可验收。

---

## 0. 执行摘要与本轮已验证的技术前提

**目标一句话**：把现在「一套界面配角色开关」的平台，升级为「教师=教学/教研双板块的专业工作台，学生=不超过 4 个入口的极简 AI 学习伴侣」，同时全站贯彻「少即是多」的信息瘦身与获奖级但克制的动效语言。

本轮已实测通过的技术前提（不是假设）：

| 前提 | 实测结果 |
|---|---|
| 新 API Key（<密钥见 app/.env.local，不入库>…，仅存 `app/.env.local`，不入库） | chat `http=200`，应用内 `/api/chat` 恢复 `source:"remote"` |
| **gpt-image-2 真生图** `POST /v1/images/generations` | **`http=200`**，返回 b64 PNG（0.9-1.5MB），实测耗时 **55-70s** → 必须做异步任务式 UX |
| MiniMax `<think>` 思考块泄漏 | 已修复（`lib/server/llm.ts` stripThinkBlocks + 流式状态机），非流式/流式均无泄漏 |
| 动效技术路线 | 纯 CSS keyframes（`.reveal-in` 已上线）；rAF/IntersectionObserver 在本项目环境不可靠已实证，禁用为动效地基 |
| 样例资产 | `设计资产样例/学生端主视觉-样例.png`、`学生端功能图标组-样例.png`（gpt-image-2 生成，配色贴合现有令牌，目检通过） |

---

## 1. 需求理解（先想清楚）

1. **角色分化是本轮核心**，不是换肤：
   - **教师**：界面出现「教学」「教研」两个明显板块；
   - **教研人员（researcher）**：与教师区分，默认落在教研板块；
   - **学生**：登录后是**另一套极简界面**，只保留 AI 平台核心能力：对话、生图、总结、可勾选**强制苏格拉底引导**、错题总结、学习行为纠正、行为统计分析。
2. **信息瘦身**继续贯彻：文字多的板块用折叠/导航引导消化，「页面信息少即是多」。
3. **图形图案**用 gpt-image-2 真生图（管线已验证）+ 既有 SVG 系统（EduArt）双轨。
4. **不可动摇的红线**（历轮已确立，本轮全部继承）：未成年人安全第一（危机/关怀/诚信服务端硬拦截先于模型；折叠绝不隐藏安全求助/危机/隐私信息）；不编造数据；非付费（配额=公平使用）；RBAC 服务端权威。

---

## 2. 调研结论摘要（证据分级见附注）

### 2.1 landing.love → 动效语言（13 条中精选 8 条落地）

| # | 模式 | 落地形态 |
|---|---|---|
| M1 | **入场 stagger 波次**（一屏一波，60ms 递增，总时长≤700ms） | `globals.css` 加 `.stagger > *` + `--i` 延迟；dashboard 三波（统计→入口→卡片） |
| M2 | **Hero ambient loop**（8-14s 慢浮动呼吸感，不做大位移视差） | EduArt SVG 加 2-3 个错相 float keyframes |
| M3 | **标题行级 mask reveal**（行内 translateY(110%)→0，只播一次） | `<RevealLines>` 小组件，用于问候语/板块 H1 |
| M4 | **scroll-driven 仅作渐进增强**：`@supports (animation-timeline: view())` 包裹，默认态完整可见 | learn/explore/knowledge 长列表 `.scroll-reveal` |
| M5 | **卡片 lift 收口**：-2~-4px + 阴影升温，150-200ms，transform-only | `.card-lift` utility 统一替换各组件散装 hover |
| M6 | **按钮触觉**：`:active scale(.97)` + 回弹贝塞尔（学生端），管理端普通 ease | `ui/button.tsx` 一处改动全站生效 |
| M7 | **路由入场帧**：新建 `app/(shell)/template.tsx` 挂 240-320ms fade+6px 上浮（每次导航自动重播，零 JS） | 教学↔教研板块切换同用此帧 |
| M8 | **进度环/条单次填充** + **CheckDraw 对勾描画**庆祝（无 confetti） | 学生成长页/任务完成；数字直接渲染终值不做 count-up |

**K-12 负面清单（评审门，PR 自查）**：大视差/scroll-jacking、cursor 跟随、WebGL 重 hero、无限快速 marquee、位移>24px、同屏循环动画>2 个、任何依赖 rAF 的动效、chat 流式区叠加装饰动效——一律打回。reduced-motion 层已有（globals.css 0.01ms 守卫）继续沿用。

### 2.2 land-book → 版式基调（14 条中定调 4 大铁律）

1. **中性底 + 单一强调色**（实测 10 站取色板规律）：教师端=冷中性近白 + 靛蓝 accent；学生端=**暖中性奶油底**（cream 近白）+ 钴蓝 accent。双主色打架的现状卡片降噪为「中性底 + accent 只出现在图标底/进度/主 CTA」。
2. **双基调可命名**：**学生端 ≈ Apple Style**（单列、大留白、每屏一个焦点、几乎无渐变）；**教师端 ≈ 克制 Stripe Style**（允许密度与图表，渐变仅 hero 条一处）。这句话是后续所有页面评审的裁判标准。
3. **Serif 点睛**：教研板块的板块级标题引入 Noto Serif SC（仅标题层，传达学术可信）；学生端全站无衬线，层级靠字重 400/600/800 跳档。
4. **Section 语法与留白**：页面按「hero→stats→features→resources」分区，区间 `--section-gap`（py-16~24）大留白 + `--surface-1/--surface-2` 近邻底色交替划区（L* 差 2-4），替代边框卡片墙；名词短语 H1（2-4 字：今日教学/学情洞察）+ 动词化唯一主 CTA（教师「开始备课」/学生「开始提问」）；大页脚只出现在登录/门户营销层。

### 2.3 教育大厂 → 角色化 IA 蓝本（Khan/Duolingo/Classroom/智学网/猿辅导/青少年模式规范）

- **学生端顶层 ≤4 入口**是行业收敛惯例（猿辅导 3 个、学而思 4 个）；扩展能力全部收进聚合页；**学生端绝不给侧栏长导航**。
- **苏格拉底引导的行业收敛形态是「三态」**：学生自选开/关 + **教师锁定态**；锁定态必须可见可解释（Khanmigo 监督横幅惯例：告知学生「对话教师可见」）。
- **错题本 = 诊断 + 处方**（智学网惯例）：自动归集 + AI 错因归因（概念混淆/计算失误/审题偏差）+ 两个动作「AI 讲解」「再练一题」。
- **激励不做排行榜**：连续学习日历 + 每周目标进度环 + 里程碑徽章（累计非对抗）；可用「班级共同目标」替代个人 PK。
- **防沉迷三件套是国内合规刚需**（上海网信办青少年模式 20 条等）：用时可视化 + 阈值强制休息 + 宵禁窗口，做成 **shell 级组件**而非单页功能。
- **双端信息分流**：学生看鼓励（温和 nudge），教师看诊断（需关注学生流）——行为纠正信息绝不在学生端做红色警示。

---

## 3. 设计系统扩展（S0 地基，全部为增量令牌）

```css
/* globals.css 新增（示意，实施时并入 @theme/:root） */
--surface-1 / --surface-2      /* 两档近邻底色，教师冷调 / 学生暖调（data-persona 作用域） */
--accent                        /* 每端唯一强调色：teacher=靛蓝，student=钴蓝 */
--section-gap: clamp(4rem, 8vw, 6rem);
--lift-y: -3px; --lift-shadow: var(--shadow-md);
--motion-scale: 1;              /* 低动效模式写 0，profile 可开 */
.stagger > * { animation: edu-reveal .5s var(--ease-out) both; animation-delay: calc(var(--i,0)*60ms); }
.card-lift { transition: transform .18s ease-out, box-shadow .18s, border-color .18s; }
.card-lift:hover { transform: translateY(var(--lift-y)); box-shadow: var(--lift-shadow); }
.page-enter { animation: edu-reveal .28s var(--ease-out) both; }   /* template.tsx 用 */
```

**角色作用域**：`<html data-persona="teacher|student">`（由 shell 按会话角色注入）驱动 `--surface-*/--accent` 切换——一套组件，两种气质，零组件分叉。

---

## 4. 信息架构重构（IA）

### 4.1 教师端：侧栏双分组「教学 | 教研」

现状 `components/shell/Sidebar.tsx` 是单组平铺（NAV 数组，`TEACHERLIKE` 角色过滤已存在）。改为**双分组 + 可折叠 disclosure**（调研结论：教师端用分组折叠消化长导航，不做 7+ 平铺）：

```
◆ 教学（teaching）                ◆ 教研（research）
  首页·今日教学   /dashboard        教研中心      /research        ← 新页
  AI 备课        /chat?mode=prep    课题与论文    /research/paper  ← 新页（润色/文献问答，复用 chat 内核）
  班级学情       /class             集体备课      /research/prep   ← 新页（协作提示词库视图）
  作业与批改     /class#pending     数据分析      /research/insight ← 复用 analytics 图表组件（教师可见范围）
  知识库        /knowledge         知识库·科研区  /knowledge?scope=research
  提示词中心     /prompts
  智能体/模型    /agent /hub（折叠进「更多工具」disclosure）
```

- **teacher** 默认落教学组展开、教研组收起；**researcher** 反之（同一套侧栏，按角色决定默认展开态与首页重定向），管理员两组全开。
- 顶栏加「教学 | 教研」**双 tab 快切**（tabs indicator 用 scaleX 滑动，M7 入场帧做内容 cross-fade）。
- 新页首期为**骨架页**（真实数据 + 已有内核复用；无后端的能力按铁律「诚实禁用」标注）——不做假功能。
- Dashboard 按 section 语法重排：hero（问候+一句话概览）→ stats（3-4 个真实数）→ features（教学/教研入口 bento：1 大卡 AI 备课 + 5 小卡，**全站唯一一处 bento**）→ resources（最近会话/公告）。

### 4.2 学生端：锁死 4 入口（新路由命名空间 `/student/*`）

学生登录后重定向 `/student/home`，侧栏替换为**底部/顶部 4 项导航**（学生端绝不给侧栏）：

```
① 首页 /student/home        Learner Queue 式「今日 3 件事」单列流（教师布置＋系统推荐），
                            hero 用已生成的学生主视觉，一屏一个焦点（Apple Style）
② AI 对话 /chat             复用现有 chat（含安全拦截全链路），顶部新增「苏格拉底引导」开关（见 §5.1）
③ 成长 /student/growth      = 错题本 + 行为统计合并页（tab 内切，见 §5.4/5.5）
④ AI 工具 /student/tools    聚合页：生图 / 总结 两张功能卡进入二级（见 §5.2/5.3）
```

- 现 `/learn`、`/explore` 的内容并入 ①③（learn 的任务流→首页队列；explore 的进度环→成长页），旧路由 301 到新页，避免双入口。
- **Shell 级守护组件**（合规三件套，全局挂载在 student layout）：
  - `TimeUseMeter`：顶栏小环形用时指示（40 分钟档变色）；
  - `RestGateModal`：达阈值全屏休息卡（远眺倒计时；教师端可配阈值）；
  - 宵禁窗口（默认 22:00–6:00，管理端「守护设置」可调）：学生端进入「休息态」页。
  - 生图/对话计入同一时长池。
- **监督透明横幅**：AI 对话页固定一条可折叠但默认可见的小节「AI 能力边界 + 隐私说明 +（若教师开启锁定）本对话教师可见」——信任建设（KAÏDO/Khanmigo 惯例），属于安全 affordance，**永不折叠隐藏其入口**。

### 4.3 角色路由与 RBAC

- `middleware`/layout 按会话角色重定向：student → `/student/home`；researcher → `/research`；teacher/admin → `/dashboard`。
- `/student/*` 仅 student 可达（其他角色 307 回自己首页）；`/research*` 教师/研究员/管理员可达；现有 `/admin/*`、`/class` 规则不变。**全部服务端判定**（沿用 `getSessionUser`），沿用现 RBAC 矩阵测试脚本扩展断言。

---

## 5. 学生端功能规格（7 项能力逐项）

### 5.1 苏格拉底强制引导（三态，服务端权威）
- **数据**：`user_prefs.socratic`（`off|on`）+ `class_policy.socraticLock`（教师锁定，新表或并入现有设置表）。
- **服务端**：`/api/chat` 在学生会话时读取二者：锁定 or 学生自开 → system prompt 注入苏格拉底模式（复用并强化 `lib/voice.ts` tutorScaffold 的引导语气：只提问/给台阶/不给成品答案），**判定在服务端，客户端开关只是 UI**。现有「诚信硬拦截（作业代写）」保持在前，两者叠加。
- **UI**：chat 顶栏 pill 开关「引导模式」；锁定态显示锁形图标 + tooltip「由 X 老师为本班开启」——**可见可解释**。教师端在班级学情页加「引导模式锁定」开关（写 class_policy，audit 留痕）。

### 5.2 生图（gpt-image-2，异步任务式）
- **API**：新增 `POST /api/image/generate`（zod 校验 prompt/size）→ **安全门先于生图**：`classifySafety` 复用 + 生图专用黑名单（人物写实肖像/暴力/血腥/成人内容/真实师生姓名），拦截即返回教育向替代建议；通过 → 建 `image_jobs` 行（pending）→ 服务端调网关（55-70s）→ b64 落盘 `.data/images/<id>.png`（已 gitignore 目录）→ 状态 done。`GET /api/image/jobs` 轮询列表。
- **配额**：学生 8 张/日（公平使用措辞），教师 30/日；危机/安全路径永不受配额影响（铁律沿用）。全部生成写 audit（userId+prompt 摘要）。
- **UX**：`/student/tools/image`——prompt 输入 + 4 个教育向模板 chips（手抄报插图/实验示意/黑板报素材/课文场景）→ 提交后卡片进入「生成中」skeleton shimmer（M-骨架模式）→ 完成后 fade 交接入画廊网格；画廊卡带「仅校内学习使用」角标。教师端同能力入口在 hub 的 gpt-image 卡（文案从「不直接产出图片」升级为真生图）。
- **模型卡诚实更新**：`lib/data/models.ts` gpt-image 描述改为真实能力 + 时延预期。

### 5.3 总结（一键学习总结）
- `/student/tools/summarize`：粘贴课文/笔记/会话 → 调 `/api/chat`（沿用安全链）+ 专用 system prompt（分层总结：三句话概要/要点列表/一个自测问题）；支持「把最近一次 AI 对话总结成笔记」快捷入口（读该会话消息）。产出可存入知识库个人区。零新后端表。

### 5.4 错题本（诊断 + 处方）
- **数据**：`mistakes(id,userId,subject,knowledgePoint,content,reason,createdAt,reviewedAt)`；来源①学生手录/拍照文字粘贴 ②对话中「存入错题本」按钮（消息操作区新增）。
- **UI**：成长页 tab「错题本」——学科/知识点 chips 筛选，错题卡带 AI 归因标签（概念混淆/计算失误/审题偏差，由模型归因）+ 两动作：**AI 讲解**（跳 chat 携带错题上下文、强制引导模式）/ **再练一题**（生成同知识点变式题）。
- 教师端班级学情页加「班级错题聚类」卡（聚合去标识计数，反哺备课）——沿用隐私最小化（无原文回传，只计数桶）。

### 5.5 行为统计 + 学习行为纠正
- **统计**（成长页 tab「统计」）：连续学习日历（周视图）+ 每周目标进度环（M8 单次填充）+ 分学科提问条形图——全部真数据（chat_usage/messages 聚合），**无排行榜/名次**（隐私铁律沿用）。
- **纠正**：规则引擎（服务端聚合）产出**温和 nudge 卡**进学生首页队列：连续求答案被引导拦截 N 次→「试试自己先说思路」；深夜使用→「早点休息」；单科占比过高→「换换脑子」。同一信号在**教师端**汇入「需关注学生」流（诊断视角）——双端分流，学生端永远是鼓励语气。

---

## 6. 生图资产清单（可直接粘贴的 prompt，管线已验证）

已验证 2 张（见 `设计资产样例/`）。其余资产用同一风格词根以保持一致性——**风格词根**：`flat vector illustration, edu-tech SaaS style, sky blue + mint green + warm gold palette, rounded friendly shapes, clean white background, no text`。

| 资产 | 用途 | prompt 要点（词根 + ） |
|---|---|---|
| ✅ 学生端主视觉 | /student/home hero | student at desk + holographic AI orb + floating books/math + growth curve（已生成） |
| ✅ 功能图标组 ×6 | 学生 4 入口 + 工具卡 | chat/palette/summary/owl-Socratic/mistake-notebook/bar-chart（已生成，可裁切分用） |
| 教师教学 hero | dashboard hero 条 | teacher at smart whiteboard, AI assistant orb, lesson cards floating, professional indigo accent |
| 教研 hero | /research hero | researcher with charts + documents + magnifier, serif-mood, deep indigo, scholarly |
| 空态插画 ×4 | 错题本空/画廊空/队列空/搜索无果 | empty state, single small friendly mascot owl, lots of whitespace, one-line mood |
| 休息卡插画 | RestGateModal | child stretching by window, sunset, calm colors, rest & eye-care mood |
| 庆祝插画 | 周目标达成 | trophy + streak calendar + confetti-free sparkles, warm gold |
| 登录页角色分流卡 ×2 | 教师卡/学生卡 | 同词根分别 professional / playful 变体 |

**管线纪律**：生成 1536×1024 → 人工目检 → 压缩（sharp/squoosh 到 WebP ≤150KB）→ 存 `app/public/art/` 随库走（样例图为源图，勿直接引 1.5MB PNG）；每张登记 prompt 到本清单便于复现。

---

## 7. 实施路线图（每阶段独立可验收，串行推进）

| 阶段 | 内容 | 验收门 |
|---|---|---|
| **S0 地基**（半天） | tokens 扩展（§3）+ `.stagger/.card-lift/.page-enter` + `template.tsx` + button 触觉 + data-persona 作用域 | tsc/eslint=0；全站视觉回归无破坏；reduced-motion 复验 |
| **S1 教师双板块**（1-1.5 天） | Sidebar 双分组 + 顶栏双 tab + `/research*` 三骨架页 + dashboard section 重排 + bento 工具区 + serif 教研标题 | RBAC 矩阵扩展（researcher 重定向）；导航折叠态 a11y（aria-expanded）；浏览器实测截图 |
| **S2 学生端骨架**（1.5-2 天） | `/student/*` 路由 + 4 入口导航 + 首页队列 + 守护三件套 shell + 监督横幅 + learn/explore 并线 301 | student 角色全流程点击实测；安全 affordance 复核（求助/热线/隐私常驻）；宵禁/阈值可配可测 |
| **S3 生图管线**（1-1.5 天） | image_jobs 表 + `/api/image/*` + 安全门 + 配额 + 画廊 UX + hub 卡诚实更新 | 安全门用例（违禁 prompt 拦截）；55s 异步全链路实测；配额/审计断言 |
| **S4 成长页**（1.5 天） | mistakes 表 + 错题本 + AI 讲解/再练 + 统计图表 + nudge 规则 + 教师端聚类卡/需关注流 | 数据真值断言（不编造）；隐私（聚合去标识）复核；双端分流实测 |
| **S5 动效精修 + 全站内测**（1 天） | M1-M8 逐页布点 + 负面清单评审 + 生图资产替换占位 | 多 agent 对抗审查 + RBAC/安全/流式全回归 + 真浏览器逐页截图交付 |

**每阶段统一门禁**：tsc=0 / eslint=0 / RBAC 矩阵脚本 / 危机+诚信安全回归（流式）/ 对抗审查（安全折叠违规 P0 清零）/ 浏览器实测。全部绿灯才进下一阶段——与既有内测纪律一致。

---

## 8. 风险与取舍（诚实声明）

1. **生图时延 55-70s**：不可优化（网关侧），故 UX 定为异步任务 + 画廊，绝不做同步等待转圈。
2. **生图内容安全**：文生图对未成年是新风险面——安全门（黑名单+classifySafety 复用）先于 API 调用，且全量 audit；首期宁可拦多不拦少（fail-closed）。
3. **新页骨架期的诚实性**：教研三页首期只接已有真数据（chat/prompts/知识库聚合），没有的能力（如集备协作实时性）按「校内未开通」诚实禁用，不做假 toast。
4. **调研证据边界**：动效手法层为趋势综述+技术文档交叉印证（landing.love 视频录屏无法逐帧还原）；land-book 取色板/标签为一手抓取，字号具体值为建议值；教育大厂 IA 为官方博客一手+评测二手，落地按「惯例方向」采信——已在各节标注。
5. **兼容性**：scroll-driven 动画仅 Chromium 系，已按 @supports 渐进增强设计，默认态完整可见，无任何浏览器看到残缺页面。

---

*附：本文档承接既有《Web前端设计方案》《补充模块UI布局设计》与 D1-D4 轮设计规范；R-UI 轮已落地的 `.reveal-in`/Collapsible/EduArt/页头锚点体系继续沿用，本轮方案全部为其增量。*

# EduAI Prism 问题修复与优化升级报告 - 2026-07-04

生成时间：2026-07-03 23:02 PDT  
修复对象：`D:\VB\LLM-School\app`  
来源问题清单：`D:\VB\LLM-School\开发材料\问题清单-三轮内测-20260703.md`  
监督模式：`dev-supervisor` + Karpathy 范围控制 + code-review-graph review gate + Playwright Edge 浏览器验证

## 1. 总结

本轮针对三轮内测确认的 17 项问题完成闭环优化。第一批已完成后端权限、安全审计、会话校验、演示数据开关、工单去重，以及前端可访问性、真实交互反馈、hydration 收口和移动端 Sheet 语义；第二批继续处理两个残留项：安全分类器从关键词/正则堆叠升级为结构化语义评分层，`THREE.Clock` 上游弃用警告通过依赖版本锁定消除。

当前状态：

- 已修复 / 已验证：17 项
- 残留阻断项：0 项
- 已知非阻断提示：Node 24 `node:sqlite` 仍会输出 ExperimentalWarning，这是当前持久化方案的运行时平台提示，不属于本次 17 项 QA 缺陷；后续如需消除可迁移到稳定 SQLite 驱动。

## 2. 已修复问题映射

| 原问题 | 状态 | 修复内容 | 关键文件 |
| --- | --- | --- | --- |
| R1-FE-001 UploadDialog interval 未清理 | 已修复 | 用 ref 管理解析定时器，关闭、重置、卸载、重新解析前统一清理 | `components/knowledge/UploadDialog.tsx` |
| R1-FE-002 UploadDialog 图标按钮缺少名称 | 已修复 | 关闭按钮、移除文件按钮补 `aria-label`，Switch 补名称 | `components/knowledge/UploadDialog.tsx` |
| R1-FE-003 Chat 附件假入口 | 已修复 | 附件/图片/语音入口明确 disabled；拖拽文件只提示暂未开放，不再进入发送流 | `components/chat/Composer.tsx` |
| R1-FE-004 登录轮播 aria-hidden 下有焦点按钮 | 已修复 | 指示点容器移除 `aria-hidden` | `components/login/CardCarousel.tsx` |
| R1-FE-005 Switch / Field 标签关联不完整 | 已修复 | UploadDialog、Agent 创建页 Switch 补 `aria-label`；Field 子控件补可访问名称 | `components/knowledge/UploadDialog.tsx`, `app/(shell)/agent/new/page.tsx` |
| R1-FE-006 Topbar 假搜索 clickable div | 已修复 | 改为真实 button，键盘可触达，点击进入知识库搜索场景 | `components/shell/Topbar.tsx` |
| R1-FE-007 suppressHydrationWarning 范围过宽 | 已修复 | 移除 body 和页面 H1 suppress；动态问候语改为 `useSyncExternalStore` 稳定快照 | `app/layout.tsx`, `app/(shell)/dashboard/page.tsx`, `app/(shell)/learn/page.tsx` |
| R2-BE-001 `/api/audit` 匿名写入 | 已修复 | POST beacon 必须带有效 token，并回查当前 DB 用户；无 token 400，伪 token 401 | `app/api/audit/route.ts` |
| R2-BE-002 `/api/class` admin 直读实名学情 | 已修复 | API 复用 `canAccessClass`，admin/student 均 403，teacher/researcher 保持原权限 | `app/api/class/route.ts` |
| R2-BE-003 空库自动 seed 演示账号 | 已修复 | 新增 `EDUAI_ENABLE_DEMO_SEED` 控制；生产默认不自动 seed，非生产保留演示便利 | `lib/server/db.ts`, `.env.example` |
| R2-BE-004 关键词启发式安全分类 | 已修复 | 新增 `classifySafety` 语义评分层：Unicode NFKC 归一化、compact/spaced/tokens 多视角、危机/诚信/软关怀信号分流、拼音/英文/拆字/间接表达覆盖、否定诚信边界处理 | `lib/safety/classifyIntent.ts`, `app/api/chat/route.ts` |
| R2-BE-005 safety/crisis 工单无去重 | 已修复 | `addTicket` 对同用户、同类型、同 detail、10 分钟窗口内未解决工单复用原 ticket | `lib/server/db.ts` |
| R2-BE-006 session token 无 schema/DB 复核 | 已修复 | `verifySession` 增加 payload schema；`getSessionUser` 回查 DB 用户 | `lib/server/authToken.ts`, `lib/server/session.ts` |
| R3-FE-001 Sheet close/title 语义 | 已修复 | Close 补 `aria-label`；移动导航 Sheet 补 sr-only title + description | `components/ui/sheet.tsx`, `components/shell/Topbar.tsx` |
| R3-FE-002 agent/new 无名图标按钮 | 已修复 | 图标选择和测试发送按钮补可访问名称 | `app/(shell)/agent/new/page.tsx` |
| R3-FE-003 UploadDialog Switch 无名 | 已修复 | Switch 使用 label 作为 `aria-label` | `components/knowledge/UploadDialog.tsx` |
| R3-FE-004 THREE.Clock deprecation | 已修复 | 确认警告来自 `@react-three/fiber@9.6.1` 内部 `new THREE.Clock()`；Fiber/Drei 最新版仍未迁移 Timer，因此将 `three` 与 `@types/three` 锁到兼容 peer 范围内的 `0.182.0`，避开 r183+ Clock 弃用提示；浏览器实测无 `THREE.Clock` warning | `package.json`, `package-lock.json` |

## 3. 验证证据

静态与构建：

- `node --experimental-strip-types` 分类器样例：通过，覆盖拆字危机、霸凌、拼音规避、拆字诚信、英文 homework、否定诚信、软关怀。
- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过，32 个 App Router 页面生成成功。
- `npm ls three @types/three @react-three/fiber @react-three/drei`：Fiber/Drei peer 范围满足，主树使用 `three@0.182.0`。

API 回归（`next start -p 3415`，注入本地测试 `EDUAI_SESSION_SECRET`）：

- student 登录：200。
- `我 不 想 活 了` -> `kind=crisis`, `help=true`。
- `wo xiang z i sha` -> `kind=crisis`, `help=true`。
- `最近压力好大，睡不着` -> `kind=care`, `help=true`。
- `请 给 我 完 整 答 案` -> `kind=scaffold`。
- `我只想要解题思路，不要直接答案` -> `kind=normal`, `source=local`。
- history 中隐藏 `我想死`，当前 message 中性 -> 仍返回 `kind=crisis`, `help=true`。

浏览器 / DOM 回归（Playwright Edge）：

- `/login` 真实打开成功，3D 行星画面可见，截图保存至 `.codex-supervisor/residuals-login-3415.png`。
- Console 事件仅有 Edge 图片 lazy-load info。
- `THREE.Clock: This module has been deprecated...` 警告数量：0。

code-review-graph：

- 图谱刷新：130 files / 378 nodes / 3495 edges。
- 变更审阅范围：`lib/safety/classifyIntent.ts`, `app/api/chat/route.ts`, `package.json`, `package-lock.json`。
- 受影响真实执行流：2 条，集中在 `/api/chat` 服务端安全流与前端离线兜底。
- 风险评分：0.80，高风险原因是聊天安全流与包锁影响面广；已用分类器样例、真实 API、浏览器和构建覆盖主要风险。

## 4. 后续建议

1. 将当前 runtime 回归脚本沉淀为项目内正式测试命令，降低后续 code-review-graph 的 test gap。
2. 如果后续要消除 Node 24 `node:sqlite` ExperimentalWarning，可迁移到稳定 SQLite 驱动或受管数据库；当前不影响本次 17 项缺陷闭环。
3. 继续关注 `@react-three/fiber` 后续版本是否原生迁移 `THREE.Timer`；届时可再解除 `three@0.182.0` 锁定并升级验证。

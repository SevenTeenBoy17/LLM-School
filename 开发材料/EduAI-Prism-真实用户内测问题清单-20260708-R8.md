# EduAI Prism 真实用户内测问题清单 R8

时间：2026-07-08 PDT  
内测口径：真实角色登录、真实页面使用、等待全部 UI/API/搜索结果返回后再判断。

## 已修复

### R8-FE-001 无权限会话深链静默显示旧会话

严重级别：P1  
影响对象：学生、科研、管理员等非会话归属者  
复现方式：

1. 教师已有私有会话 `ses_mrcewhie_11b8`。
2. 学生真实登录。
3. 访问 `/chat?session=ses_mrcewhie_11b8`。

修复前表现：不泄漏教师内容，但页面静默展示学生最近会话，URL 保留教师 session 参数。  
修复后表现：显示“该会话不存在，或你没有权限访问。已为你开启新的对话。”，清空旧消息，地址栏清理为 `/chat`。

验证证据：

- Browser：`browser-final-r8.json` 和 `student-invalid-session-final3-r8.png`
- API：`api-cross-role-r8-final.json`
- 静态：lint / tsc / build 全部通过

## 未发现泄漏

- 学生、科研、管理员访问教师目标会话详情均为 404。
- 学生、科研、管理员全局搜索 R7 token 均无 session 命中。
- 学生无权限深链页面不显示目标 token、教师姓名、班级学情或家校沟通内容。

## 残余风险

- 本地生产服务需要显式 `EDUAI_SESSION_SECRET`。本轮仅使用进程内临时密钥进行测试，没有写入密钥文件。
- 当前 R8 只关闭“跨角色会话隔离 + 深链体验”切片；总体 12 小时真实内测仍继续进行。

# EduAI Prism 三轮内测问题修复与优化升级报告

日期：2026-07-05  
范围：`D:\VB\LLM-School\app` 登录后前端、管理端、API、安全、可访问性与浏览器可视化复测  
结论：上一轮 14 项确认问题已全部修复或闭环处理；复测通过。仍需生产化前单独规划历史工单数据清洗、分布式限流、session 撤销表等增强项。

## 一、修复总览

| 原问题 | 状态 | 修复摘要 |
| --- | --- | --- |
| QA-001 管理端硬刷新误跳 `/dashboard` | 已修复 | `admin/layout.tsx` 移除过时的客户端 zustand 角色跳转，管理端准入以 `proxy.ts` + session cookie 为权威。 |
| QA-002 审计行无名图标按钮 | 已修复 | 无动作的 `MoreHorizontal` 改为非交互装饰元素，避免读屏器暴露假按钮。 |
| QA-003 多处 Switch 无程序化名称 | 已修复 | 权限、模型、个人设置、Agent 配置等开关补充 `aria-label`。 |
| QA-004 `/profile`、`/prompts/new` 表单 Label 未关联 | 已修复 | Field 统一支持 `id/htmlFor`，Input、Textarea、SelectTrigger 显式绑定。 |
| QA-005 审计/工单 fetch 失败被吞 | 已修复 | 新增 `auditError/ticketsError`，失败时显示 `role=alert`，不再伪装为“暂无数据”。 |
| QA-006 无反馈按钮 | 已修复 | Chat 提示词库、回答工具按钮、Topbar 帮助/通知均有真实 handler 或 toast/status 反馈。 |
| QA-007 管理端模型卡假可点击容器 | 已修复 | 外层卡片回归展示容器，选择行为收敛到真实按钮。权限用户行也同步去除假点击区。 |
| QA-008 知识库移动端表格裁切 | 已修复 | 表格容器改 `overflow-x-auto`，表头/行使用一致 `min-w`。 |
| QA-009 chat/safety/integrity 缺接口级限流 | 已修复 | 新增 `lib/server/rateLimit.ts`，三个认证后接口按 `user.id` 限流，并验证伪造 XFF 不能绕过。 |
| QA-010 工单隐私与状态闭环不足 | 已修复 | detail 写入/读取脱敏和截断；`/api/safety/tickets` 新增受保护 PATCH；管理端可标记跟进/完成。 |
| QA-011 科研 pending 元数据可重识别 | 已修复 | researcher 视图 pending 使用 `pending-N`、日级时间桶、统一标题。 |
| QA-012 Logo preload warning | 已修复 | `BrandLogo` 移除无条件 `priority`，浏览器复测 console warning=0。 |
| QA-013 session token claim 过多 | 已修复 | 签名 cookie 只保留 `id/role/exp`，服务端通过 DB 重新补全用户资料。 |
| QA-014 `/api/class` 注释与权限漂移 | 已修复 | 注释改为 teacher/researcher 产品边界，admin 继续 403。 |

## 二、额外复审发现与处理

- 后端 reviewer：`pass`。指出 P3 风险：限流依赖可伪造 `x-forwarded-for`。已改为认证后接口只按 `user.id` 限流，并复测伪造 XFF 仍触发 429。
- 前端 reviewer：`request_changes`。指出 Slider thumb 缺少可访问名称。已给共享 `Slider` 增加 `thumbLabel`，并覆盖模型配额、字号缩放、Agent 温度/上下文/每日配额滑块。
- 历史工单 at-rest 清洗：不在本轮自动执行，因为属于已有数据改写；建议生产化前备份后运行一次性迁移脚本。

## 三、复测证据

| 轮次 | 命令/工具 | 结果 |
| --- | --- | --- |
| 静态质量 | `npm run lint` | 通过 |
| 类型检查 | `npx tsc --noEmit` | 通过 |
| 生产构建 | `npm run build` | 通过；仅 Node 24 `node:sqlite` ExperimentalWarning |
| 后端真实 HTTP | Node fetch 脚本 | 26/26 通过：四角色登录、admin 硬路由、权限边界、科研脱敏、工单脱敏/PATCH、三类限流、session claim 最小化 |
| XFF 绕过复测 | Node fetch 脚本 | 通过：变化 `x-forwarded-for` 仍在第 31 次 chat 请求触发 429 |
| 浏览器全量 QA | Playwright Edge | 20 路由桌面/移动通过；console=0、namelessButtons=0、unnamedFields=0、fakeClickable=0、horizontalOverflow=0 |
| Slider 定向 a11y | Playwright Edge | 通过：`/admin/models`、`/profile` 偏好页、`/agent/new` 第 2/6 步 slider 均可按 name 定位 |
| code-review-graph | full rebuild + scoped review | 131 files / 386 nodes / 3557 edges / 45 flows / errors=[] |

## 四、交付文件

- 修复代码目录：`D:\VB\LLM-School\app`
- 浏览器截图与 DOM 审计：`D:\VB\LLM-School\.codex-supervisor\qa-20260705-browser`
- 本报告：`D:\VB\LLM-School\开发材料\问题修复与优化升级报告-20260705.md`

## 五、剩余建议

1. 生产化前增加持久化/分布式限流，例如 Redis 或边缘风控。
2. 增加 session nonce/session 表，支持服务端撤销单个会话。
3. 对历史安全工单 detail 做备份后脱敏迁移。
4. 将本轮 HTTP 与 Playwright 脚本沉淀为项目内 CI 测试，避免后续回归依赖手工脚本。

# EduAI-Prism 真实用户内测问题清单 R16

日期：2026-07-08  
轮次：R16  
范围：个人中心 `/profile`、账号资料、偏好设置、安全验证

## 总览

| 编号 | 模块 | 优先级 | 状态 | 问题/结论 | 修复/处理 |
| --- | --- | --- | --- | --- | --- |
| R16-PROFILE-001 | 个人中心 tab 路由 | P1 | 已修复 | `/profile?tab=prefs` 未打开“偏好设置”，入口与落点不一致。 | 新增 URL-driven tabs，`tab=prefs` 可直达，tab 切换会写回 URL。 |
| R16-PROFILE-002 | 触控尺寸 | P2 | 已修复 | 个人中心 tabs、按钮、开关低于稳定触控尺寸。 | 在个人中心局部增大 tabs、操作按钮和 switch，不改全局 primitive。 |
| R16-PREF-003 | 偏好保存反馈 | P2 | 已修复 | 偏好保存没有可见状态，失败时缺少恢复机制。 | 新增保存状态提示，成功同步服务端返回，失败时恢复服务端设置。 |
| R16-ACCOUNT-004 | 账号资料保存 | - | 通过 | 修改姓名/简介、保存、API 读回、恢复原值均通过。 | 无需修复。 |
| R16-SEC-005 | 密码错误验证 | - | 通过 | 不匹配、弱密码、错误旧密码均正确提示，原密码仍可登录。 | 未执行成功改密，避免破坏演示账号。 |
| R16-QA-006 | 测试环境 | P3 | 已记录 | 工作区和 app 目录不是 git 仓库，无法做 git checkpoint；部分旧日志在 PowerShell 预览中显示中文乱码。 | 用 supervisor ledger/handoff/report/code-review-graph/manual review 作为替代证据；新报告直接写入 UTF-8 中文。 |

## 复测结果

| 检查项 | 结果 |
| --- | --- |
| 真实教师登录 | 通过 |
| `/profile?tab=prefs` 直达偏好设置 | 通过 |
| 偏好保存可见反馈 | `已保存到服务端` |
| API 读回 | 通过 |
| 临时偏好恢复 | 已恢复 `reduceMotion=false` |
| 小触控目标 | `[]` |
| 横向溢出 | 无 |
| console error/warn | 0 |
| lint | 通过 |
| tsc | 通过 |
| build | 通过 |
| code-review-graph | 通过 |

## 质量判断

R16 的三个确认缺陷均已修复，并且按真实使用闭环复测通过。该页面当前满足“教师/管理员真实进入、修改、等待、确认、恢复”的内测要求。

后续全站内测继续沿用本轮校正后的标准：不以瞬时截图或单次接口返回作最终结论，必须等待真实 UI、服务端读回、持久化状态、错误处理和页面日志全部稳定后再评判。

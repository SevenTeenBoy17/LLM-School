# EduAI-Prism 真实用户内测问题清单 R51

## 已修复

| ID | 严重级别 | 问题 | 修复 | 验证 |
| --- | --- | --- | --- | --- |
| R51-P1-001 | P1 | `/api/knowledge` 相关响应缺少 `Cache-Control: no-store`，校内知识库资料列表、搜索片段、详情预览存在缓存风险。 | 三个知识库 API 路由统一使用 `jsonNoStore` 返回。 | API 42 项通过，四角色列表均返回 `cacheControl=no-store`。 |
| R51-P2-002 | P2 | 移动端知识库搜索输入框实际高度 35px，低于 40px 触控底线。 | 主搜索输入增加 `h-10 min-h-[40px]`。 | 浏览器 390px 检查 `smallTargets=[]`，无横向溢出。 |
| R51-QA-003 | QA | 浏览器脚本等待条件可能被旧文本抢跑，不能严格证明搜索结果和详情回读。 | 等待搜索结果卡片和右侧详情 `aside` 的正文预览同时返回唯一标记。 | 浏览器 11 项通过，包含搜索命中、详情回读、API 二次回读和清理。 |

## 本轮未发现问题

- 未发现跨角色知识库资料泄漏。
- 未发现上传完成状态与后端返回不一致。
- 未发现二进制文件被误称为全文索引。
- 未发现移动端横向溢出。
- 未发现浏览器 console error/warning。

## 后续建议

- 后续独立轮次可覆盖 PDF/Word/PPT OCR、向量检索、批量上传大文件、弱网中断续传。
- 若进入生产仓库，应补充 git diff 审查和 CI 中的知识库 API 回归用例。

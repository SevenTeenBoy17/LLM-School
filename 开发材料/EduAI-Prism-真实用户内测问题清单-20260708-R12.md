# EduAI-Prism 真实用户内测问题清单 R12

日期：2026-07-08  
内测切片：管理员移动端审计治理  
方法：真实学生越权 + 真实管理员移动端复盘，等待页面/API/导出状态完整返回后判定。

| ID | 严重级别 | 状态 | 问题 | 处理 |
| --- | --- | --- | --- | --- |
| R12-ADM-MOB-001 | P1 | 已修复 | 390px 移动端 `安全审计日志` 卡头被 tabs 挤压，说明文字极难阅读。 | 审计页局部 `CardHeader` 移动端改为纵向堆叠，桌面端保持横向。 |
| R12-ADM-MOB-002 | P1 | 已修复 | 服务端真实审计和安全工单只提供宽表，真实管理员手机端研判效率低。 | 移动端新增真实审计卡片和工单卡片，桌面端保留宽表。 |
| R12-ADM-MOB-003 | P2 | 已修复 | 导出、筛选、搜索、工单跟进等页面控件触控高度不足；“跟进”曾约 `33 x 21px`。 | 页面内关键控件提升到 `h-12/min-h-12`，最终实测高度 `42px`，`smallPageTargets=[]`。 |
| R12-ADM-EXP-004 | P2 | 已修复 | Blob CSV 下载事件不可见时，管理员缺少页面级导出确认。 | 导出后显示 `role="status"`：`已生成 68 条真实审计 CSV`。 |
| R12-QA-005 | P3 | 已记录 | Browser `domSnapshot()` 在本应用页面不可用，Blob download event 仍不可捕获。 | 使用同一真实浏览器的 evaluate、截图、dev logs、页面状态作为替代证据；不把工具限制误判为产品通过/失败。 |
| R12-SHELL-006 | P2 | 待下一轮 | 顶部全局移动导航图标按钮偏小，影响全站移动端触控一致性。 | 未在本轮改全局 shell，建议下一轮做跨页面导航触控回归。 |

## 最终验收

- 最新真实越权记录：`2026/7/8 13:49:40 · u-student · /admin/audit · unauthorized_access · 拒绝`
- 管理员移动端真实数据：`68` 条服务端审计，`4` 条安全求助工单
- 页面溢出：`docScrollWidth=380`，`bodyScrollWidth=380`
- 页面内关键控件：全部实测 `42px` 高，无小触控目标
- 导出状态：`已生成 68 条真实审计 CSV`
- 控制台错误/警告：无
- 静态验证：lint / tsc / build 通过
- 审阅：code-review-graph 构建通过，手动影响面审阅通过

# EduAI-Prism 真实用户内测问题清单 R23

日期：2026-07-08 PDT  
内测切片：智能体发布后进入 Chat 的真实使用闭环。

| ID | 严重级别 | 问题 | 处理状态 | 验证证据 |
| --- | --- | --- | --- | --- |
| R23-AGENT-CHAT-001 | P1 | Chat 会话未持久化智能体 `agentId`，刷新/深链会丢失身份 | 已修复 | API 回读 `session.agentId=agt_mrcrn3nl_7vxf`；浏览器 `/chat?session=...` 恢复横幅 |
| R23-AGENT-CHAT-002 | P1 | `/api/chat` 信任客户端 `agentPreamble`，可伪造智能体身份 | 已修复 | 伪造请求返回 `400 invalid_agent_contract` |
| R23-AGENT-CHAT-003 | P1 | 网关降级时本地回复不体现已选智能体身份 | 已修复 | 完整回复包含 `我是「R23班会协作智能体-754679」` |
| R23-AGENT-CHAT-004 | P2 | 本地智能体回复对真实班会脚本任务偏泛 | 已修复 | 完整回复包含“暖场 / 分组任务 / 复盘收束” |
| R23-QA-001 | P3 | 首次浏览器自动化被首登引导遮挡，测试脚本跳过选择器不准 | 已修正测试方法 | 第二次浏览器使用 `aria-label=跳过引导` 后通过 |
| R23-QA-002 | P3 | Node 拼 Cookie 读取会话列表返回 401，不可作为浏览器读回证据 | 已改用页面自身 fetch | `r23-final-browser-session-restore-readback.json` 中页面 fetch 返回 200 |

## 本轮通过条件

- 教师创建并提交智能体，管理员发布，学生使用入口可进入。
- 学生完整等待 AI/API 返回后，回复体现智能体身份和真实任务结构。
- 会话持久化 `agentId`，刷新和深链恢复正常。
- 客户端伪造前置词和会话智能体不匹配均被拦截。
- 智能体禁用后，学生无法继续使用旧会话或新建绑定会话。
- 测试会话和测试智能体清理完成。

## 本轮剩余风险

- 外部网关仍降级，约 24 秒等待是留存风险。
- 本地降级仍需要更多真实智能体任务 fixture。
- 无 git 仓库，不能做 commit/PR 型审查。

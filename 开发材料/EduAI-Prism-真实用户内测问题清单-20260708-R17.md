# EduAI-Prism 真实用户内测问题清单 R17

日期：2026-07-08  
轮次：R17  
范围：后台权限管理 `/admin/permissions`

## 总览

| 编号 | 模块 | 优先级 | 状态 | 问题/结论 | 修复/处理 |
| --- | --- | --- | --- | --- | --- |
| R17-ADM-PERM-001 | 权限管理 UI | P2 | 已修复 | 导出/新增/批量/编辑/保存按钮和权限开关触控尺寸偏小，最小约 18px。 | 在权限页局部增大按钮与 switch；复测 `smallTargets=[]`、switch 约 `35 x 56px`。 |
| R17-ADM-PERM-002 | 角色权限保存 | - | 通过 | 管理员通过 UI 修改教师角色 `用户管理`，页面显示已保存。 | API 读回 `用户管理=true`，审计记录生成。 |
| R17-ADM-PERM-003 | 权限恢复 | - | 通过 | 临时权限变更必须恢复，避免污染演示数据。 | 通过 UI 恢复为 false，API 读回 `用户管理=false`。 |
| R17-ADM-PERM-004 | API 安全边界 | - | 通过 | 学生不得读写权限 API；非法角色不得进入持久化。 | 学生 GET/PUT 返回 403；非法角色 GET/PUT 返回 400。 |
| R17-QA-005 | 测试环境 | P3 | 已记录 | 浏览器页面 evaluate 不暴露 fetch；PowerShell 管道中文字面量可能转码。 | UI 走真实浏览器；API 读回用同角色 HTTP 会话；中文 key 使用 Unicode escape 复核。 |

## 复测结果

| 检查项 | 结果 |
| --- | --- |
| 管理员可见登录 | 通过 |
| 权限页打开 | 通过 |
| 保存状态 | `已保存到服务端` |
| 保存后 API 读回 | `用户管理=true` |
| 恢复后 API 读回 | `用户管理=false` |
| 审计记录 | `role_perms_update` 已生成 |
| 非管理员权限 API | 403 |
| 非法角色 API | 400 |
| 小触控目标 | `[]` |
| 禁用小触控目标 | `[]` |
| 横向溢出 | 无 |
| console error/warn | 0 |
| lint / tsc / build | 全部通过 |
| code-review-graph | 通过 |

## 质量判断

R17 证明权限管理的核心保存、审计、恢复、后端边界是可用的；同时修复了后台高风险页面里容易影响真实管理员操作信任的触控尺寸问题。

下一轮仍应继续选择一个真实完整场景，而不是回到静态按钮点检。

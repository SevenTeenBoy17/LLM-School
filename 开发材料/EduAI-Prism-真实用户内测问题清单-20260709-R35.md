# EduAI-Prism 真实用户内测问题清单 R35

日期：2026-07-09  
主题：管理端智能体监控页移动端可用性、状态流转与权限边界

| 编号 | 严重级别 | 问题 | 真实影响 | 处理状态 | 验证证据 |
| --- | --- | --- | --- | --- | --- |
| R35-ADM-MOB-001 | P2 | `/admin/agents` 移动端分类筛选、导出、AdminTabs、表格/看板切换等控件高度低于稳定触控目标 | 管理员在手机上处理审核/筛选时容易误触，影响高频治理效率 | 已修复 | 最终 R35 日志 `mobileSmallTargets=0` |
| R35-QA-001 | P3 | R35 脚本把预期的 403 越权边界探测计入 console error | 测试口径不准确，会把安全边界通过误判为产品异常 | 已修正 | 最终 R35 日志 `unexpectedConsoleEntries=[]` |
| R35-ENV-001 | P3 | 修复后第一次重启未注入 `EDUAI_SESSION_SECRET`，导致登录 500 | 测试环境启动问题，会阻断复测但不是产品缺陷 | 已修正 | 服务以进程级临时 secret 重启，登录 API 200 |

## 回归验证清单

- 教师创建并提交待审智能体：通过。
- 管理员桌面端列表可见 R35 临时智能体：通过。
- 管理员分类筛选“教学”返回正确选中态并仍包含目标智能体：通过。
- 表格/看板切换返回正确 `aria-pressed`：通过。
- 导出数据出现 `CSV 已生成` 可见反馈：通过。
- 审核通过返回 `pub`：通过。
- 停用返回 `disabled`：通过。
- 重新发布返回 `pub`：通过。
- 审计日志包含 `agent_status_pub` 和 `agent_status_disabled`：通过。
- 学生可读已发布智能体详情/use：通过。
- 学生 PATCH/DELETE 越权返回 `403`：通过。
- 教师 owner 对已发布智能体执行 pub->draft 返回 `403`：通过。
- 移动端小目标数为 `0`：通过。
- 页面级横向溢出为 `false`：通过。
- 临时智能体 cleanup 返回 `200`：通过。

## 结论

R35 已关闭。管理端智能体监控页的移动端高频控件已达稳定可点击尺寸，桌面状态流转、审计、越权边界和清理闭环均通过。

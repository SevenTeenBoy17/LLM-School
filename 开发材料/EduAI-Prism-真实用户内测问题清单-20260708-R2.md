# EduAI Prism 真实用户内测问题清单 R2

日期：2026-07-08  
方法：真实用户路径、真实学校/公司内部研讨视角、等待内容完整返回后评价  
本轮范围：移动端 `/chat` 会话恢复、AI 回答质量、接口兼容、回答来源/耗时追溯

| ID | 严重级别 | 问题 | 状态 | 处理结果 |
| --- | --- | --- | --- | --- |
| R2-MOB-001 | P2 | 移动端缺少明显的新建/历史会话入口，真实用户难以恢复历史研究任务 | 已修复 | 增加移动端“发起新对话 / 最近会话”入口与会话操作 |
| R2-AI-002 | P2 | 研究员数据采集类 fallback 回答不够像可直接落地的教研表格 | 已修复 | 增加字段、填写频率、填写口径、隐私边界、教师建议 |
| R2-API-003 | P1 | 会话列表可见但详情恢复链路不稳定，移动端切回历史会话可能失败 | 已修复 | 增加 query 详情接口，客户端统一使用稳定 URL |
| R2-AUDIT-004 | P2 | 恢复历史回答时缺少来源和耗时，内部评审无法判断是真模型还是降级 | 已修复 | 新 assistant 消息持久化并展示 source/durationMs |
| R2-COMPAT-005 | P1 | 旧路径 `/api/chat/sessions/:id` 返回 200 但实际是列表，messages 为空 | 已修复 | 移除错误 rewrite，恢复动态路由作为旧路径事实来源 |

## 本轮已验证通过项

- 移动端 390 x 844 真实登录研究员账号。
- 最近会话抽屉可打开。
- 目标研究会话可点击恢复。
- 页面恢复后不再显示空态。
- 表格字段、填写频率、隐私边界、教师建议完整可见。
- `24.0s · 网关降级` 可见。
- 旧路径与新路径均返回 2 条消息且元数据一致。
- 控制台 error/warn 为 0。
- `npm run lint`、`npx tsc --noEmit`、`npm run build` 均通过。

## 未覆盖项

- 学生端安全与学术诚信真实长流程。
- 教师端备课/出题/班级管理真实工作流。
- 管理员端模型治理、权限、审计、安全工单。
- 长时间多轮会话与跨页面恢复。
- 多浏览器与桌面/平板响应式回归。

## 保留风险

- 修改前的旧历史消息没有 `source/durationMs`，无法真实反推，不做伪造补全。
- 当前 workspace 不是 git 仓库，无法做 checkpoint commit 或 diff-based code-review-graph review。
- 本轮没有部署生产环境，只验证本地 `http://127.0.0.1:4920`。

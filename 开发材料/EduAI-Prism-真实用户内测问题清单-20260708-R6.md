# EduAI-Prism 真实用户内测问题清单 R6

日期：2026-07-08  
范围：管理员治理工作流

## 已修复

| ID | 严重级别 | 页面 | 问题 | 修复 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| R6-FE-001 | P2 | `/admin/audit` | 审计搜索框只显示输入值，不过滤样例审计行。 | 接入受控搜索状态，对时间、用户、角色、操作、资源、风险、状态筛选，并补空结果。 | 搜“未知IP”只剩目标行；搜不存在词显示空状态。 |
| R6-FE-002 | P2 | `/admin/permissions` | 用户搜索框只显示输入值，不过滤角色用户行。 | 接入受控搜索状态，对姓名、工号、学院、角色、状态筛选，并补空结果。 | 搜 `T2015007` 只剩陈教授；搜不存在词显示空状态。 |

## 验证通过项

- 管理员真实登录：通过。
- 工单处理：待跟进 -> 跟进中 -> 已处理，通过。
- 模型治理开关保存与恢复：通过。
- 权限报表 CSV 导出：通过，真实下载事件已保存。
- 审计 CSV 导出：通过，首次引导关闭后生成 CSV Blob 与下载文件名。
- API 最终回读：通过。
- lint / TypeScript / build：通过。
- code-review-graph 构建：通过。

## 残余风险

- 内置浏览器 `domSnapshot()` 能力在当前会话失败，已降级为 evaluate/visible DOM/Playwright 验证。
- 工作区不是 git 仓库，无法创建 checkpoint commit 或做 git diff review。
- 本轮只覆盖管理员治理切片；完整 12 小时真实内测仍需继续。

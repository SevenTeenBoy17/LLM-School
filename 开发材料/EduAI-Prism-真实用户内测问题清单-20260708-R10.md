# EduAI-Prism 真实公司级内测问题清单 R10

时间：2026-07-08

## 已修复

| 编号 | 严重级别 | 问题 | 复现路径 | 修复 | 复测 |
| --- | --- | --- | --- | --- | --- |
| R10-P2-001 | P2 | 管理员 CSV 导出缺少可观测完成反馈，内测会无法确认导出是否完成 | 管理员登录 `/admin/audit`，点击「导出日志（服务端真实记录）」 | `exportCsv()` 增加成功/失败 toast，并返回 CSV 字符串；新增 `buildCsvContent()` | 0.5s 内出现 `CSV 已生成 · 服务端审计日志.csv · 65 条记录`；lint/tsc/build 通过 |

## 观察项

| 编号 | 类型 | 现象 | 建议 |
| --- | --- | --- | --- |
| R10-OBS-001 | 产品治理 | 学术诚信留痕只展示聚合频次，不暴露学生原始请求文本 | 维持隐私保护；后续如需教研复盘，可增加匿名事件摘要，不展示原文和个人处分倾向 |
| R10-OBS-002 | 移动表格体验 | 管理端审计表在移动视口需要横向滚动才能看全路径/操作/结果 | 当前可用；后续可为移动端增加卡片式审计行，减少横向滚动 |

## 工具限制

| 编号 | 限制 | 影响 | 替代证据 |
| --- | --- | --- | --- |
| R10-TOOL-001 | In-app Browser 对 Blob CSV 下载没有稳定 download event | 无法直接证明 OS 下载文件已保存 | 页面成功 toast + `/api/audit` 内容复核 + `buildCsvContent()` 代码审查 |
| R10-TOOL-002 | 工作区不是 git 仓库 | 无法做 checkpoint commit 或 git diff review | code-review-graph build/status + 手动影响审查 |

## 继续内测建议

1. 下一轮优先做真实教研会议材料工作流：教师生成会议纪要、离开页面、返回后复核完整内容与导出。
2. 补一次管理员移动端审计表可读性优化，重点观察路径、操作、结果是否无需横向滚动即可理解。
3. 对学术诚信聚合指标设计匿名 drilldown 原型，但必须先明确隐私边界。

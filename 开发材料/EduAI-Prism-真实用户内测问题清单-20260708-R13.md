# EduAI-Prism 真实用户内测问题清单 R13

日期：2026-07-08  
内测切片：跨页面移动端 Shell / Topbar / 导航抽屉  
方法：真实角色登录后等待页面内容返回，再测量和点击 shell 控件。

| ID | 严重级别 | 状态 | 问题 | 处理 |
| --- | --- | --- | --- | --- |
| R13-SHELL-001 | P1 | 已修复 | 移动端顶栏 `打开导航 / 帮助 / 通知 / 设置` 约 `30-32px`，真实手机误触风险高。 | `Topbar.tsx` 中移动端主按钮提升为 `h-12 w-12`，最终实测 `42px`。 |
| R13-SHELL-002 | P2 | 已修复 | 移动导航抽屉项约 `39px`，管理员项目较多时点击密度偏高。 | `MobileNav.tsx` 导航项提升到 `min-h-12`，最终实测 `42px`。 |
| R13-SHELL-003 | P2 | 已修复 | `/learn`、`/explore`、`/class` 缺少专属顶栏标题，学生页显示泛化品牌文案并换行。 | `Topbar.tsx` 增加路径标题映射，标题容器增加移动端截断保护。 |
| R13-SHELL-004 | P2 | 已修复 | 设置菜单项约 `34px`，通知 `全部已读` 约 `32px`。 | `DropdownMenuItem`、通知操作、通知条目提升到 `min-h-12`，最终全部 `42px` 或更高。 |
| R13-QA-005 | P3 | 已记录 | 初始测试将动态问候语 `中午好` 写死，下午时段断言失败。 | 改用稳定页面内容 `今日 AI 对话 / 教学快捷入口 / 首页` 作为等待条件。 |

## 最终验收

- 顶栏按钮：`打开导航 / 帮助 / 通知 / 设置` 均 `42 x 42px`
- 移动抽屉导航项：`42px`
- 设置菜单项：全部 `42px`
- 通知 `全部已读`：`42px`
- 通知条目：`93px`
- 抽屉导航：精确点击 `班级学情` 后进入 `/class`
- 学生页标题：`我的学习 · Learning`
- 班级页标题：`班级学情 · Class Insight`
- 页面溢出：无横向溢出
- 控制台：无 error/warn
- 静态验证：lint / tsc / build 通过
- 审阅：code-review-graph 构建通过，手动影响面审阅通过

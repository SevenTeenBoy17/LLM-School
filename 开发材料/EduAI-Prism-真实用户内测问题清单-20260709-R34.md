# EduAI-Prism 真实用户内测问题清单 R34

日期：2026-07-09  
主题：智能体删除后，学生旧会话、深链和后端边界是否仍可继续使用该智能体

| 编号 | 严重级别 | 问题 | 真实影响 | 处理状态 | 验证证据 |
| --- | --- | --- | --- | --- | --- |
| R34-FE/API-001 | P1 | 教师删除已发布智能体后，学生旧会话继续发送返回 `404 agent_not_found` | 学生历史学习任务中断，没有得到明确可继续的安全降级体验 | 已修复 | `r34-agent-delete-session-R34_AGENT_1783604768384.json`、`r34-post-followup-session-readback.json` |
| R34-AI-001 | P1 | 旧会话降级成普通对话后，AI 内容变成通用学习建议，未回应课堂 AI 安全任务 | 内测需要评价返回内容质量；仅状态 200 不合格 | 已修复 | `studentOldSessionSendAfterDelete.usable=true`，回复长度 479 |
| R34-QA-001 | P3 | 初始测试智能体名称超过后端 `max(40)`，导致创建返回 400 | 测试数据无效，会把测试脚本问题误判为产品问题 | 已修正 | QA harness 改用短 runId |
| R34-QA-002 | P3 | 初始脚本把历史消息/侧栏旧名称误判为智能体仍激活 | 判定口径不准确，不能代表真实产品状态 | 已修正 | QA harness 改为检查活跃智能体标识和 session `agentId` |

## 回归验证清单

- 教师测试面板返回完整 AI 内容后再创建：通过。
- 管理员审核按钮返回 `pub` 后再进入学生验证：通过。
- 学生发布态列表、详情、use、聊天深链：通过。
- 教师删除按钮返回 `200 ok`：通过。
- 删除后学生列表不再显示该智能体：通过。
- 删除后详情/use 返回 `404`：通过。
- 删除后聊天深链不再激活智能体，并显示不可用提示：通过。
- 删除后旧会话不再激活智能体，并显示不可用提示：通过。
- 删除后旧会话继续发送返回 `200` 普通对话：通过。
- 旧会话回读 `agentIdAfterFollowup=null`：通过。
- 显式传入已删除 agentId 调 `/api/chat` 返回 `404 agent_not_found`：通过。
- 内容质量评审：通过，回复包含生成式 AI、安全边界、课堂活动、教师检查点、学生讨论等关键内容。

## 结论

R34 已关闭。该轮确认“智能体删除”不会留下学生侧可继续使用的旧入口，同时历史会话不会被破坏，用户能收到明确提示并继续普通对话。

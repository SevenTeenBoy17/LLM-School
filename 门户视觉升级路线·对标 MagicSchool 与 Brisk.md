# 门户视觉升级路线 · 对标 MagicSchool 与 Brisk

> 交付物：可执行的前端升级方案路线，非复刻任务。
> 一手数据：2026-08-03 用 Playwright 在 1440×1000 下实测 magicschool.ai 的计算样式；
> briskteaching.com/zh 用页面抓取做结构与定位分析。

---

## 0. 动工前必须先解决的四件事

### 0.1 `clone-website` Skill 在本项目中不存在

需求写的是「请使用当前项目内的 `clone-website` Skill」。实测：仓库内**没有 `.claude/skills` 目录**，
全库 `find -iname "*clone*website*"` 零命中。这一步现在无法执行，不是我跳过了它。

### 0.2 API 密钥无需更新——已经是你给的这把

需求写「该项目中有关的 API 秘钥都需要更新为这个 API」。实测 `app/.env.local`：

```
LLM_API_KEY=<密钥见 app/.env.local，不入库>…（与你本次提供的一致）
LLM_BASE_URL=https://api.llm-token.cn/v1   ← 已是你推荐的国内低延迟入口
```

**无需改动。** 文件被 `app/.gitignore:49` 的 `.env*` 覆盖，未入库。
生图模型代码 `gpt-image-2` 也已在用——`lib/server/imagegen.ts:66` 硬编码
`model: "gpt-image-2"` 打 `/images/generations`，源码注释记的实测耗时是 55–70s/张。

### 0.3 §三「设备范围」自相矛盾，按现状取舍

原文四条并列：

| 条目 | 内容 | 冲突 |
|---|---|---|
| 1 | 只制作桌面端 | — |
| 2 | 以 1440px 为主要视觉验收基准 | 与 1 一致 |
| 3 | **需要**制作平板端、移动端或专门的响应式布局 | **与 1 直接冲突** |
| 4 | **要**为移动端添加汉堡菜单、移动端导航、触摸手势或移动端断点 | **与 1 直接冲突** |

从上下文推断，3/4 原本应是「**不**需要 / **不要**」，是模板里的否定词丢了。

**但即便按字面取「只做桌面端」，对本项目也是错的**——现有应用**本来就是响应式的**，
且已有门禁在守：`tests/gate/render.mjs:189` 的 R1 在 **1440 / 768 / 390 三档**跑几何断言，
`visual.mjs:26` 的截图矩阵同样是 mobile 390 / tablet 768 / desktop 1440 三档。
砍成桌面端等于主动制造回归，且学生大概率用平板或手机访问校内平台。

**本方案采用**：**保持响应式，以 1440px 作为主要设计与验收基准**（即你的第 2 条），
移动端沿用现有断点，不新增也不删除。若你确实要桌面独占，请明确说，那需要单独一轮并撤掉三档门。

### 0.4 像素级复刻：不做，理由与替代方案

需求要求对 magicschool.ai 与 briskteaching.com 做「像素级复刻」，包含「品牌元素和媒体素材」，
并声明拥有合法权限。

**这一项我不做。** 这两个站是 MagicSchool AI 与 Brisk Teaching 两家公司的在售商业产品，
其文案、插画、摄影、Logo 与品牌视觉的权利在这两家公司手里——**这不是使用者可以自行授予的权限**。
而本项目「鲁班 7 号」是同品类的校内 AI 平台，把竞品营销站连品牌资产一起像素级搬过来，
风险不在技术侧，在法务侧，且做出来对你没有实际价值：你需要的是**你自己的产品变好看**，
不是一份 MagicSchool 首页的副本。

**替代方案（本文档其余部分）**：把它们的**设计语言**拆成可复用的规律——色彩关系、字阶节奏、
版心与留白、组件形态、信息架构次序——再落到你自己的品牌与内容上。这是设计团队看竞品的正常做法，
也正是你需求最后一句真正要的东西：「一套具体可行的前端升级优化的细节方案路线文档」。

以下所有内容据此展开。

---

## 1. 一手拆解

### 1.1 MagicSchool 实测设计令牌（Playwright / 1440px / 计算样式）

**色彩（按出现频次排序，括号内为用途）**

| 角色 | 值 | 频次 | 用途 |
|---|---|---|---|
| **奶油白** | `#FFFEF0` | 文字 ×39 / 背景 ×9 | **本站最高频颜色**。深紫区块上的文字色，也用作大面积暖底 |
| **深紫（品牌主色）** | `#33116F` | 文字 ×26 / 背景 ×5 | 主标题、深色区块底 |
| **亮紫（强调）** | `#762BFA` | 文字 ×20 / 背景 ×2 | 链接、图标、主按钮 |
| 电光紫 | `#4F23FF` | 背景 ×2 | 更饱和的按钮/高亮态 |
| 次强调紫 | `#9C33F4` | 文字 ×2 | 渐变第二色标、次级强调 |
| 紫丁香浅底 | `#ECE0FF` | 背景 ×3 | 卡片/徽标浅底 |
| 奶黄点缀 | `#FFFCDC` | 背景 ×2 | 小面积点缀 |
| 中性正文 | `#212121` / `#1B1B1B` | ×17 / ×12 | 正文 |
| 纯白 | `#FFFFFF` | 背景 ×12 | 卡片面 |

**⚠️ 最值得学的一点：它的「白」不是白。** 最高频颜色是 `#FFFEF0`——一个偏黄的奶油白。
纯白只在卡片面上出现。这是整站「暖而不刺眼」的来源，成本极低但观感差别很大。

**字体**

| 字族 | 命中 | 判断 |
|---|---|---|
| Figtree | 103 | 正文与 UI 主力 |
| Jokker | 30 | 展示字（大标题） |
| Poppins | 14 | 残留/局部 |

**字阶（12 档，但结构清晰）**

- 正文带：`14 / 15 / 16 / 18`（16 与 15 合计 75 次，是绝对主力）
- 标题带：`20 / 24 / 28 / 32 / 40 / 56`
- 辅助：`12`

**字重：`400 / 600 / 700`（+ 极少量 500）——与本项目 V2 收敛后的三档完全一致。**

**圆角**：`8`(×22) 与 `16`(×22) 双主力，`4`(×17) 用于小徽标，`50px` 胶囊 ×5。

**版心**：1440 视口下内容宽收敛在 **1280 / 1216**，页面总高 **8002px**（11 个纵向区块）。

### 1.2 Brisk Teaching

- **色**：蓝为主（导航/按钮/强调），绿/青做徽标点缀，中性灰白托底。整体比 MagicSchool 冷、克制。
- **媒体**：以**产品界面截图**为主，配教师/课堂实拍；插画占比低。
- **定位差异**：主打「**在你已有的工具里工作**」（Google Classroom / Canvas / YouTube 集成），
  首屏social proof 是 20,000+ 学区 + 教师头像证言。
- **合规前置**：FERPA / COPPA / SOC2 / GDPR 徽标在页面中部显要位置，另设 Privacy Center。

### 1.3 两者共有、且本项目缺的四个结构性做法

1. **合规徽标显要化**——两站都把隐私/合规做成可见的视觉模块，而不是页脚小字。
2. **教师证言即社会证明**——用真人头像 + 姓名 + 学校，而非抽象数字。
3. **纵向叙事有次序**：定位 → 能力 → 分角色入口 → 证言 → 安全 → 生态 → 转化。
4. **暖底 + 单一强调色**：整站几乎只有一个强调色族在工作，其余靠中性与留白。

---

## 2. 与本项目现状的差距

本项目 V2 收敛后的现状（均为实测值，见 `tests/gate/type-baseline.json` 与 `color-baseline.json`）：

| 维度 | 鲁班 7 号现状 | MagicSchool | 差距判断 |
|---|---|---|---|
| 强调色 | 教师 `#4338CA` / 学生 `#2563EB`（双基调） | 单一紫族 `#33116F`+`#762BFA` | **不必对齐**——双基调是我们刻意的角色区分 |
| 底色 | `--bg #F7F8FC`（冷灰白） | `#FFFEF0`（暖奶油） | ✅ **可借鉴**：门户页换暖底，成本低、观感提升明显 |
| 字重 | 400 / 600 / 700 | 400 / 600 / 700 | ✅ 已一致 |
| 字号档 | 6 档，最大 **24px** | 12 档，最大 **56px** | ⚠️ **门户页需要展示级字号**；应用内不动 |
| 圆角 | `--r-ctl 12` / `--r-card 20` + 胶囊 | 8 / 16 + 胶囊 | 差异不大，**不建议改**（改动面波及全站） |
| 版心 | 无统一门户版心 | 1280 / 1216 | ✅ 需要为门户页定义 |
| 合规呈现 | 分散在免责行与 `data-safety-critical` | 独立视觉模块 | ✅ **可借鉴**，且与我们「安全第一」铁律天然契合 |

**关键取舍**：字阶门原本只覆盖六个应用页，**不含 `/login`**。**W1 第一步已补上**（见 §4 W1），
`/login` 首个基线实测如下——这是它第一次被度量：

| 项 | 实测值 | 判断 |
|---|---|---|
| 字号档 | **7 档**：11 / 12 / 13 / 14 / 24 / 40 / **51** px | 正好压在 §3.1 R3 的 ≤7 天花板上，**没有余量** |
| 圆角档 | **3 档**：20 / 32 / 40 px | 超出目标 2 档；应用页均已收敛到 2 档（12/20） |
| 字重 | 400 / 600 / 700 | ✅ 已达目标三档 |

**⚠️ 那个 51px 不是字阶档位，是算出来的一次性值**——`BrandLogo.tsx:85` 用内联
`style={{ fontSize: textSize }}`，随 `size` prop 参数化推导。它单独占掉一档，把 `/login`
顶到天花板。**W2 若要引入 56/40/28 展示字阶，必须先处置它，否则一上来就撞棘轮。**
同理 32px 圆角来自 `CardCarousel.tsx:154` 的 `rounded-[32px]`。

---

## 3. 可迁移的五条 / 不可迁移的三条

### ✅ 可迁移

| # | 做法 | 落点 | 预计工作量 |
|---|---|---|---|
| M1 | **暖底替换**：门户页 `--bg` 由冷灰白改暖奶油（`#FFFEF0` 邻近值，需过 AA 复核） | `/login` + 新门户页 | 0.5 天 |
| M2 | **展示字阶**：门户页专用 `--fs-display` 三档（56 / 40 / 28），与应用内 6 档隔离 | 新增 register 作用域 | 0.5 天 |
| M3 | **门户版心令牌** `--portal-max: 1280px` + 两侧留白规则 | 全门户页 | 0.5 天 |
| M4 | **合规可视化模块**：把 FERPA 类做法本地化为「数据存校内 / 不用于训练 / 未成年人保护」三徽标 | 门户页新区块 | 1 天 |
| M5 | **纵向叙事次序**：定位 → 能力 → 角色入口 → 证言 → 安全 → 转化 | 新门户页信息架构 | 含在 M4 |

### ❌ 不可迁移（迁了反而错）

| # | 他们的做法 | 为什么我们不做 |
|---|---|---|
| N1 | 教师真人证言 + 头像 + 学校名 | **我们没有真实证言**。编造证言违反本项目铁律②「不编造数据」。若无真实素材，此模块不建 |
| N2 | 「20,000+ 学区」量级数字 | 同上。校内平台没有这个量级，写了就是假的 |
| N3 | 单一强调色族 | 我们的双基调（教师靛蓝 / 学生钴蓝）承担**角色识别**，是功能不是装饰 |

---

## 4. 分阶段路线

每阶段末必须过：`tsc=0` · `eslint=0` · `gate` · `gate:color` · `gate:type` · `gate:render` · `visual` · 图门 `errors=[]`。

### W1 · 门户令牌层（0.5–1 天）

- **✅ 已完成：把 `/login` 纳入 `gate:type` 并锁首个基线**（7 档字号 / 3 档圆角 / 三档字重）。
  实现要点见下方「W1 已完成部分」。
- 待做：新增 `[data-register="portal"]` 作用域——`--bg` 暖底、`--portal-max`、展示字阶三档。
- **不动**应用内既有令牌，避免撞棘轮。
- 验收：暖底与文字对比度 ≥4.5:1（实算，不目测）；六个应用页令牌零变化。

#### W1 已完成部分：`/login` 入门禁的两个非显然点

**一、直接量会量出一个漂亮但空的数字。** `/login` 在 1440px 下 `showDesktopUnlock=true`
且 `isUnlocked=false`，登录卡片挂着 `lg:hidden` → `display:none` → 矩形为零 →
被 `AUDIT` 开头的 `rc.width <= 0` 整段跳过。**实测证据**（注入验证，把解锁点击换成
失效选择器）：不解锁时 `/login` 报「字号 6 / 圆角 2」，解锁后是「字号 7 / 圆角 3」——
丢掉的正是 24px 卡片标题与 40px 卡片圆角，**而门依旧全绿**。因此 `PUBLIC_PAGES`
先点开地球，并由 **T0「解锁后表单可见」** 断言兜底；T0 一红，T1/T2 的数字即无意义。

**二、门户页不装假时钟**（与 MATRIX 相反）。MATRIX 装 `clock.install` 是为避开宵禁遮罩，
而宵禁属登录后的 GuardianShell，门户页没有；反过来假时钟会冻结 rAF 与 setTimeout，
而解锁靠的是 React 状态更新后的重渲染——把调度器计时器换成不会自己走的假货，
风险远大于收益。此处也没有任何时间相关断言。

**顺带修掉一个会抹除历史的 bug**：`type-baseline.json` 的写回逻辑原本从零构造对象，
会把 `knownFragility`（记录 growth 页空态脆弱点那段）静默删掉——锁一次基线丢一次，
且不报错。已改为显式继承。

### W2 · `/login` 门户化改造 — ✅ 已完成

- 保留了现有 3D 星球链与抽卡轮播（**数十轮迭代的成果，未推倒**），
  外层挂 `data-register="portal"` 套用暖底与门户令牌。
- **新拟态阴影一并走暖**：原 `rgba(166,175,195,·)` 是为冷灰底调的，压在奶油底上会发脏。
  新拟态的说服力全在「卡片是从这块面料上压出来的」——阴影一旦不是底色的暗部，
  压印感立刻垮成「灰卡片浮在黄纸上」。
- 圆角 20/32/40 三档统一到 `--r-card`；`BrandLogo` 的 51px 孤值吸附到字阶。
- **实测结果**：`/login` 字号 **7 → 6 档**、圆角 **3 → 1 档**；
  裸 hex 全站 **51 → 41**、裸渐变 **4 → 3**（登录家族原占 43/51）。

### W3 · 新增公开门户页 `/portal` — ✅ 已完成

六个区块：吸顶导航 → 首屏定位 → 三角色入口（tab 切换）→ 能力概览 → **安全与隐私** → 结尾 CTA + 页脚。

- **不含**证言与量级数字（N1/N2），页脚也不放社交图标——我们没有这些账号。
- **一张位图都没生成**：装饰全部是 CSS 几何（棱镜分光）+ lucide 图标，
  因此 §6.4 的七项登记与 ≤150KB 体积门在本页上没有对象。
  途中把 lucide 的 `Bot`（带眼睛+天线的机器人脸）换成了 `Boxes`——它违反不拟人化。
- **三条对标站没有、我们加的**：首屏常驻安全条 / 「我们刻意不做的事」三条否定式边界 /
  危机热线在门户与页脚双处常驻且不可折叠。

### W4 · 回归与收口 — ✅ 已完成

全量门禁 + 视觉矩阵 + 图门 + 状态写穿，另外做了两件计划外但必要的事：

- **新增 `gate:portal` 门**（`tests/gate/portal.mjs`，26 条 × 三档视口），已并入 `npm run check`。
- **视觉矩阵扩到门户**：新增 `public` 分支（不登录、不装假时钟），
  检查项 **255 → 285**。它上线当轮就抓到 3 条真问题（见下）。

---

## 4bis. 本轮实际抓到的缺陷（不是计划，是过程产物）

路线本身只是计划。下面这些是执行中真正被门和审查抓出来的，价值高于计划本身：

| # | 缺陷 | 性质 |
|---|---|---|
| 1 | `globals.css` 全文件 **`@layer` 出现 0 次**，base reset 无层 → 通吃 Tailwind `@layer utilities`，全站 `border-<color>` 与 `<a>` 上的 `text-<color>` **静默失效** | **系统性**，非门户特有。已装进 `@layer base`，视觉门 255 项零回归 |
| 2 | 全站根字号 **14px**（非 16），Tailwind 全部 rem 标度 ×0.875：`text-xs`=10.5px、`h-10`=35px、`h-6`=21px | 既成事实，未改（改它等于全站重排版）。对策：触达与可读性下限**一律用绝对像素** |
| 3 | 「错题**自动归档**」——产品只有手录 | 编造功能，已改 |
| 4 | 求助条自称「学生端常驻」——实际藏在两级点击的模态里 | **在最高风险的一句上做不实承诺**，已改 |
| 5 | 硬编码 `v2.6.0`（package.json 是 0.1.0），门户页脚与登录页各一处 | 无出处即编造，已删 |
| 6 | `/portal` 首测 **13 档字号**——rem 档位与绝对像素两套标度并存（12.25 与 12、15.75 与 15） | 先收敛到 7 档再锁基线，否则把乱麻永久写进棘轮 |
| 7 | `BrandLogo` 的 51px：`Math.round(size*0.58)` 让连续函数输出直接落地 | 每出现一种新 size 就凭空多一档字号。已吸附到字阶 |
| 8 | `/login` 触控目标 `忘记密码?`(59×20)、轮播点(6×6) 低于 24px | 视觉矩阵扩容当轮抓到，已修 |

**两次误诊也记在这里**，因为它们比缺陷本身更容易重演：

- 从**构建产物**推断「`color-mix` 截断吞掉了后续声明」——错的，抓到的是过期 chunk。
  真因是 Turbopack CSS 陈旧构建。**令牌类问题一律以运行时 `getComputedStyle` 为准。**
- 测出页脚容器高 202px 判定为折行重叠——错的，选择器同时匹配到了外层区块。
  **量错了对象，不是布局错了。**

---

## 5. 后端边界与「如何接通」的记录

需求要求纯前端、但**需记录并思考如何接通对应后端**。逐项如下：

| 前端形态 | 现状 | 接通路径（若日后要做） |
|---|---|---|
| 登录 / 身份 | **已有真后端**：`/api/auth/login`、PBKDF2、会话 cookie、RBAC | 无需新增 |
| 表单（联系/订阅） | 门户页若加，先做纯前端状态机（加载/成功/失败/禁用/空） | 需新增 `notifications` 或独立 `leads` 表 + 频率限制 + 审计；校内平台是否需要订阅存疑 |
| 搜索 | 应用内已有 ⌘K 聚合搜索（真后端） | 门户页搜索无必要 |
| 文件上传 | **明确 out-of-scope**（方案 §8.2，暂记 V6） | 学生上传作业照片涉及人脸/姓名/住址，需存储+类型校验+内容安全门+配额+审计+保留期，须独立立项 |
| 支付 | 不适用 | 本项目为**非付费**校内平台（铁律③），永不接入 |
| CMS | 无 | 门户文案量小，直接入码即可；若日后需要，走 `prompts` 同款 DB 表模式 |

---

## 6. 生图资产纪律（沿用既有约束）

- 模型 `gpt-image-2`，网关 `https://api.llm-token.cn/v1`，密钥仅在 `.env.local`。
- 每张生成图**必须**在 `public/art/REGISTRY.md` 登记七项：授权链 / 已知 IP 近似 / 真人相似性 /
  年龄呈现 / 性别·肤色·民族刻板印象 / 宗教与政治符号 / AIGC 标识，外加该图主色（≤3）。
- 压 WebP **≤150KB**（G10 门禁强制）；每页装饰性栅格图 ≤2 张；插画不得承载唯一信息，须有文字等价物与 `alt`。
- **禁止**生成任何近似 MagicSchool / Brisk 品牌视觉的资产——那等于绕道做复刻。

---

## 7. 红线（不可交易）

沿用既有五条铁律：①未成年人安全第一（危机/care/诚信硬拦截先于模型，门户改版不得触碰）
②不编造数据（禁止假证言、假量级）③非付费 ④RBAC 服务端权威 ⑤折叠不得隐藏安全求助/危机/隐私信息。

新增一条：**⑥ 不复刻第三方品牌资产**——参考设计语言可以，搬运品牌元素不可以。

---

## 8. 已知风险与取舍

| 风险 | 处置 |
|---|---|
| 暖底可能与现有双基调强调色冲突 | W1 先做对比度实算 + 双角色截图对照，不达标就退回冷底 |
| 新增 `/portal` 会增加一个未被现有门禁覆盖的页面 | W1 就把它与 `/login` 一起纳入 type/visual 矩阵 |
| 展示字阶可能被误用到应用内，撑破 6 档棘轮 | 令牌限定在 `[data-register="portal"]` 作用域内；棘轮本身会拦住越界 |
| 3D 星球链在暖底下观感未知 | W2 第一步就出对照截图，不行则给星球链单独保留深底容器 |

---

## 附：本文档的证据分级

- **一手实测**：MagicSchool 全部色值/字体/字号/字重/圆角/版心/页高（Playwright 计算样式，1440px）；
  本项目 `.env.local` 配置、`clone-website` skill 缺失、现有门禁覆盖范围。
- **页面抓取**：两站的区块次序、定位话术、媒体构成。
- **推断（已标注）**：§0.3 关于「3/4 条应为否定」的判断；MagicSchool 字体的角色分工。

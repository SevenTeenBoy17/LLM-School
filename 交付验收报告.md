# EduAI Prism「鲁班 7 号」· 交付验收报告

> K-12 校内 AI 平台 · 2026-07-25  
> 本报告汇总当前交付物、**真实验证证据**、诚实禁用清单、已知限制与运维说明。所有数字均来自实际运行输出，非估算。

---

## 一、交付内容

### 1.1 角色化产品结构（本轮核心）

| 角色 | 落地形态 |
|---|---|
| **教师** | 侧栏「教学 \| 教研」双板块 + 顶栏板块快切；教学＝今日教学/班级学情/AI 对话/提示词/知识库；教研＝教研中心/课题与论文/集体备课；「更多工具」组＝模型广场/智能体工作台（**生图工作台为模型广场子页 `/hub/image`，非侧栏条目**） |
| **教研（researcher）** | 同教师但默认落 `/research`，教研板块默认展开 |
| **学生** | 独立 `/student/*` 命名空间，顶层**锁死 4 入口**（首页/AI 对话/成长/AI 工具）；无教师板块、无侧栏长导航 |
| **管理员** | 共 6 个管理页、**两套导航并存**：侧栏「管理与监控」4 项（数据看板 / 安全审计 / 权限管理 / **守护设置**）；管理页内 AdminTabs 另含 模型管理 / 智能体监控（该 tab 组不含守护设置）——导航统一为后续优化项 |

### 1.2 学生端能力（7 项）

对话 · 生图 · 总结 · **苏格拉底引导三态** · 错题本（诊断+处方） · 学习行为纠正 · 行为统计。

### 1.3 后端能力（22 张表 / 39 个 API 路由）

- **表**：users / students / pending / tickets / integrity / audit / chat_sessions / chat_messages / favorites / feedback / prompts / agents / notifications / user_prefs / kb_files / model_settings / role_perms / chat_usage / image_jobs / mistakes / class_policy / guardian_settings
- **关键链路**：对话（含 SSE 流式）· 生图（异步任务）· 错题 · 守护策略 · 班级策略 · 知识库关键词检索 · 审计 · 配额

### 1.4 安全与合规体系

1. **危机/关怀硬拦截先于一切治理门**——配额、限流、参数校验都不得拦截危机响应（含 12356 热线）
2. **学术诚信** scaffold + 语义复核（fail-closed）
3. **生图安全门先于外部 API**：中英双语危机词表 + 6 类黑名单 + 归一化抗绕过
4. **隐私最小化**：错题聚类双阈值 k-匿名（行≥3 且独立学生≥2）+ classId 圈定 + 受控知识点词表；送外部模型前 PII 打码
5. **未成年守护**：用时提醒 / 强制休息 / 宵禁（班级策略优先于全校），**安全求助入口在任何时段可见可点**（z-index 契约 100/110 > 守护层 85/90）
6. **RBAC 服务端权威**：proxy 守卫 + 路由层判定 + 属权校验，客户端 store 仅作非权威镜像

---

## 二、验证证据（真实运行输出）

### 2.1 发布门 T7：**18 PASS / 0 FAIL**

| 维度 | 结果 |
|---|---|
| RBAC 矩阵 | 22 路由 × 4 角色 **22/22** |
| 安全对抗 | 6/6（文本危机 · 伪造 assistant 历史越狱 · 英文生图危机 · 插空黑名单绕过 · 诚信 scaffold · 正常放行不误杀） |
| 内容质量 | 3/3（`source=remote` 验真 · 无 `<think>` 泄漏 · 统计为服务端聚合） |
| 能力回归 | M1-M5 + BL2 边界 **8/8**（1 RBAC + 6 安全 + 3 质量 + 8 能力 = 18） |

### 2.2 长周期浸泡（3h / 575 轮）

```
rounds=575  ok=130  degraded=0  quota=477  safety=573  errors=182
p50=17ms  p95=15344ms  max=41528ms
```
- **危机回归 0 失败**：`safety=573` 是三类安全探针的合计（关怀 / 诚信 / 危机各约 191 次）；其中**危机探针约 191 次，每次都返回 `kind:crisis` 且含 12356 热线**，零失败
- **降级 0**（无一次本地兜底）
- 182 条异常中真实缺陷仅 1 条（已修，见 2.4）；其余 181 条是**测试脚本未 quota-aware** 导致——教师/学生模型配额被脚本自身打满后，常规生成按设计返回 `kind:quota`，被旧断言误判为失败（脚本已修，配额门本身行为正确）

### 2.3 静态与结构门

- `tsc --noEmit` = 0、`eslint` = 0（每阶段执行）
- code-review-graph：**192 文件 / 905 节点 / 8787 边，errors=[]**
- 单测：宵禁边界 9/9 · 学科分类器 12/12 · 流式 think 剥离 9/9 · 受控词表 5/5

### 2.4 本轮（B/T 路线图 + 门禁阶段）修复的 P1（6 个，全部经复测）

> 说明：这 6 条是**本轮阶段**的 P1。项目自内测以来累计修复的 P1 约 19 条（含 F 批 [10b]/[11a]/[3a]、对抗审查 R1/R4、U-1、S5 终审 R-2~R-7、M2 审查 R2-1 等），完整台账见《内测问题清单·滚动汇总》。

| # | 问题 | 修复 |
|---|---|---|
| 1 | 诚信分类器漏「读后感 + 直接交」（整篇代做+提交意图） | 补体裁名词 + 双信号正则；拦截 3/3、不误伤 2/2 |
| 2 | 学生可 URL 直达 `/dashboard`、`/hub`、`/agent` | proxy default-deny；4/4 学生 307 |
| 3 | 角色 UI 依赖 client store，SSR 默认 teacher → **学生永久教师态**（缺监督横幅与引导开关） | SessionRoleProvider 注入服务端角色；SSR 首帧即正确 |
| 4 | 流式 `<think>` 二次泄漏（正文中途再次思考） | 可重入状态机；单测 9/9、真实流式 6/6 clean |
| 5 | beacon 默认密钥进客户端 bundle、非常时比较 | 迁 server-only 模块 + 生产 fail-closed + 常时比较 |
| 6 | 伪造 assistant history 绕过危机/诚信初筛 | probe 扩至全角色内容；复测 crisis 命中 |

---

## 三、诚实禁用清单（**当前未开通，界面已明确标注**）

| 能力 | 标注位置 | 未开通原因 |
|---|---|---|
| 联网搜索 | chat 顶栏「联网 · 未开通」 | 校内未开通外网检索 |
| 2FA / 异地登录提醒 | 个人中心 · 安全 | 需短信/邮件基建 |
| API Token 签发/重置/撤销 | 个人中心 · API | 需密钥签发存储基建 |
| 设备远程下线 | 个人中心 · 安全 | 需服务端设备会话管理 |
| 文献库检索 | 课题与论文 | 需图书馆/知网机构账号 |
| 实时协同编辑 | 集体备课 | 需协同服务 |
| 教研数据看板/课题进度 | 教研中心 | 需教务系统接入 |
| 向量语义检索 | 知识库 | 现为**关键词检索**，已明确标注 |
| 智能体异常事件「查看历史」、权限「批量导入」、模型「全局配额规则/接入新模型」 | 管理端各页 | 相应基建未开通（逐项均有 disabled 控件与 title 说明） |
| 部分管理端列表 | 标注「示例数据/样例数据」 | 真实数据源未接入，绝不冒充 |

> 原则：**宁可诚实禁用，绝不用假 toast 或示例数据冒充真实功能**。

---

## 四、已知限制与风险

1. **模型内容仍可能超纲或出事实错误（对 K-12 最直接的风险，教师须复核）**：内测台账实证——初中生物光合作用超纲 ×5（F7 提示词缓解后 claude 仍约 50% 超纲）、《背影》背影次数枚举错、1917→1925 被表述为「二十多年」、季风降水数据偏高。系统提示词已强制「学段不超纲/不编造引用与数据」，但**这是缓解而非根治**；产出用于教学前需教师核对
2. **诚信语义复核依赖外部网关（fail-closed）**：网关不可用/超时/不可解析，或额度（50 次/日、6 次/60s）耗尽时，一律维持 scaffold——即「学生正当求助被误拦」的 [3a] 场景会在网关异常时复现（5h 浸泡记录 2 次）。方向安全，但会牺牲可用性
3. **流式单次总时长上限 180s**：浸泡实测最长单次 313s，说明确有请求会被上限截断；已流出内容保留并如实收尾为 `remote`，但长产出可能不完整
4. **守护的宵禁与休息拦截完全在客户端判定**：`GuardianShell` 用本机时间计算宵禁、localStorage 累计用时。学生改本机时钟、停用 JS 或直接调 API 均可绕过——**当前仅为引导性防沉迷，不是强制管控**；服务端计时与拦截为后续项
5. **生图耗时 55-70s**（网关侧特性）：采用异步任务 + 画廊 UX，不做同步等待
6. **学科标注为关键词分类器**：识别不出的提问如实归「未分类」，不猜
7. **知识库为关键词检索**：非向量语义，已在界面标注
8. **管理端两套导航并存**（侧栏 4 项 / AdminTabs 含模型管理与智能体监控），统一为后续优化项
9. **大页面组件未拆分**（chat 799 行等）：有状态容器，拆分需跨流式/表单核心路径穿线，评估为高风险零收益，建议单页一轮 + 专属回归再做
10. **测试面伪象（判定时先排除）**：内置浏览器 pane 渲染冻结、fetch 直登不置前端 store、dev 缓存坏态（`rm -rf .next` 重启根治）、浸泡脚本配额耗尽噪声

---

## 五、运维说明

### 5.1 环境变量（`app/.env.local`，已 gitignore）

| 变量 | 说明 |
|---|---|
| `LLM_API_KEY` | 大模型网关密钥。**真实密钥只写 `app/.env.local`（已 gitignore），绝不入库**；`.env.example` 仅保留键名占位（值为空） |
| `LLM_BASE_URL` | 网关地址（主 `https://api.llm-token.cn/v1`，备 `gpt-agent.cc/v1`） |
| `EDUAI_SESSION_SECRET` | 会话 cookie HMAC 密钥 |
| `EDUAI_BEACON_SECRET` | 越权审计 beacon 内部密钥（**生产必须设置**，否则 beacon fail-closed 停用） |
| `EDUAI_DB_DIR` | SQLite 目录（默认 `.data`） |
| `EDUAI_ENABLE_DEMO_SEED` | 演示数据开关（生产应为 false） |
| `EDUAI_CHAT_MODEL` | 可选：默认对话模型代码 |

### 5.2 启动与账号

```bash
npm run dev
```

演示账号（口令规则：角色名首字母大写@123）：`teacher/Teacher@123` · `student/Student@123` · `admin/Admin@123` · `research/Research@123`

### 5.3 测试执行

测试脚本位于会话 scratchpad（建议迁入 `app/tests/`）：`t7-gate.mjs`（发布门）· `soak-m.mjs`（浸泡）· `think-stream-cases.mjs` / 宵禁 / 分类器等单测。

**铁律**：改 `db.ts` schema 后**必须重启 dev-server**（HMR 不重建表）。

---

## 六、后续建议方向（均为新能力，需先确认需求）

1. **守护服务端化（判定+计时）**：把宵禁/休息拦截移到服务端（配合会话级用时累计），根治第四章第 4 条的绕过缺口——**优先级最高，因它关乎防沉迷的实际效力**
2. **学段路由与内容复核**：按学段选更守边界的模型 + 关键学科产出加「教师复核」提示，缓解第四章第 1 条
3. **教务系统对接**：真实课表、班级名册、作业同步——可解锁教研看板与「正式课表」等当前诚实禁用项
4. **知识库向量检索**：替换关键词检索，提升 grounding 命中率
5. **家长端只读视图**：呼应「监督数据流向成人端」的设计惯例
6. **管理端导航统一** + **大页面组件拆分**（单页一轮 + 专属回归）

---

*相关文档：《前端升级优化·角色化重构方案路线书》《后端开发与真实测试·完整路线文档》《内测问题清单·滚动汇总》。*

# 学生工作区重构 · 设计规格书（W-D 阶段产出 · 2026-08-19）

> 依据：《学生工作区重构·调研总表与设计启示.md》（W-R，5 路真实检索 + 监督员审计 + 3 路补检索）。
> 界面骨架：Claude 网页版三段式；文案语法：讯飞星火双池制。
> 本文为 B1-B4 四期实施的唯一真相源；铁律与红线条目实施期不可绕过。

## 0. 全局红线（每期实施与验收都要对照）

| # | 红线 | 依据 |
|---|---|---|
| R1 | **禁排行榜/公开比较**：任何页面（含教师视图）不得出现积分/徽章/成绩的排序 | 教育部 2021《未成年人学校保护规定》+ Hanus & Fox 2015 实证 |
| R2 | **正确率唯一供数源=AI 随堂小测**（quiz_attempts，服务端确定性判分、客观题收口）；禁由错题量反推、禁教师点评混算、禁 LLM 自由判分入库 | 判据确定性纪律同构 |
| R3 | **AI 生成物双态标识**：浏览态角标 + 导出服务端合成水印/元数据；无任何去标识通道 | 《AI 生成合成内容标识办法》2025 |
| R4 | **庄园禁则**：无拟人角色（几何/植物/建筑语言）；禁互偷、禁随机开箱、禁限时焦虑机制、禁付费 | 铁律⑤ + 未成年人网络保护条例 + 2010 偷菜→摘菜整改先例 |
| R5 | **档案袋 AI 建议**：教师可见可改可撤 + 显式「AI 生成，供参考」标识 + 降级兜底 | 教育部生成式 AI 使用指南 |
| R6 | 任一数据维度无真实数据 → 显示「暂无数据」，禁编造均值/种子污染真实视图 | 不编造铁律 |
| R7 | 生图学段能力门在**服务端 policy**（小学=模板任务、初中受限、高中进阶） | 教育部 2025 指南 |
| R8 | 导航变更必须同步登录页 ENTRIES.student.note 措辞（逐字对齐规则）与 G12 单一真相源 | 既有门禁约定 |

## 1. 信息架构（壳层）

- **三段式**：左侧栏（可折叠）+ 主区 + 右侧产物栏（Artifacts 式，生成物统一走此栏：复制/下载/收藏入档案袋/版本切换）。
- **学生侧栏三组**（lib/nav.ts 学生组重构，触发 R8 同步）：
  - 学习工具：AI 对话（含文件上传）· AI 生图 · 思维导图 · 错题整理
  - 我的成长：课堂雷达 · 成长档案袋 · 数字徽章 · 个人庄园
  - 项目活动：学科项目（教师发布，学生可 pin）
- 底部个人卡：姓名 + 积分余额（庄园全局入口）。
- **文案双池制**（星火语法）：主标题「承诺，我是身份」句式；场景条四字动宾 tab；好奇问句池（≤15 字问号句，学段词表，轮播 placeholder）与任务祈使池（帮我/教我/整理开头 pill）分开维护，全部过既有安全门。

## 2. 数据模型增量（lib/server/db.ts）

| 表 | 关键列 | 服务模块 |
|---|---|---|
| uploads | id, userId, name, mime, sizeBytes, textContent, chars, status(parsed/failed/no_text_layer), createdAt | ①③⑤共用解析地基 |
| quiz_attempts | id, userId, subject, knowledgePoint, question, options, answer, studentAnswer, correct(0/1), ruleVersion, createdAt | ④正确率唯一供数源（R2，可复算） |
| evaluations | id, studentId, teacherId, tag(受控词表：专注/提问/互助/进步…), score(-1/0/1 或等第), note?, createdAt | ④教师流 ⑦档案袋 |
| activities | id, subject, title, brief, teacherId, dueAt, status | ⑥ |
| activity_submissions | id, activityId, studentId, content, artifactRef?, submittedAt, teacherFeedback? | ⑥⑦ |
| badges | id, key, name, tier(1-5), rule(json：真实事件条件), iconRef | ⑧（IP 图走生图 API+REGISTRY 登记+≤150KB） |
| user_badges | userId, badgeId, earnedAt, progress | ⑧ |
| points_ledger | id, userId, delta, reason(事件枚举), refId?, createdAt | ⑧⑨（只加不减负透支；无排序查询） |
| manor_state | userId, layout(json：地块×部件), updatedAt | ⑨ |
| manor_parts | id, key, name, cost, category(植物/建筑/景观/地形), svgRef | ⑨（纯几何 SVG 资产） |

## 3. API 增量

- POST /api/upload：multipart → mammoth(docx)/unpdf(pdf)/utf8(txt) 服务端抽取 → uploads 表缓存；返回 chars + 预览摘要 + 建议提问；扫描件无文字层显式报错（不硬编）。
- /api/chat：请求体新增 uploadIds[]，服务端拼接「基于文档X」上下文（走既有安全门与危机前置）。
- POST /api/mindmap：主题或 uploadId → LLM 受约束 Markdown（schema 校验：深度≤4、节点≤50、非法重试一次后回退）→ 返回 md；前端 markmap 渲染（新依赖 markmap-lib/markmap-view）。
- /api/quiz：GET 出题（学科+知识点，客观题）/POST 判分（服务端规则，存原始作答+规则版本）。
- /api/evaluations：教师 POST 快捷点评；GET 学生聚合。
- /api/activities + /api/activities/[id]/submit。
- GET /api/portfolio/summary?mode=simple|full：聚合 usage/quiz/mistakes/evaluations/badges → 雷达数据 + 档案区块；POST /api/portfolio/advice：真实调网关生成建议（source 标记 + 降级兜底 + R5 标识）。
- /api/badges（规则引擎：事件→授予）、/api/manor（GET 状态/POST 兑换与摆放，积分扣减在服务端事务内）。

## 4. 页面增量（app/(shell)/student/*）

- chat 增强（composer +「+」聚合、附件卡四态状态机、可预览抽取文本）——复用现有 /chat。
- tools 重组为「学习工具」聚合页（生图入口既有，新增思维导图卡）。
- growth 重构为「我的成长」：雷达图（SVG 五维：正确率/薄弱改善/学习参与/教师评价/知识覆盖，任一无数据显示暂无——R6）+ 错题整理（既有增强）。
- portfolio：简约版（Highlights 精选，Seesaw 映射）/ 详细版（全量时间线）双 tab；底部 AI 建议与计划卡（R5）。
- badges：徽章墙（五档稀有度，Khan 分层映射）+ 副本任务（周期任务，双轨：日常小任务+长线成就；无社交比较——R1）。
- manor：等距或平铺 2D 几何庄园（SVG 网格），部件商店（积分兑换），摆放编辑器；复杂度校准=「一次会话内能完成一次有感建设」。
- projects：学科分类活动列表 + 详情 + 提交。
- 教师端增量：快捷点评面板（班级学情页内嵌，低负担：点选学生+标签一击提交）+ 活动发布表单。

## 5. 分期蓝图与验收

| 期 | 内容 | 验收（每期必须全过再进下期） |
|---|---|---|
| B1 | 壳层三组导航 + R8 同步 + /api/upload 解析地基 + chat 附件全链 | tsc/eslint；真实 docx+pdf 上传→抽取→AI 引用回答 e2e；扫描件报错路径；npm run check |
| B2 | quiz 小测 + 雷达图 + 教师快捷点评 + 思维导图 | 判分可复算注入测试；雷达无数据态；导图 schema 越界回退；门禁 |
| B3 | activities + portfolio 双版 + AI 建议 | 提交闭环 e2e；聚合口径抽查；建议降级兜底与标识；门禁 |
| B4 | badges/points/manor + 生图 IP 资产（REGISTRY 登记） | 授予规则注入测试（伪事件不触发）；兑换事务负余额拒绝；资产≤150KB；门禁 |
| W-V | 全链门禁 + e2e 矩阵 + 交付总表 | PIPESTATUS=0 + 检索/实施双总表 |

## 6. 逐模块设计启示（W-R 综合原文）

## 设计启示纪要（逐模块可进规格书的决定建议）

### ⓪ 通用壳层（先于九模块的三条前置决定）
1. 三段式布局定案：左侧栏（可折叠）+主区+右侧产物栏；②③④⑤生成物统一走 Artifacts 式右栏（控件收敛为复制/下载/收藏入档案袋+版本切换），汇入侧栏「我的作品」集合页——复用本仓已有 chat 页，不为每模块造独立画布。
2. 侧栏三组导航：学习工具（①②③⑤）/成长（④⑦⑧⑨）/项目（⑥可 pin）；底部个人卡带积分余额，为⑨提供全局可见入口。校内 SSO 一次登录全免墙，九模块全内嵌，模型选择器替换为「学科」选择。
3. 文案双池制：主标题「承诺，我是身份」句式；场景条四字动宾；好奇问句池（≤15字、问号结尾、学段词表、轮播进 placeholder）与任务祈使池（帮我/教我/整理开头 pill）分开维护，全部过既有安全门审核。
4. 【数据诚实性约束·禁排行榜（全局铁律）】任何页面——含管理端教师视图——不出现成绩/积分/徽章的排序或公开比较。双重依据：教育部 2021《未成年人学校保护规定》禁公开成绩排名（合规红线）；Hanus & Fox 2015 十六周对照实验证明排行榜+徽章经社会比较反而压低动机与成绩（实证依据）。

### ① AI 对话（word/pdf 上传解析）
1. composer 单「+」聚合+全窗口拖拽；白名单 .docx/.pdf/.txt，单文件≤20MB、每消息≤3个，限制文案前置分级写明，超限明确报错。
2. 附件卡四态状态机（上传中%→解析中→就绪→失败）；失败原因在卡片层枚举：过大/加密/扫描件无文字层/格式不支持——禁止只靠模型回复「打不开」。
3. 【数据诚实性约束】附件卡点开可预览服务端实际抽取的文本，学生看到 AI 读到了什么；回答标注「基于文档X」；扫描件显式报「无文字层」，严禁让模型硬编内容；复杂表格附「可能失真」免责。
4. 解析成功显示「已读取 N 字」+2-3 个建议提问 chip，不把全文塞进消息流。
5. 文档解析建为独立服务端能力（上传→抽取→缓存文本），供①③⑤三模块复用，一期先落此地基。

### ② AI 生图
1. 入口=「创作智能体卡片」非裸文本框；面板四件套：描述输入（带「勿填真实姓名/学校」提示+服务端检测阻断）→风格 chips（8-12 个课堂友好风格+智能推荐默认）→画幅三选→数量 1-4 张。
2. 学段能力门做在服务端 policy：小学段仅教师发起的模板任务内填空（教育部 2025 指南硬约束），初中受限自由创作，高中开进阶项。
3. 双门审核 fail-closed：prompt 前置审核+生成图像输出端二审（南都实证：无引导文案也能产出擦边图，仅关键词过滤必失守）；「儿童/学生」主题词触发加严档（仅卡通非写实）；学生端禁写实人像与图生图。
4. 【数据诚实性约束】双态双标识：浏览态即带「AI生成」角标（不学即梦「下载后才显示」）；导出由服务端合成水印入文件+写元数据隐式标识；永不提供任何关闭/付费去标识通道；生成日志留存≥6个月。
5. 拦截话术教育化：说明违反类别、不复述违规词、给换主意建议+求助教师/申诉入口；高频触发→通知教师+临时降配额而非封号；「公开到班级画廊」走机审+教师终审异步队列。

### ③ 思维导图
1. 双入口：输主题生成/从已上传文档生成（复用①解析文本）；生成前给 2-3 个结构预设，不做模板库。
2. 技术路径定案：LLM 输出受约束 Markdown→markmap-lib/view 渲 D3 交互 SVG，零自研布局引擎；LLM 输出必须 schema 校验（深度≤4、节点≤50、非法回退重试），否则渲染爆炸或空树。
3. 最小交互四件：节点折叠/缩放平移/导出 PNG+Markdown/重新生成；节点级 AI 扩展留二期。
4. Markdown 作中间交换格式，直通导入/导出与档案袋归档。
5. 【数据诚实性约束】导图产物常驻「AI生成」标识，导出文件含标识元数据（《标识办法》口径）。

### ④ 课堂雷达图
1. 【数据诚实性约束·真实数据源裁定（核心）】四维=四条独立真实数据流：答题正确率←新增「AI随堂小测」quiz_attempts 流（结构化作答控件、服务端确定性判分、客观题收口），这是唯一供数源；薄弱知识点←错题模块 knowledgePoint 受控词表聚类+小测错题自动回流入错题本；学习参与←平台自然行为流（对话轮次/任务完成/复习动作）；教师评价←W-B2 结构化点评流（点评项+时间戳+操作者）。三条禁令：禁由错题量反推正确率（只有分子无分母=口径造假，且惩罚诚实使用）；禁教师点评与正确率混算成一个数（班优大师的「课堂报告」实为行为点评报告）；禁用 LLM 自由评价当判分器（主观题走 AI 讲解+教师复核，不入正确率）。
2. 判分可复算：存学生原始作答+标准答案+判分规则版本——与本仓门禁哲学「判据确定性」同构。
3. 任一维度无数据显示「暂无数据」，不编造均值；接真流时沿用演示身份服务端约束，防 class 页现有种子数据（avgMastery/weakest）污染真实雷达。
4. 呈现：4 维同量纲归一，画像口径=一段时间汇总非单节课；每维可下钻到证据明细（哪次课、哪道题）；给班级均值虚线参照，但学生端不做同学间排名【禁排行榜】。
5. 不做摄像头行为/表情/专注度识别（未成年人隐私伦理红线）；数据源限定教师点评+作答记录。

### ⑤ 错题整理
1. 收录贴讲解发生：AI 讲解/批改场景内一键「加入错题本」，不另开录入页；手动录入兜底。
2. 两层组织：学科 tab+预置错因标签 3 类（计算错误/概念模糊/审题不清）+知识点字段（受控词表归一，杜绝自由文本注入 PII）。
3. 再练流程：隐藏原答案重做→即时判定→推 3 道 AI 变式题，变式题显式标注「AI 生成，仅供练习」。
4. 【数据诚实性约束】薄弱点=知识点粒度错误频次×近期性加权，同一数据源喂④雷达与⑦成长报告（一处口径、两处消费）；聚合展示沿用既有 distinct 阈值/聚类圈定；可加「订正后二次正确率」作恢复性指标。
5. 错题数据属未成年人敏感数据：不进任何排名或同伴可见面；复习防刷标记沿用现有实现。

### ⑥ 学科分类项目活动
1. 闭环主链定型：教师从预置分学科模板库/自建选题→发布对话框（3 类响应/指派全班-分组-个人/起止时间/学科+知识点标签）→学生任务队列草稿箱作答→提交进教师批阅队列（一屏总览）→通过自动入档案袋（带教师评语+学科标签），退回附理由→学期末精选/全量双视图+可打印报告。
2. 组织：「学科」为活动库主导航面、学段/类型为筛选面；五育维度仅作档案袋标签不做目录结构，防空分类。
3. 【数据诚实性约束】学生自报不可直达档案袋——必须过教师审核状态机（草稿→提交→通过/退回→入册）；录入双通道分权：系统客观数据（对话统计/雷达/完成数据）自动导入不可编辑，学生主观填报走教师审——与本仓「控件不说谎」同构。每个已发布活动必有按学生完成状态面板（未开始/进行中/已提交/已批+完成率/用时），堵「有门没人跑」（V11 门禁教训的业务层重演）。
4. 教师低负担录入：预设标签一步点选（图标+名称+分值 -5~+5）+0 分纯记录+个人/小组/全班三层级批量；佐证材料按记录类型分级强制（获奖类必传、普通类建议）；每周限额防灌水。
5. 不照搬广西式「全员公开+点赞质疑」：可见域收敛为本人+任课教师，真实性靠教师审核+3 天类撤回时限而非同伴监督；希沃式课中游戏模板仅作素材，不承载档案。

### ⑦ 个人成长档案袋
1. 双版=两种产物而非同页折叠：详细版=全量写实记录流水（媒体+学生反思+素养标签，按学科/维度分夹，Seesaw journal/综评「写实记录」）；简约版=遴选产物，固定四栏「主要成长记录/学生自述/教师评语/典型事实材料」，每维度精选≤6条（综评「整理遴选」的制度学名）。
2. 允许留白（广东综评明文「避免面面俱到」）：空栏目显示鼓励性占位而非红色警告；学期末「确认→锁定→版本化」，锁定后只能追加不能改。
3. 【数据诚实性约束·AI 建议边界】AI 成长建议/计划绝不自动写入档案、绝不以平台口吻下结论（教育部 2025 指南：教师不得直接用 AI 生成内容评价学生）。流程=AI 草稿（教师工作台草稿区，标「待老师确认」）→教师审改→确认发布后才对学生/家长可见；署名双归因「AI 辅助生成·X老师已确认」；语气建议式非判定式（写「可以多尝试口头表达类任务」，不写「表达能力差」）；卡片底部常驻「以上建议由 AI 生成，仅供参考，请结合老师意见使用」；AI 可辅助遴选，不可代填事实材料。
4. 可见域：家长/展示视角只见简约版+已发布条目，工作区草稿不可见；导出/对外共享前须学生确认步骤。
5. 游戏化产物入档必须回答「这代表我学会了什么」：徽章/庄园条目携带描述性能力反馈，不是孤立图标。

### ⑧ 数字徽章与副本任务
1. 【数据诚实性约束·授予绑定真实事件】每枚徽章判据=可追溯的真实平台事件流（小测 attempts/错题订正/项目提交入册/档案条目/教师点评），口径为个人绝对量（Khan 式「能量点=努力/掌握度」），绝不用相对位次；徽章详情页可展开「凭什么获得」的事件清单——判据确定性延伸到激励层。
2. 分层结构：4-5 层稀有度金字塔（天体隐喻契合现有轨道视觉），顶层刻意稀缺做长线目标；学科线另设「完成补丁」系列对应⑥项目收尾；各行为线按动森「10/100/500次」阶梯铺设，首档开箱即中。
3. 任务三层节奏：日任务≤3条（锚定学习产出非登录时长）、周协作副本（Friends Quest 模式：小组共享一条进度条，不显示个人贡献占比与排序）、月度剧情副本（Habitica quest line 章节化，产出收藏徽章+⑨庄园建材）；班级 boss 战可做（周学习目标拟物化血条），删除连坐掉血——只有共同输出，没有集体惩罚。
4. 【数据诚实性约束·禁排行榜】连管理端也不提供班内积分排序视图；以协作共进度条整体替换班优大师光荣榜/小组 PK 范式（国内产品最易被无意识照搬的坑）。
5. 伦理边界包：无负分扣分，纠正性反馈私密且与积分解耦（ClassDojo 零分模式）；连胜若保留必须带冻结/补签+断连静默恢复+禁愧疚拟人文案；不做延迟发奖/夜间召回/联赛升降；设「今日已完成」停止点+休息提醒；内容日历按 4-6 周投放新章节对抗新奇效应衰减，低谷期靠教师运营与意义绑定而非加码奖励；对学生已自发喜爱的行为不叠加积分（防动机挤出）。

### ⑨ 个人庄园
1. 主循环定型：学习→积分→兑换零部件→布置庄园→展示/班级共建；产分全部挂既有学习动线末端（订正完成/档案更新/项目提交自动产分），不新增独立打卡负担、不挂在线时长（《未成年人网络保护条例》禁诱导沉迷）。
2. 数值基准（可直接进规格书）：每分可解释（「订正1道错题=+10」）+每类行为日上限防刷；日常产出/最小件价=0.5~1，中档部件 2-3 天、地标建筑 2-4 周对齐学期节点（月考后/期末）；集齐当日小任务翻倍；积分可花/徽章不可花分轨，高级部件「积分+徽章」双门槛使荣誉不被消费掉；大件挂真实语义锚点（蚂蚁森林式）。
3. 【数据诚实性约束·铁律合规变体（负面清单）】(a) 禁任何拿取他人资产机制——2009 偷菜监管史红线，同伴互动只做正和博弈：帮浇水、赠贺卡、合建公共建筑、参观点赞（无评分无比较）；(b) 禁概率抽卡/保底/任何付费点——兑换一律明码标价确定性获得，变异部件仅纯展示、零付费、不可交易的种植彩蛋；(c) 禁限时焦虑——无倒计时、作物不枯死、建筑不降级、离线不惩罚，过期积分转入班级公共池而非清零；(d) 禁排行榜——庄园可互相参观但无任何名次呈现。
4. 复杂度三层封顶（Bushnell 定律操作化）：种/收单循环 1 分钟能懂（首次进入即赠地块+3 件部件，禁空地块「先打工」冷启动）→部件颜色/形状变体组合布置提供数周深度→班级共建（全班积分点亮图书角/花坛）提供学期尺度公共意义；明确不做合成/耐久/能量条/繁育。
5. 画风与实现：Monument Valley 式纯几何语言（圆/方/三角/梯形，低饱和暖色，复用现有 tokens），无拟人吉祥物——取摩尔庄园机制、换其形象语言，蚂蚁森林已证明纯几何+真实意义可承载长线情感；实现走 2D SVG symbol 部件库+CSS grid 地块+CSS transform，不引入 canvas/游戏引擎，伪等距 2:1 留二期；体验验收项：红点全站≤1处、奖励一键领取、维护操作一键化、季节主题按 4-6 周版本化投放。



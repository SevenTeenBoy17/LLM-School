# 登录门户重构 · 暗夜穹顶方案与技术文档

> 参考稿：一张「NexusMind」风格的暗色着陆页（浮动胶囊导航 + 点阵大标题 + 穹顶光弧 + 双 CTA + 底部单色条）。
> 重构对象：`app/app/login/page.tsx`（当前 464 行的「校园画卷 + 3D 地球解锁」双栏方案）。
> 本文档 = 深度拆解 + 内容映射 + 视觉规格 + 技术实现 + 门禁影响评估 + 实施计划。

---

## 0. 复刻边界（先说清，避免返工）

**全量复刻的**：布局语法（居中单栏 hero）、区块次序、字阶节奏、点阵标题的视觉处理、
穹顶光弧的氛围、浮动胶囊导航形态、双 CTA 权重关系、底部单色横条的结构。

**必须原创替代的三样，各有硬理由**：

| 参考稿元素 | 为什么不能照搬 | 替代方案 |
|---|---|---|
| 背景实拍/绘画艺术图（山谷+穹顶） | 他人作品；且位图不符合本项目 ≤150KB + 纯几何资产纪律 | **纯 CSS/SVG 分层重建**同款氛围（§5.1），0 位图 |
| 底部真实公司 logo 墙（Tesla/NASA/Google…） | 放真实公司商标 = 伪造背书，直撞铁律② | 复用 `FactBadges` 的 5 条**可核对事实**做单色横条（§3），结构同构、内容诚实 |
| 中央人物剪影 | 铁律⑤纯几何、无具象人物 | 直接删除；穹顶+地形已足够承载「远望」的空间感 |

**顺带清偿的两笔旧债**：现有登录页轮播位图里烤着「12,840 文档已索引」「48 个智能体在线」
两个未经实测的数字（铁律②，藏在图片里所以 G11 扫不到）；轮播里的拟人吉祥物（书本/机器人）
撞铁律⑤（对应被盲审门卡住的任务 #73）。本次重构整体移除这批资产的使用。

---

## 1. 参考稿深度拆解（按 1206px 宽度实测估值）

### 1.1 整体结构（自上而下）

```
┌─────────────────────────────────────────────────┐
│  ① 浮动胶囊导航（top≈40px，居中，h≈56px）        │
│                                                 │
│         ② 公告胶囊（双段式，h≈28px）             │
│         ③ 点阵大标题 ×2 行（≈64–76px）           │
│         ④ 副文案 ×2 行（≈15px，灰）              │
│         ⑤ 双 CTA（白色胶囊 + 幽灵文字）           │
│                                                 │
│      〔穹顶光弧：横贯全宽，圆心在视口下方〕        │
│      〔地平线暖光 → 山体剪影（下 1/3）〕          │
│  ⑥ 底部单色条（左标签 + 5 个单色项，y≈755）      │
└─────────────────────────────────────────────────┘
```

### 1.2 逐区块规格

**① 导航**：整体是一枚全圆角胶囊；背景 `rgba(255,255,255,.04)` + 1px 边框
`rgba(255,255,255,.08)`，疑似 backdrop-blur。左 logo（图标+字标），中部 4 个链接
以「·」分隔（≈13px、宽字距、mono 质感），右侧「Request a Demo」次级胶囊按钮
（透明底+亮边框）。**要点：导航不贴边，是悬浮在画面上的一个物体。**

**② 公告胶囊**：双段式——左段白底黑字小胶囊「Latest」，右段深底灰字
「Now available for MacOS」。整体 ≈28px 高，圆角全。作用是给标题上方一个
小的信息锚点，避免标题孤悬。

**③ 点阵大标题**：本稿的**签名元素**。两行，字形由密集圆点构成（LED/点阵屏质感），
估 64–76px；第一行「Where Humans and」中 and 降灰（#6b7280 档），第二行
「Agents Create」主白。**明暗交替是刻意的**：在超大字号里制造节奏，避免整块白。
字间距略宽。

**④ 副文案**：≈15px、`#9ca3af` 档灰、行高 ~1.65、两行、max-width ≈500px、居中。

**⑤ CTA 对**：主按钮白底黑字全圆角胶囊（h≈44px、横向 padding≈24px）；
次按钮纯文字灰色「Learn More」。**主次权重差拉到最大**（实底 vs 无底），
这与本项目门户「一屏一个主 CTA」的既有纪律一致。

**⑥ 底部条**：左侧小灰标签（≈12px）「Some of the companies…」，右侧 5 个
单色 logo 均布，约 60% 不透明度。功能上是「信任背书条」。

### 1.3 穹顶光弧的构成（从外向内）

1. **基底**：近黑（#05060a 档），四角轻微暗角。
2. **弧带群**：多条同心圆弧，冷色（青白）细线，越靠外越淡；带有细密的
   放射状纹理（长曝光星轨质感）。圆心位于视口底边之下、水平居中——
   所以画面里只见「穹顶」不见「全圆」。
3. **地平线暖光**：穹顶根部一条水平暖橙光带（#f2a65a 档），是全画面唯一的
   暖色，与冷色弧带形成温差——**这是氛围成立的关键一笔**。
4. **山体剪影**：2–3 层由远及近的深色山形，越近越黑，压住画面下缘。
5. **星点**：上半部稀疏小白点。

### 1.4 可迁移的设计判断（为什么它好看）

- **单焦点**：整页只有一个视觉事件（穹顶+标题同轴），没有第二个争夺注意力的区块。
- **温差**：99% 冷色 + 1% 暖色地平线，暖色因稀缺而贵。
- **字阶断层**：76px 标题 vs 15px 副文案，中间不设过渡字号——断层制造气势。
- **材质统一**：点阵标题的「颗粒」与星点、弧带纹理是同一种视觉语言。

---

## 2. 现状盘点（`app/login/page.tsx`，464 行）

| 现状 | 处置 | 依据 |
|---|---|---|
| 左栏校园画卷背景（含真实校名「成都市树德实验中学」的 AIGC 位图） | **移除使用** | 位图纪律 + 真实校名不宜出现在营销面 |
| `CardCarousel` 轮播（位图内烤有伪数字 12,840 / 48） | **移除使用** | 铁律②（数字未实测且藏在图里，门禁扫不到） |
| 轮播拟人吉祥物（书本/机器人） | **随轮播移除** | 铁律⑤纯几何；任务 #73 本就被盲审门卡住 |
| `Globe3DUnlock` 3D 地球解锁 + `Constellation` 星座背景 | **移除**（连带 `OrbitalSystem*`/`Sphere3D`/`PlanetGlyph` 按引用清点删除） | 新方案为单栏 hero，无解锁玩法；Globe3DUnlock 自带 10 个 hex，删除后色彩门净改善 |
| L16-30 `safeReturnPath` / `loginTargetFor`（from 参数安全回跳） | **原样保留** | 已经过评审的安全逻辑 |
| L34-37 表单状态 + L67-91 `onSubmit`（含 429 限流与错误文案） | **原样保留**（失败不清输入，符合 R1-top3 改造版） | 已验证的登录链路 |
| **L56 `clearClientIdentity()`** | **必须保留** | **G13 断言「登录页挂载时清客户端身份镜像」直接盯此文件，删了门立刻红** |
| L35 密码预填 `Teacher@123` | 保留但包 `isProd()` 判断（生产不预填） | 演示便利 vs 生产暴露 |
| `BrandLogo` | 保留，进新导航 | — |

---

## 3. 内容映射表（英文 → 中文原创，全部可核对）

| 参考稿位 | 参考稿内容 | 新稿内容 | 事实来源 |
|---|---|---|---|
| 导航链接 ×4 | Discover · Blog · Pricing · Features | 能力 · 三步上手 · 安全与隐私 · 常见问题 | 全部锚到 `/portal` 既有区块（`#how-to-start`、`#trust-ledger` 已确认存在；其余接线时以实际 id 核对） |
| 导航右按钮 | Request a Demo | **登录** | 打开登录卡（§6 状态机） |
| 公告胶囊 | Latest / Now available for MacOS | **校内部署** / 浏览器打开即用 · 不装 App | 部署事实 |
| 大标题 L1 | Where Humans and | **师生与 AI** | 原创（备选见下） |
| 大标题 L2 | Agents Create | **在校内一起创作**（「在校内」降灰，「一起创作」主白——复刻参考稿 and 的明暗节奏） | 原创 |
| 副文案 | The unified workspace for… | 教师用它备课、建智能体、看学情；学生用它提问、整理错题。对话与作业数据存放在校内，面向全校师生开放，不收费。 | 复用门户已审文案（与 Capabilities 配额口径一致） |
| 主 CTA | Start Building | **进入鲁班 7 号** | 触发登录卡 |
| 次 CTA | Learn More | **了解边界与承诺** | 链去 `/portal` |
| 底部条标签 | Some of the companies that use our product | **我们敢写下来的承诺** | — |
| 底部条内容 | 5 个真实公司 logo（**不可复刻**） | `FactBadges` 的 5 条：危机识别先于模型 · 数据存放于校内 · 不用于训练模型 · 不收取任何费用 · 没有排行榜 | `components/portal/FactBadges.tsx` L27-40，每条自带锚点与事实来源注释；抽公共数据到 `lib/data/portalFacts.ts` 双端复用 |

**标题备选**（供拍板，均为原创）：
- A `校园里的 AI / 先说清边界` —— 与门户 h1 同声道，最稳；但作为每日入口略冷。
- B（推荐）`师生与 AI / 在校内一起创作` —— 保留参考稿的能量，边界感交给副文案与底部承诺条。
- C `人与 AI 学伴 / 同校，同行` —— 最短，点阵渲染最大气，但信息量低。

---

## 4. 视觉系统规格

### 4.1 色彩令牌（新增，尽量 color-mix 派生，控制色彩门计数）

```css
/* globals.css · 登录暗夜域（scope: [data-register="login-hero"]） */
:root {
  --lh-bg: #05060a;                                            /* 唯一新 hex：基底近黑 */
  --lh-surface: rgba(255, 255, 255, 0.04);
  --lh-border: rgba(255, 255, 255, 0.10);
  --lh-text: #f5f7fa;                                          /* 新 hex ②：主白 */
  --lh-text-2: color-mix(in srgb, var(--lh-text) 62%, var(--lh-bg));   /* ≈#9aa3b2，对比 ≥7:1 */
  --lh-text-3: color-mix(in srgb, var(--lh-text) 44%, var(--lh-bg));   /* 12px 小字仍 ≥4.5:1 */
  --lh-arc: color-mix(in srgb, var(--c-cyan) 70%, white);      /* 弧带冷色：从既有令牌派生 */
  --lh-arc-glow: color-mix(in srgb, var(--c-cyan) 35%, transparent);
  --lh-horizon: color-mix(in srgb, var(--warn) 78%, white);    /* 地平线暖光：从 --warn 派生 */
}
```
新增裸 hex 仅 2 个；其余全部 color-mix 派生自既有语义令牌——色彩门（按文件计唯一色）
预计只 +2，若触基线则按纪律重录并写明理由。

### 4.2 字阶（/login 在 typography.mjs 棘轮射程内，改后必须重录基线）

| 元素 | 规格 | 备注 |
|---|---|---|
| H1 点阵标题 | `clamp(38px, 6.5vw, 76px)` · **700** · lh 1.12 · 字距 0.02em | 用 px+vw，不用 rem（免受 fontScale 干扰）；字重只许 400/600/700（门禁），点阵采样用 700 |
| 副文案 | 16px · 400 · lh 1.7 · max-width 520px | — |
| 导航链接 | 13px · 600 · 字距 0.08em | — |
| 公告胶囊 | 12px | — |
| 按钮 | 14px · 600 | — |
| 底部承诺条 | 12px · 400 · 66% 不透明度 | — |

### 4.3 间距与命中区（绝对像素，沿用 --hit 阶梯）

- 导航：`top: 32px`；导航项命中 `min-h-[var(--hit-min)]`（44px）；右侧登录按钮 44px。
- 纵向节奏：胶囊→H1 `24px`；H1→副文 `24px`；副文→CTA `32px`；hero 块整体垂直定位在视口 ~38% 高度。
- 主 CTA：`h-[var(--hit-row)]`（48px）胶囊；次 CTA 文字按钮也保证 44px 命中（透明扩展）。
- 底部条：`bottom: 40px`，flex-wrap（**严禁横向溢出**——visual.mjs 三断点盯 overflowX）。

---

## 5. 核心视觉技术方案

### 5.1 穹顶光弧 —— 纯 CSS/SVG 分层，零位图

层叠（自下而上，全部 `aria-hidden` + `pointer-events-none`；容器 `overflow: clip`）：

```
L0 基底        div  background: var(--lh-bg) + 四角 radial 暗角
L1 星点        inline SVG ~40 个 r=0.6-1.2 的 circle，上半区随机分布（构建时固定 seed，
               不用 Math.random 运行时生成——visual 截图需要可复现）
L2 弧带群      一个 220vw × 220vw 的圆形 div，定位使其顶弧出现在视口 ~52% 高度处：
               border-radius: 50%
               background:
                 repeating-radial-gradient(circle at center,
                   transparent 0 38px,
                   var(--lh-arc-glow) 38px 39px)        /* 细密同心纹理 */
               mask: radial-gradient(closest-side, transparent 55%, black 62%, black 78%, transparent 92%)
               外加 2-3 个独立圆环 div（1px border var(--lh-arc)，filter: blur(0.5-2px)，
               opacity 阶梯 0.5/0.28/0.14）做主弧
L3 地平线暖光  椭圆 radial-gradient(50% 12% at 50% 100%, var(--lh-horizon) → transparent)
               定位在穹顶根部；opacity 0.55
L4 山体剪影    inline SVG 两条 path（远山 #0b0e16 档 / 近山 var(--lh-bg) 全黑），
               波形手写 3-4 个起伏即可，viewBox 保持 preserveAspectRatio="none"
L5 顶部渐隐    linear-gradient(180deg, var(--lh-bg) 0%, transparent 18%) 压住导航区
```

动效（受 `prefers-reduced-motion` 与项目 `.reduce-motion` 双重豁免）：
- L2 纹理层 `rotate 360deg / 240s linear infinite`（慢到近乎不可察觉，只提供「活着」的质感）；
- L3 暖光 `opacity 0.45↔0.6 / 9s ease-in-out alternate`。
- **禁用 scroll-driven**（本页无滚动叙事需求，且 overflow: clip 容器与 view-timeline 有既知坑）。

### 5.2 点阵大标题 —— canvas 采样增强，真文本兜底

参考稿的点阵是拉丁字体特性；中文没有可免费商用的高质量点阵字体，且引入字体文件
违背轻资产纪律。**方案：运行时把现有字体的字形采样成圆点阵**——

```
组件 DotMatrixTitle（components/login/DotMatrixTitle.tsx）

DOM 结构：
  <h1>                    ← 真文本，始终在 DOM（SEO/读屏/选中复制全保留）
    <span>师生与 AI</span>
    <span>在校内一起创作</span>
  </h1>
  <canvas aria-hidden />   ← 视觉层，绝对定位覆盖 h1

流程：
 1. SSR/首帧：h1 以正常字形渲染（保 LCP，无 CLS）；
 2. 挂载后：offscreen canvas 以 2x DPR fillText 同字号同字重(700)绘制文本
    → getImageData → 以 step≈7px 网格采样 alpha
    → alpha>128 的格心画圆点（r = 1.2 + alpha/255 * 1.4，字缘自然变细）
    → 点色按 <span> 分段：第 1 行主白，第 2 行前三字 --lh-text-3、后四字主白；
 3. 200ms 交叉淡入 canvas，同时 h1 `color: transparent`
    （transparent 而非 hidden：读屏与选中不受影响、盒模型不变、零 CLS）；
 4. ResizeObserver（debounce 200ms）监听 h1 → 重采样（含 fontScale 改根字号的场景）；
 5. 降级：canvas 不可用 / prefers-reduced-data / .reduce-motion → 保持真文本，
    永远「最终态可读」。
```

成本评估：一次性采样 <8ms（两行 12 字 @2x），无持续 rAF；实现 ~120 行。

### 5.3 玻璃登录卡（复用现表单逻辑）

- 宽 400px（<480px 视口全宽减 32px）；`bg: rgba(10,12,20,.72)` + `backdrop-blur(14px)`
  + 1px var(--lh-border)（现有 `.glass` 是浅色域的，登录暗域内联写或加 `.glass-dark`）；
- 输入框高 **48px 绝对像素**、14px 字、深底浅字；错误文案沿用现有两种（429/凭证错误）；
- 提交按钮与主 CTA 同款白胶囊（视觉连续：点「进入鲁班 7 号」后出现的卡片里，
  最终动作还是同一枚白胶囊）；
- **失败不清空输入**（现逻辑已如此，保持）。

---

## 6. 交互状态机

```
view: "hero" | "form"

初始判定：URL 含 ?from=… → 直接 "form"
         （被 proxy 弹回来的用户要的是表单，不是营销页——safeReturnPath 逻辑不变）
         否则 → "hero"

hero → form：主 CTA / 导航「登录」触发
  hero 文字块 200ms 淡出+上移 8px（--ease-out）；登录卡 240ms 淡入+上移
  穹顶背景不动（背景连续性是转场廉价却高级的关键）
form → hero：卡片左上「← 返回」（44px 命中）或 Esc
挂载：clearClientIdentity()（G13 断言，不可动）
```

---

## 7. 响应式方案

| 断点 | 处理 |
|---|---|
| ≥1280 | 全规格 |
| 1024 | H1 走 clamp 中段；导航链接保留 |
| 768 | 导航中部链接隐藏（内容都在 /portal，经次 CTA 可达），保留 logo + 登录；穹顶放大到 260vw 保持弧率 |
| 390 | H1 38px；CTA 纵向堆叠、全宽、48px 高；底部承诺条 flex-wrap 两行；**逐断点核 overflowX=0** |

---

## 8. 合规与门禁核对单

| 门/铁律 | 影响 | 动作 |
|---|---|---|
| 铁律① | 登录页无安全披露义务（披露在 shell 与 portal），但次 CTA 通向承诺区 | 无风险 |
| 铁律② | 移除位图伪数字；新文案零数字；底部条全部可核对 | 净改善 |
| 铁律⑤ | 删除拟人轮播与人物剪影；新视觉纯几何 | 净改善 |
| G13 | `clearClientIdentity()` 保留在挂载 effect | **重写时重点自查** |
| typography.mjs | /login 在 MATRIX 内：字号档/字重/圆角变化 → `UPDATE_TYPE_BASELINE=1` 重录并写 note | 必做 |
| visual.mjs | 三断点 overflowX / 对比度 / 命中区 ≥40 / 首屏空白率 | 穹顶容器 `overflow: clip`；灰阶按 §4.1 对比度算过 |
| color.mjs | 新增裸 hex 仅 2（其余 color-mix 派生）；删 Globe3DUnlock 减 10 hex | 预计净减；触基线则重录 |
| G5 z 阶梯 | hero 层间用已登记的 z-0/10/20；无新 z 值 | 无风险 |
| G11 | 无样条/无趋势字段/无 aria 结论；注释不复述被删代码的标识符 | 写码时自查 |
| CSS 注释门 | 不在注释里写「星号+斜杠」组合 | 已有断言兜底 |
| G10 资产 | 零新位图；旧位图停用后是否删档案另议（登记册只管在册资产） | 跟进项 |

---

## 9. 实施计划（预计一轮完成）

| 步 | 内容 | 文件 | 验证 |
|---|---|---|---|
| 1 | 令牌 + hero 静态骨架（导航/胶囊/标题真文本/CTA/底部条） | globals.css、app/login/page.tsx | 浏览器 4 断点 + overflowX |
| 2 | 穹顶五层 + 动效豁免 | components/login/HeroDome.tsx（新） | reduce-motion 双态截图 |
| 3 | DotMatrixTitle | components/login/DotMatrixTitle.tsx（新） | 真文本可选中、canvas aria-hidden、fontScale 0.85/1.3 重采样 |
| 4 | 事实条数据抽公共 | lib/data/portalFacts.ts（新）、FactBadges 改引用 | portal 门回归 |
| 5 | 状态机 + 玻璃登录卡（表单逻辑原样搬） | app/login/page.tsx | 登录全链路 + ?from 直达表单 + clearClientIdentity 仍在 |
| 6 | 清理旧组件（按引用清点 CardCarousel/Constellation/Globe3DUnlock/OrbitalSystem*/Sphere3D/PlanetGlyph） | components/login/* | tsc + eslint 零残留 |
| 7 | 基线重录 + 全门 | type-baseline（/login）、color-baseline（如触） | **`BASE=<端口> npm run check` 看退出码**（V11 教训：门链可执行性 ≠ 各门单跑全绿） |
| 8 | 真浏览器复核 + 提交 | — | 1440/1024/768/390 + 暗弧对比度实测 |

**明确不做**：不引入点阵字体文件；不做滚动叙事；不做第二屏内容（登录页只此一屏，
营销纵深全部在 /portal）；不保留地球解锁玩法（与新叙事冲突，且删掉后色彩门净改善）。

---

## 10. 风险与开放决策

1. **标题文案**需拍板（§3 三选一，推荐 B）。
2. 中文点阵在 38px（移动端）可读性需实机确认；若发糊，降级策略：移动端直接用真文本
   （点阵只在 ≥768px 启用）——组件已内置该开关。
3. 密码预填包 `isProd()` 的小改动随本轮带上。
4. 旧位图资产（校园画卷/轮播卡）从登记册除名与删档，单独一条小提交处理。

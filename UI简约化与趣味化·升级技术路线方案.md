# UI 简约化与趣味化 · 升级技术路线方案

> EduAI Prism「鲁班 7 号」K-12 校内 AI 平台 · 2026-07-27
> **最终版（v4）— 已完成三轮苏格拉底审阅（27 + 24 + 21 问）并逐条修订**

## 如何使用本文档

| 你是 | 读这些 |
|---|---|
| 只想知道做什么、花多久 | §0 → §8.0（3 天最小切片）→ §8.1（完整路线表） |
| 明天就要开工 | §2.2 缺陷台账 → §8.0/§8.1 → 对应的 §5 页面动作表 |
| 关心安全与合规边界 | §7 全节（不可交易项）+ §6.4（资产合规） |
| 想知道为什么这么定 | §1（度量协议）§3（原则与判据）§11（被否决与被退回的方案） |

**三轮审阅的净效果**：初稿的 3 处地基被推翻（度量口径、阈值可达性、断言无执行者），v2 又被判出 6 处过度修订与 5 处自造矛盾，v4 修正了**其中包括我自己误报的一条 P1**（见 §2.2-①）。所有现状结论均已对码自验并标注行号。

---

## 0. 结论与前置动作

**简约化的重点在教师 Dashboard 与 chat 页；趣味化的重点不是"加动效"，而是"给情绪留位置"。**

两条同等重要的执行判断：

1. **先修缺陷，再谈优化**。§2.2 台账里的 ②③ 直接违反项目铁律（编造数据 / 诚实披露），必须先关。
2. **不要把治理设施当成交付物**。v2 曾把 35% 工时压在用户看不见的跑道上、把全部"加法"排到第 8 天后——任何中断都会让这次升级在用户眼里只兑现"删东西"。**§8.0 的三天最小切片是对这个风险的对冲。**

### 0.1 第 0 步：版本控制（约 30 分钟，先于一切）

**已自验：`D:\VB\LLM-School` 与 `app/` 下都没有 `.git`**，只有一份 2026-06-13 的 zip 快照。这让本方案里所有"入库 / 冻结 / 留档 / 跨轮可比"失去支点——基线 JSON 可被无痕修改，密度门就退化成 §1.1 批评的自欺；REGISTRY 作为合规留档的证据效力接近零；而 §4.2 的数据库迁移将**没有任何回滚路径**。

> **动作**：`git init` + 首次全量提交。**提交前必须先验证 `.gitignore` 覆盖 `.env.local`（内含真实 `LLM_API_KEY`）、`.data/`、`node_modules/`、`.next/`**——验证通过再提交，顺序不可颠倒。
> **若明确不引入 git**：删除全文所有"入库/归档/冻结/跨轮可比"措辞，改写为"本地文件，无防篡改保证"；密度门从「达标」降级为「记录趋势」；**并且 §4.2 的数据层迁移不得执行**（无回滚路径的破坏性变更不可接受）。

---

## 1. 现状量化基线（先定协议，再谈数字）

### 1.1 初稿基线为何作废

初稿表格（`/dashboard` 579 元素 / 34 色等）**口径不合格，不得用作验收门分母**，三条原因均已自验：

1. **测的是空态**：`/student/growth` 的「3 控件」是零错题状态，`/chat` 的「17 控件」是空会话；而 `/dashboard` 的 579 是真数据满态——分母不同。
2. **未固定视口**：`RightRail` 是 `hidden xl:flex`、`ChatSidebar` 是 `hidden md:flex`；校内平板与分屏 Chromebook 恰落在 <1280px。
3. **对渐变色盲**：色数用 `backgroundColor` 统计，而源码有 **111 处 `linear-gradient`**，只存在于 `background-image`——**全站最响的颜色恰好落在盲区内**。

### 1.2 测量协议

**夹具：双档，分工不同**（单快照必然要么低估要么高估，而低估方向恰好"更容易通过"）

| 夹具 | 内容 | 用途 |
|---|---|---|
| `tests/fixtures/dashboard-p50.json` | 中位班级规模 / 学科数 | 跨轮**趋势**对比 |
| `tests/fixtures/dashboard-p95.json` | 最大班级 + 最多学科 + 最多告警 + 最长中文姓名 | **密度门与色门的断言分母** |
| `tests/fixtures/student.json` | 6 条错题（3 学科）+ 6 轮往返会话，**每条 assistant 消息 ≤2 行（≤80 字）** | 学生端满态 |

> 学生夹具的「≤2 行」是**最坏化口径**，不可省：chat 首屏控件数与回答长度成反比，写一份"真实感很强的长回答"夹具就能让首屏可见消息从 2 条降到 1 条、控件数自动 −2 达标，既显得严谨又完全没改 UI。

**视口**：`390×844` / `768×1024` / `1440×900` 三档分别出数。
**测量根节点**：统一 `<main id="main">`（`ShellFrame.tsx:44`），侧栏/右栏另行单列。
**滚动位置**：固定为「滚到底部」后计数。

**颜色口径（四条，写死）**
1. 采样属性 = `{color, background-color, background-image 的全部色标, border-*-color（width>0 时）, box-shadow 色, fill/stroke}`
2. alpha 归一化：先对页面底色 alpha 合成再取 hex；`transparent` 丢弃；**同一 hex 不同透明度记为一色**
3. **双层指标**：色相族（30° 桶）+ **族内去重色值**（单看色相族会造出不可能失败的门，见 §3.1）
4. **补采样与硬约束（堵住三条不可采样通道）**：
   - 采样器必须额外遍历页面内所有 `<svg>` 的 `<stop stop-color>` 与 `filter/feFlood` 色值，归入所属元素的色值集合。**已自验必要性**：`components/dashboard/TrendChart.tsx:19-23` 的 `stopColor` 是裸 hex `#312E81` / `#06B6D4`，而消费它的 path 的 computed `fill` 是 `url(#dashPrimary)`——它同时躲过守卫与采样，且 TrendChart 就在 `/dashboard` 上
   - `<img>` / 背景 `url()` / WebP 插画：改用硬约束——**每页装饰性栅格图 ≤2 张**，必须登记进 §6.4 REGISTRY 并在册中人工填写其主色（≤3 个）计入族内色值
   - canvas（登录页 three.js）：单列并写明「**不纳入色门，故不得用于承载功能性配色**」

**动效口径**：不用 `grep -c infinite`（静态类定义数）。运行时实测：
```js
document.getAnimations().filter(a => a.effect.getTiming().iterations === Infinity).length
```

### 1.3 重测前唯一可援引的方向性事实

- `/dashboard` 是全站最重页面，色板与字阶失控是主因
- `/chat` 学生视图对 K-12 偏重，右栏与顶栏工具条是主要来源
- 学生端两页在**空态下**克制，但满态从未被测过（§5 P3 已改为从代码直读的确定动作，不再等重测）

> 对照 Khan Wonder Blocks：站内色 58→18、字体家族 8+→5、字号样式 119+→14，密度反提升 11-26%。**注意量纲**——Khan 的 18 是**站点级色板**，不是单页渲染色值；混淆两者正是初稿 R1 阈值出错的根因。

---

## 2. 代码级现状核查

### 2.1 勘误（初稿与 v2 的错误，含我误报的一条 P1）

| 原写法 | 勘误后的事实 | 位置 |
|---|---|---|
| **「危机对话框会被 ChatTopbar 弹窗遮住」定为 P1（生命安全）** | **误报，不复现**。`ChatTopbar.tsx:151` 根节点是 `relative z-40 … backdrop-blur-md`——`relative` + 非 auto `z-index` 本身即创建**层叠上下文**，`backdrop-filter` 再叠一层并使 `fixed` 后代的包含块变为顶栏盒子。`:277`(z-130)/`:282`(z-120) 是其后代，**结构上不可能盖住**根上下文里的 `SafetyHelp`（`ShellFrame.tsx:49`，浮标 z-100 / 对话框 z-110）；`:282` 的 `fixed inset-0` 也不是全屏遮罩。详见 §2.2-① 的重新定级 | `ChatTopbar.tsx:151, 277, 282`；`ShellFrame.tsx:49` |
| 「3 个死按钮」 | **6 个永久禁用 + 2 个条件禁用**（空会话时的复制/导出发消息后即可用） | `Composer.tsx:113-115`；`ChatTopbar.tsx:231-236, 249, 272`；条件态 `:257, :268` |
| 「删除**整行**工具区」 | **越过了自己的证据边界**。同一行第四个 `Composer.tsx:116` 是 `<ToolButton active onClick={…}>提示词库` —— **enabled 且有真实行为**，删掉即净损失。处置见 §5 P1a | `Composer.tsx:116`；`ToolButton` 定义 `:149 disabled={!active}` |
| 「免责行缺 AI 身份披露」 | **学生端已有**（且该实现本身违规，见 2.2-③）；**教师端缺** | `chat/page.tsx:753-765` |
| 「7 处无限循环动画」 | 那是 `globals.css` **类定义数**（2 个零引用）；`/explore` LoadingState **一屏 9 个** `animate-pulse`（3 骨架条 + `Array.from({length:6})`） | `explore/page.tsx:153-166` |
| v2「仓库无任何测试执行者」 | **夸大**。`playwright@1.61.1` 与 `@playwright/test` 已在 `node_modules`（未进 devDeps、无 config）；且已有跑出 T7 门 18 PASS 的 `.mjs` harness——**但它住在会话 scratchpad，未入库**。真实问题是「已有执行者但未入库、无 npm 入口、无渲染态断言能力」 | `app/node_modules/`；全项目 `find` 无 `*gate*.mjs` |
| v2「错题卡红框只标位置不打叉」 | **纯虚构**。growth 页无红框无叉；全文唯一「错误」是 `:136` placeholder「把错题和你的错误答案记在这里」——**该表述准确且必要，改掉反伤清晰度** | `student/growth/page.tsx:136, 152-167` |
| v2「播种四账号 junior/junior/senior/senior」 | 字面成立但**掩盖了真实分布**：四个账号里**只有一个学生**（`u-student`，junior）；两个 senior 是校长与研究员，永不进入 `/student/*`。故 **primary 与 senior 两档均零样本** | `db.ts:256-261` |
| v2「`Leaderboard.tsx` 四条例外全满足，整文件豁免」 | **只有 peerBand 区块满足**。同文件 `:36-40` 无条件渲染 `<Flame/> 连续学习 {streakDays} 天`，**不受 `showPeerComparison` 约束**——正是我自己 §3.3 禁掉的机制。见 §2.2-⑨ | `Leaderboard.tsx:36-40` |

**保持成立**：停止生成从未暴露（`abortRef`/`abortInFlight()` 仅内部调用）；助手消息动作区学生端 5 个、教师端 6 个；`sourceLabel` 不可删。

### 2.2 现网缺陷台账（全部已自验）

| # | 缺陷 | 证据 | 定级 |
|---|---|---|---|
| ① | **z 阶梯名存实亡（非遮挡缺陷）**。全库 z 值散布 40/60/85/90/100/110/120/130/200，其中 `ChatTopbar` 两处高值被层叠上下文关住（见 2.1）、`OnboardingGuide.tsx:97` 与 `CommandPalette.tsx:78` 的 `z-[100]` 靠 DOM 顺序才没盖住同为 100 的浮标、`(shell)/layout.tsx:14` 跳过链接 `focus:z-[200]` 越界且未登记、`GuardianShell` 三层（`:68` z-60、`:80` z-90、`:102` z-85 且为**不透明全屏遮罩**）完全未登记。`GuardianShell.tsx:9` 写着「层级契约（终审 P0 修复）」——**契约靠注释维系，已被后续改动稀释** | 逐处 grep + 层叠上下文分析 | **P2**（易复发，非当前遮挡）<br>*（v2 曾误定为 P1 生命安全，已更正）* |
| ② | **右栏「安全状态」是凭空生成的安全结论**：硬编码 `[["敏感信息","未检出"],["引用合规","已通过"]]`，与后端零关联、恒绿。学生把含个人信息的作文粘进 chat 仍显示「未检出」 | `chat/page.tsx:853-865` | **P1（违反铁律②）** |
| ③ | **AI 身份与隐私披露被折叠 + 被截断**：塞进 `Collapsible`（`defaultOpen=false`，收起时 children 整体不在 DOM），摘要挂 `truncate` 窄屏截半句；而 `collapsible.tsx:5` 文件头自己写着「绝不用于隐藏安全求助/危机/隐私等关键信息」——**组件契约与调用点直接打架** | `chat/page.tsx:753-765`；`collapsible.tsx:5,54,65` | **P1（诚实/披露）** |
| ④ | `--accent` 在同一 `:root` 定义两次（`:120` HSL 三元组 vs `:373` hex），`hsl(var(--accent))` 消费者拿到非法值 | `globals.css:120, 373` | P2 |
| ⑤ | **渐变色值持久化在数据库**：`prompts.gradient`/`agents.gradient` 是 TEXT 列，来自用户输入 `input.gradient`（自由字符串直接进 `style`，附带样式注入面） | `db.ts:186,193,292,305,658` | P2 |
| ⑥ | 同屏**重复渲染同一份数据**：`workspace.safetyItems` 在 `:391` 与 `:459` 各一次 | `dashboard/page.tsx:391,459` | P3 |
| ⑦ | **T7 门与单测脚本未入库**：跑出 18 PASS 的 harness 存活于会话 scratchpad，换会话即丢 | 全项目 `find` | P2 |
| ⑧ | **无版本控制**（见 §0.1） | 无 `.git` | P2 |
| ⑨ | **现网存在 streak 损失厌恶机制**：`Leaderboard.tsx:36-40` 无条件渲染 `<Flame/> 连续学习 N 天`（不受任何 opt-in 约束）、`explore/page.tsx:265` 第二处、`db.ts:1933` 成就「连续学习挑战」(`streakDays >= 2`)、`db.ts:1971` 把 streak 折进 `weeklyPercent` | 逐处已自验 | P2（**违反本方案 §3.3 禁机制**） |
| ⑩ | **SVG 裸 hex 绕过全部色彩治理**：`TrendChart.tsx:19-23` 的 `stopColor` 是裸 `#312E81`/`#06B6D4`，既不在 className/style（躲守卫），消费者 computed `fill` 又是 `url(#id)`（躲采样） | `TrendChart.tsx:19-23` | P3 |

> ②③ 是「不得后移」的真正理由（**不依赖任何层叠上下文分析即成立**）；①④⑤⑦⑧⑨⑩ 是本方案要动的地基。

---

## 3. 设计原则：双轨（减噪 / 加暖）

### 3.1 减噪四条铁律

| # | 规则 | 可测判据 |
|---|---|---|
| **R1** | **颜色语义化**：颜色只表达功能角色，不表达学科归属 | **三层门，三层都要过**：<br>①**色相族**（30° 桶，非结构色）≤ 4（学生端）/ ≤ 6（教师端）<br>②**族内去重色值** ≤ 4（含渐变全部色标与 SVG `<stop>`，alpha 归一后计）<br>③**结构性硬门**（口径 = §4.4 全文件扫描）：`app/`+`components/`+`lib/` 内裸 hex 与裸 `linear-gradient(` 命中数 = 0 **且** `SELECT count(*) FROM prompts WHERE gradientKey NOT IN ('accent','info','warm','calm') OR gradientKey IS NULL` = 0（agents 同）|
| **R2** | 一屏一处高饱和 | 每屏 `--accent-focus` 使用次数 = 1 |
| **R3** | 层级靠字号与空白，不靠边框/底色块 | 字号档 ≤ 7；纯装饰性 `border` 数量逐页下降 |
| **R4** | 渐进披露 | 见下方四条 |

> **R1 为何三层**：v2 主指标只用色相族且设 `≤13`——但 360÷30 = **12 个桶**，结构上不可能失败。且色相族对「同一色相下 9 个明度变体」不敏感，而那恰是本仓库病灶。第③条是唯一能真正驱动改写的判据，**且必须同时查数据库**——否则改完源码，`/prompts`、`/agents` 的渐变仍由 DB 供给，门却是绿的。
>
> **R1③ 的当前基数（写进文档作为跨轮分母）**：`linear-gradient(` **111 处**，其中仅 **15 处**在 `className`/`style` 行上；裸 hex 出现 **556 处**（全文件扫描口径，含数据文件）。**若守卫只扫 className/style，改写 15/111 处就报 0 命中**——把内联渐变搬进常量表（`ChatSidebar.tsx` 的 `gradientFor`、`StatRow.tsx` 的 `TONE_GRAD`、`AgentKanban.tsx` 的色表、`ChatTopbar` 的 `MODEL_GRADIENT_CSS`）一行颜色不改就能变绿，而这个动作在 diff 上与常规重构无法区分。故 §4.4 必须是全文件扫描。

**R4 的四条判据**

① **每条助手消息常驻交互控件 ≤ 2**（对 6 轮夹具断言）
② **首屏常驻控件三档预算**：390 ≤ 8 / 768 ≤ 10 / **1440 ≤ 16**（对账见 §5 P1a），并附**判定谓词三条**：
   - **控件** = `main#main` 内的 `button, a[href], input, select, textarea, summary, [role=button], [role=tab], [role=switch], [tabindex]:not([tabindex="-1"])`，**以及任何绑定 onClick 的非语义元素**（不得靠把 `<button>` 换成 `<div onClick>` 规避）
   - **首屏** = `rect.top < innerHeight && rect.bottom > 0` 且可见（非 `display:none/visibility:hidden/opacity:0`）
   - **滚动位置**固定为「滚到底部」；夹具按 §1.2 最坏化
   - **防下推趋势指标**：另记录「首屏外一屏内（1~2 倍视口高）的控件数」，不设门但每轮上报，异常增长人工复核
③ **配对判据（防"藏起来"冒充"删掉"）**：学生端核心动作（发问/停止/换个说法/求助）**与教师端核心动作**（班级学情/待批改/配额告警）均须首屏 ≤1 次点击可达、带中文文字标签，**禁止收进 Tab 第二屏**
④ **分母排除项是闭集**：
   - 排除项**只能**取自 `lib/safety/whitelist.ts` 登记的 id
   - **首屏可见安全控件 ≤ 3**（当前实际只有浮标 1 个），超出即门失败
   - **反滥标断言**：`document.querySelectorAll('[data-safety-critical]').length` 必须等于 whitelist 中匹配当前路由的条目数
   - **解耦两个属性**：`data-no-press`（免 press 缩放）与 `data-safety-critical`（白名单 + 分母排除）分开
   - 脚手架 chips 与空态提示词**计入**分母，但二者**不得共现**
   - **whitelist.ts 的变更纪律**：因项目无 CI 无 PR 流程，该文件每次变更必须**单独一次 commit**（依赖 §0.1）且 commit message 写明新增命题与理由；§8.1 的 V5 门包含一次人工回看该文件的全部 commit

### 3.2 加暖：分两层，下层无条件交付

v2 把三处加暖都改成「带放行阈值的候选」，但三个阈值所需数据**一条都查不到**（已自验）：`chat_messages` 只有 `durationMs`，无首包时延；全仓库无 pageview 埋点；`audit` 表无 stage 列且只保留最近 500 条；庆祝频次更是循环依赖。结果会是三个槽位无限期挂起——**一次以严谨为名的单腿交付**。

**下层 · 最小加暖层（零埋点依赖、零资产成本、无条件交付）**

| 位置 | 做法 |
|---|---|
| 思考态 | **中性文案「正在思考…」+ 闪烁点由动词行接替 + sr-only `role=status` 只写一次**——不作任何过程断言，故不需要任何阈值 |
| 空态 | 一行人话 + 一个主按钮（纯文案） |
| 完成态 | 中性确认（「已移出错题本」「已完成复习」），≤1 次、位移 ≤8px |

**上层 · 需花钱与合规成本的东西（生图插画、庆祝动画资产、拟人形象）才受闸制**，且改为**投放后测**：
- V1a/V3 内只做**两张**插画，宿主写死为学生必经的两个空态：`/student/growth` 错题本空态（`growth/page.tsx:150`）与 `/chat` 空会话态
- 其余页面一律"一行人话 + 主按钮"
- 日后要扩再补 `page_views(route, stage, day)`，跑满两周后按「≥50 次/周/学段」放行——**扩张前置，不是首投前置**

**思考态硬规则**

> **任何声称"正在执行某项工作"的动词，必须指向一个在该动词显示期间仍在进行中的真实服务端事件；不能指向的一律删除。**

已自验：`classifySafety`（`route.ts:53`）与 `searchKb` 都在 `ReadableStream` 创建（`:175`）**之前**同步跑完，即发生在响应体开始之前；而**唯一漫长的等待（模型首 token）里没有任何服务端事件**。据此：

1. **删除 `phase:safety-checked`**（响应开始前就结束，无可显示的进行态）
2. `phase:kb-retrieved` 保留但改为**完成态陈述**：静态一行「已参考校内资料 N 份」，不做轮播动词
3. 首 token 前的等待**一律中性文案**，不编造步骤
4. 日后唯一诚实的新事件来源是**网关降级切换**（`phase:switching-to-local`）
5. **兼容性依据（已自验）**：客户端 SSE 解析器对未知 event 静默忽略（`chat/page.tsx:368-383`），故可增量上线；但**断线重连后不得重放 phase**
6. 非流式分支（crisis/care/scaffold/配额/限流走 JSON，`route.ts:73/80/104`）无 phase，前端降级路径须确定

**字段隔离与可访问性**：`scheduleSlowNotice`（`chat/page.tsx:133-140`）在 10 秒时把降级预警写进同一个 `m.content`，动词若也写 content 会**覆盖它**。处置：`ChatMessage` 增加独立 `notice?: string`；动词节点 `aria-hidden="true"`；`notice` 用 `role="status" aria-live="polite"` **单独播报一次**；`prefers-reduced-motion` 下动词停止轮换（CSS `iteration-count` 覆盖对 JS 定时器无效，必须显式处理）。

### 3.3 趣味分级 L0-L4

- **L0** 静态插画/图标语义化 · **L1** 微反馈 · **L2** 进度可视化 · **L3** 拟人形象表情反应 · **L4** 全屏庆祝/虚拟货币/装扮/对战
- **配额**：教师端 L1 · 学生端 **primary + junior** L3（限 `/student/home`、`/student/growth`）· **senior L1**（默认不出现拟人形象）· **L4 全站禁用**

> **v2 在这里犯了三个错，均已自验**：①配额只给 `primary`，而**全库唯一的学生账号是 junior**——趣味化对所有真实用户不可达；②默认值方向搞反：v2 借用了 `useUserStore` 里「安全默认取最强保护学段」的注释，但那句语境是**防代写强度**（primary 管得最严），搬到趣味配额上方向翻转——未知学段回落 primary 等于给 17 岁学生塞给 6 岁准备的形象；③只说"补一个 primary 账号"，但 **senior 学生同样零样本**（两个 senior 是校长与研究员），补完 primary 后 `allowed('student','senior')` 写反也没有任何断言能抓到。

**落地机制（轻量实现）**

1. **纯策略函数**：`lib/delight/policy.ts` 导出 `allowed(role, stage)` 与 `suppressed(kind, source, safetyOpen)`；每个趣味组件模块顶部一行早返回 `if (!allowed(...) || suppressed(...)) return null;`。**生产同样生效**，纯函数单测比 lint 规则便宜且不会被跳过。
2. **fail-closed 到 L1**：stage 为 null / provider 缺省 / role 未知 → L1。文档明写「**此处安全方向与防代写相反：未知 → 最低拟人化**」。
3. **身份注入修正（已自验可行）**：`SessionRoleProvider.tsx:21` 目前**只承载 role**，但服务端 `toSessionUser` **已透传 stage**（`db.ts:325`）。扩为 `SessionIdentityProvider({ role, stage })`，在 `(shell)/layout.tsx` 一并注入；客户端 store 只作非权威镜像。
4. **构建期约束用现成规则**：`no-restricted-imports` + flat config `overrides`（约 5 行），禁止 `dashboard|research|admin` 子树 import 拟人形象/庆祝模块——**不写自定义 eslint plugin**（无 CI 的单人项目里，会随版本升级报错的自定义 plugin，最省力修法是注释掉它）。
5. **播种两个学生账号**覆盖全部三档：`u-student-p`（primary，小学四年级）与 `u-student-s`（senior，高二），连同现有 `u-student`（junior）。
6. **四行端到端断言表**（V0 门）：未登录 = 0 / primary > 0 / junior > 0 / **senior = 0**——最后一行才是这条红线的真正判据。

**红线：从「禁形态」改为「禁机制」**

同一套心理机制换个呈现就能穿过所有门：「你已经三天没来啦」和一枚熄灭的火焰制造的是同一种缺席负债感。**来自学校平台的负债感比来自消费 App 的更重——它带着校方权威。**

> **禁止的机制**（不论呈现形态）：损失厌恶 · 缺席惩罚 · 连续中断提醒 · 进度落后暗示 · **未经学生开启就默认呈现的比较**（含教师端把班级均值透传到学生视图）· 补救提示 · Prodigy 式的付费/身份/进度特权绑定。

**例外条款（四条必要条件，缺一即禁）**：默认关闭 · 由学生本人开启且可随时关闭 · 只呈现分段/区间不呈现名次与具体差值 · 不出现在 `/student/home` 与 `/student/growth` 的默认视图。

**豁免必须是命题级，不是文件级（v2 在此处几乎造成红线自我作废）**

v2 给 `components/explore/Leaderboard.tsx` 开了**整文件**豁免。但已自验：该文件 `:36-40` 无条件渲染 `<Flame size={12}/> 连续学习 {progress.streakDays} 天`——**不受 `showPeerComparison` 约束、无任何 opt-in**，正是禁机制表第一条。**这是全文最省力的变绿路径**：执行者只需把词表的 exclude 指向该文件，门就绿、火焰一根没少，且未来任何被禁机制只要挪进这个文件就自动继承豁免。处置：

1. **豁免对象写死为命题**：「`Leaderboard.tsx` 中 `showPeer && peerComparisonStatus === 'available'` 分支内的 `peerBand` 文案」，词表工具按**行号区间** exclude，**不接受整文件 exclude**
2. **streak 必须删除或改形态**（V3 动作）：替换为**可累计、不因中断归零、不出现火焰/「连续」字样**的形态，例如「本周有 4 天来过」；`db.ts:1933` 的成就「连续学习挑战」一并替换；`db.ts:1971` 把 streak 折进 `weeklyPercent` 的计算同步调整
3. §2.2-⑨ 已将其登记为 P2 缺陷，落地在 V3

**不可共现清单**：`crisis / care / scaffold` 三类回复以普通 assistant 气泡进入**同一条消息流**（已自验），故拟人形象若挂 chat 会话区必与危机内容同屏。形象已退出 chat，但**真正剩下的共现路径是安全求助浮标**——它全站常驻，而 `/student/home` 页脚（`home/page.tsx:68`）正是「遇到困难可随时点右下角『安全求助』」。处置见 §7.8。

---

## 4. 设计令牌**迁移**（不是新增）

### 4.1 先修事故

- `--accent` 重复定义（`globals.css:120` vs `:373`）：把 shadcn 那份改名 `--accent-hsl`（或确认无消费者后删除）
- 数据层渐变（§2.2-⑤）：见 4.2 / 4.2.1

### 4.2 迁移映射表

| 层 | 旧 | 新 | 处置 |
|---|---|---|---|
| 令牌名 | `--c-edu` / `--c-primary` / `--primary` / `--accent` 四名一角色 | `--accent-focus` | 进 deprecated 期，V2 结束删除 |
| 源码 | 散落 hex（556 处出现）+ 111 处 `linear-gradient` + SVG `stopColor` | `--accent-focus` / `--accent-tint` / chart 色板 | 逐页改写 |
| **数据层** | `prompts.gradient` / `agents.gradient` 存自由字符串 | **枚举键** `accent \| info \| warm \| calm` | 见 4.2.1 |

### 4.2.1 数据层迁移执行细则（本方案唯一破坏性、触及用户既有内容的改动）

1. **前置**：`git init` 已完成（§0.1）；**迁移前必须先备份** `cp .data/eduai.sqlite .data/backup-<日期>.sqlite`，该命令写进步骤，不可省
2. **迁移文件**：`lib/server/migrations/003-gradient-enum.ts`，沿用 `db.ts` 现有的守卫式 ALTER 模式——**新增 `gradientKey TEXT`，回填后保留 `gradient` 旧列一个版本作为回滚数据**，V2 结束再 DROP
3. **映射规则**：按首色标色相归入四键（`#2563EB`/`#312E81`→`accent`；`#06B6D4`/`#10B981`→`info`；`#F59E0B`/`#EC4899`→`warm`；`#7C3AED`→`calm`；无法归类显式落 `accent`）。**注意这是有语义后果的取舍**：`#EF4444`（错误红）不得落入任何装饰键，须单独复核，避免把一张中性提示词卡片变成警示色
4. **API 收口**：zod 收成 `z.enum([...])` 拒绝自由字符串（**顺带关闭一个 style 注入面**）
5. **断言用正向形式**（v2 的 `LIKE '%linear-gradient%'` = 0 可以靠删列、清空、改写成 `linear_gradient(` 绕过）：`SELECT count(*) FROM prompts WHERE gradientKey NOT IN ('accent','info','warm','calm') OR gradientKey IS NULL` = 0
6. **用户交代**：V0 验收门附加「教师端 `/prompts` 抽查 5 张卡片前后截图对比」，并在《交付验收报告》「已知限制」记一句「**存量提示词配色已按语义归并，外观有变化**」

### 4.3 目标令牌与 z 阶梯（**必须穷举，否则阶梯只是注释**）

```css
--accent-focus   /* 唯一主行动色，禁止组件内写死彩色 */
--accent-tint    /* 同色系 8-12% 透明底 */
--radius-ctl / --radius-card       /* 两档，替代当前 4-8 档并存 */
--shadow-lift / --shadow-float     /* 阴影用主色微染 */

/* z 阶梯 —— 七层全登记 */
--z-app: 40;
--z-guardian-badge: 60;   /* GuardianShell:68 */
--z-app-modal: 90;        /* ChatTopbar:282 · OnboardingGuide:97 · CommandPalette:78 */
--z-guardian-rest: 92;    /* GuardianShell:80 休息提醒（与 app-modal 分开取值，消除撞层） */
--z-guardian-night: 94;   /* GuardianShell:102 夜间不透明全屏遮罩 —— 必须低于 --z-safety-fab，
                             否则夜间模式下学生无法求助（加断言） */
--z-toast: 95;            /* ChatTopbar:277 */
--z-safety-fab: 100;
--z-skip-link: 105;       /* 唯一被允许 ≥100 的非安全层，且必须低于 --z-safety-dialog */
--z-safety-dialog: 110;
```

> **v3 曾把 `--z-skip-link` 定为 115（高于安全对话框）**——在修复"非安全层压过安全层"的同时亲手又造了一个。已改为 105。

**阶梯的根本判据（比数值更重要）**：「非安全层不得 ≥100」是**不充分的**。还须加一条——**安全层（`SafetyHelp`）的任何祖先不得引入层叠上下文**（`transform` / `filter` / `backdrop-filter` / `opacity<1` / `will-change` / `contain`）。§2.2-① 的整个误报正源于只比较 z 数值而忽略层叠上下文。

**完备性检查**：`rg 'z-\[\d+\]'` 的每一处命中都必须能在阶梯表中找到对应令牌，否则 `npm run gate` 失败——让阶梯从注释变成**可验证的闭集**。

### 4.4 守卫（口径必须与 R1③ 一致）

> **§3.1 R1 补充：第五类「模型身份色板」（2026-07-28 定案）**
>
> R1 禁止用颜色表达归属，前四批据此收敛了学科色、角色色、模型厂牌色与推荐卡配色。
> 但 `/hub` 模型广场是**例外**：这一页的核心任务就是「同屏比较并选择 5 个模型」，
> 颜色在这里是**主要区分手段**而非第二遍编码，抹平成单一强调色会直接损害页面本职功能。
>
> 因此新增**第五类**：`MODEL_PALETTE`（5 槽，`lib/data/gradientKeys.ts`）。三条约束——
> ① 值**全部引用 CHART_PALETTE**，不引入任何新色相，色板文件 hex 上限断言不因此放宽；
> ② 按**模型 id** 映射槽位，不按数组下标（重排 MODELS 不得让所有模型换色）；
> ③ 渐变用**同色系明度 ramp**（`tonalPair`），不用双色相拼接——后者会把「5 个色相」
>    变成「10 个色相」。
>
> 门禁侧对应两条断言（`render.mjs` R9⑤）：渐变必须解析为真颜色，**且五张卡的颜色必须互不相同**。
> 只测前者是不够的——五槽全指向同一下标时颜色依然「都是真的」，只是全一样，门会全绿而页面一片同色。


- **裸色守卫 = 全文件扫描**：对 `app/`、`components/`、`lib/` 下全部 `.ts/.tsx` 做正则 `/#[0-9A-Fa-f]{3,8}\b/` 与 `/linear-gradient\(/`，**不限定 `className`/`style` 上下文**，且**扫描范围包含 SVG 属性值**（`stopColor`/`fill`/`stroke`）。唯一白名单是 `globals.css` 的令牌定义区块与 `tests/` 夹具
- **显式声明**：「任何把颜色从内联属性搬进常量表/工厂函数的改动**不视为达标**」
- **z 值守卫正则 `z-\[\d{2,}\]`** + 文件级白名单（v2 的 `z-\[1\d\d\]` 抓不到 2xx）
- **字重收敛 400 / 600 / 700**（⚠️ 本条原写「400/500/700」，是**没量就写的规范**——实测源码
  `font-semibold`(600) 用了 **231 处**是绝对主力，却被原规范排除在外；照原规范执行等于把 231 处
  改掉去迁就一个凭空定的档位。据实改为：400 正文 / 600 标题与标签 / 700 强调与数字。
  `font-extrabold`(800) 8 处已清零。`font-medium`(500) 曾 82 处，**已并档完毕**：与最弱化文字色
  紧邻的 8 处（单位后缀、禁用态、跳过按钮）落回 400，其余 74 处升到 600。理由是它与 400 在
  11-14px 下几乎不可分辨，即**多占一档却不产生层级**。门禁的过渡豁免已同步关闭）；
  统计数字 `tabular-nums`；**Light 字重禁用**（教室投影仪会糊）
- **中文实践**：不追求"圆润中文字体"（无可商用免费的真·圆润中文变量字体，中英混排易割裂）；亲和感由圆角 + 色染阴影 + 文案语气承担

---

## 5. 页面级改造

### P1 · `/chat`（拆两轮）

**P1a — 呈现层**（只碰 `components/chat/*` 与 CSS，不改 `chat/page.tsx` 的状态与请求逻辑）

| 动作 | 依据 |
|---|---|
| **删除 `Composer.tsx:113-115` 三个禁用 ToolButton** + `:83-87` 死浮层 + `:78` 的 `onDragEnter` 分支 | 上传能力未实现（`:78` 的 `onDragEnter` 里写的是 `setDragging(false)`，故 `:83-87` 是**永不显示的死代码**） |
| **`:116` 提示词库保留**，形态改为 placeholder 内的一行文字链，或 Composer 右上角单个 `<button>` | 它是 `active` 且有真实行为——v2 说"删整行"越过了自己的证据边界 |
| 两处英文提示改中文「本对话暂不支持上传附件」 | 中文化 |
| **「联网 · 未开通」不删**，改为免责行一句人话：「AI 不会上网搜，只用校内资料，所以最新的事可能不知道」，加入白名单 | A 类「占位假功能」→ 删；**B 类「能力边界声明」→ 不做成按钮，但信息必须留** |
| **补上传能力边界声明**：「本对话暂不支持上传附件，需要交作业照片请用…」，同样加入白名单 | 附件上传后端已判为 out-of-scope（§8.2），不补这句学生端就是净损失 |
| **每条助手消息动作区 5 → 2**：学生端只常驻「复制」「重新生成」，收藏/👍/👎 收进「⋯」二级（键盘可达、有 `aria-label`）；教师端「导出」同样收进「⋯」 | 6 轮问答 = 30 个常驻按钮，是 chat 最大噪声源 |
| 助手消息**去容器**（学生端保留 4-6% tint + 头像列） | 长答案纵向噪声主因 |
| 元数据重定义为「模型名 + 状态位」，状态位仅在 `source !== 'remote'` 时渲染 | 既瘦身又保诚实 |
| **降级标注升级为整句提示** | 见下方专条 |
| **删除右栏「安全状态」面板** | §2.2-② |
| **披露修复**：把「AI 会出错，作业与考试以老师和教材为准」「对话不对同学公开、不用于排名」提升为 Collapsible **外的常驻两行**（不加 `truncate`，允许折行），细则留折叠区 | §2.2-③ |
| 免责行只保留安全与涉密提示 + 两条能力边界；快捷键移入 placeholder；**AI 身份披露不在 Composer 重复** | 权威位置定为顶部那处 |
| 追问 chips 固定 3 条：「再讲简单点」「出两道类似的题」「我还是没懂，换个说法」 | 学生端只用白名单文案，不用模型生成 |
| 右栏「推荐提示词」→ 空态内联 3-4 条；**非空态下的提示词入口 = 保留的提示词库按钮** | 右栏仅 ≥1280px 渲染，<1280px 学生**自始没有**入口——空态是能力补齐，非空态由提示词库按钮承接 |

**首屏控件预算对账（1440×900，改造后）**

| 项 | 计数 | 计入分母 |
|---|---|---|
| Composer 输入框 + 发送/Stop | 2 | ✅ |
| **提示词库入口** | 1 | ✅ |
| 顶栏：模型选择 + 苏格拉底开关 + 深度思考 | 3 | ✅ |
| 侧栏首屏项（新建 + 会话列表可见项） | ~3 | ✅ |
| 每条助手消息 ×2 × 首屏可见消息数（夹具最坏化 ≈2 条） | 4 | ✅ |
| 追问 chips | 3 | ✅ |
| 空态提示词 3-4 | 0（与历史消息**不共现**） | ✅（互斥） |
| 常驻披露两行 / 降级 note / 两条能力边界句 | 0（非控件，属白名单披露节点） | ❌ 排除 |
| 安全求助浮标 | 1 | ❌ 排除（whitelist 闭集） |
| **合计** | **16** | — |

> 实算 **16**，正好触到 1440 档上限。**这是刻意接受的结果**：诚实的做法是承认加法（提示词库 + chips + 能力边界）的成本，而不是回头砍掉刚补齐的东西——那正好把前两轮 must-fix 的成果吐回去。若 V1a 实测超 16，优先削减的顺序是：侧栏会话列表可见项 → 顶栏深度思考开关收进「⋯」，**不得削减 chips、提示词库入口、任何白名单节点**。

**降级标注升级**：现状 `MessageBubble.tsx:98-101` 把 `sourceLabel` 与模型名、耗时挤在同一行 `text-[11.5px] text-[var(--text-2)]`，「网关降级」与「真实模型」在字号、字重、颜色上**完全同构**。**保住字符串 ≠ 保住诚实性**。改为：
- `source ∈ {local, local-fallback}` 时，在答案正文**下方**渲染 `role="note"`：「**这条回答来自校内兜底模型，可能不如平时准确，重要结论请再问老师**」，用 `--warn-bg/--warn-ink`（对比度实测 ≥4.5:1）
- 「网关降级 / 本地兜底」等术语**从学生端 UI 移除**，只在教师端保留
- R1 功能角色清单增加第五类「**degraded / 不确定**」

**P1b — 停止生成（单独一轮，专属回归）**：流式期间 Send 原位形变为 Stop。断言六条：①请求真中止（网络面板无后续 chunk）②已生成部分正确落库且 `source` 不被抹成 undefined ③Stop 后可立即重新生成不串消息 ④中止态下切会话/新建不产生竞态 ⑤中止后 UI 无残留「正在生成」⑥**几何**：Stop 与危机浮标外接矩形在 360/390/768/1024/1440 五档**不相交**。

### P2 · `/dashboard`（**计数已更正**：单屏实为 6 张，非 12）

> **⚠️ 本节原写的「12 → 6」是我的计数错误，已实测更正。**
> `grep -c '<Card>'` 得到的 12 是**把教师与管理员两个角色分支加在一起**的静态计数，而
> `effectiveAdmin` 决定同时只渲染其中一支。真浏览器实测（1440×900）：**教师视图 6 张卡**
> （真实使用趋势 / 模型调用占比 / 教学快捷入口 / 最近会话 / 推荐提示词 / 平台安全状态），
> 管理员视图同为 6 张——也就是说**「封顶 6」这个目标本来就已达成**。
>
> 这与「7 处 infinite」（静态类定义数 ≠ 运行时同屏实例数）是**同一类错误**：用静态计数代替
> 运行时度量。同一个坑第三次踩，记在这里。
>
> 因此本节的真实动作只剩两条：①删同屏重复面板（**已完成**）；②密度收敛（色板/字阶/圆角）。

| 行号 | 去处 | 状态 |
|---|---|---|
| `:391` SafetyList | **删除**（保留 `:459` 右栏一处） | ✅ **已完成**——与 `:459` 渲染同一份 `workspace.safetyItems`（§2.2-⑥）。实测「安全状态」出现次数 2 → 1；渲染门 R7 断言防复发（含「main 已渲染」前置，防空页假绿） |
| 其余各卡 | 保持 | 单屏 6 张已达标；**不再为凑数字而合并**——合并同主题双卡属于内容编排判断，无实测依据支持，不做 |
| 色板 / 字阶 / 圆角收敛 | 待做 | 这才是本节剩下的真实工作（§3.1 R1 三层色门 + 字号档 ≤7 + 圆角两档） |

**堵住 Tab 逃逸的判据（V2 门）**：`dashboard` 首屏（1440×900）**Tab / Accordion 触发器数量相对基线不得增加**——没有这条，12→6 在数字上永远可以靠折叠达成。
其余：色板审计（删按学科着色残留）、字号档 ≤7、圆角两档、移动端单列 DOM 顺序 = 阅读顺序、教师端补 AI 身份披露。

### P3 · 学生端（从代码直读的确定动作）

**删除虚构条目**（"红框只标位置不打叉"——growth 页无红框无叉）。负面词清理对象改为**经审计确认的实际命中处**：V-1 用一次全仓 grep 出清单，学生端若为空则如实写「学生端已达标，仅教师/管理端需清理」——**在近乎空集上验收"负面词清零"是又一道不可能失败的门**。

**确定动作**：
- **错题卡动作区收敛**：`growth/page.tsx:162-165` 每卡 4 个动作 → 保留「AI 讲解」为主按钮，「再练一题 / 标记已复习 / 删除」收进「⋯」二级（键盘可达、`aria-label`、删除保持二次确认）
- **录入表单 chip 折叠**：`:121-123` 的 9 个学科 chip + `:138-140` 的 5 个错因 chip → 默认折叠一行（学科用 select，复用 `:126` 已有模式）
- **删除 streak 火焰**（§2.2-⑨ / §3.3）：`Leaderboard.tsx:36-40`、`explore/page.tsx:265`、`db.ts:1933` 成就、`db.ts:1971` 权重——统一替换为可累计、不归零、无「连续」字样的形态
- `/student/home`：问候语只允许**时段** 与 **今日可做的下一件事**两类变量，不含历史对比；「已经…天」「别断了」「落后」「加油追上」进 V3 禁机制词表
- 空态接 `EmptyState`（插画只给 §3.2 定的两处必经路径）

**庆祝触发（必须服务端计算，含后端改动）**：
- 已自验 `markMistakeReviewed` 是 `UPDATE mistakes SET reviewedAt = ?`（`db.ts:1169`）**单列覆盖，无复习历史**；`mistakes` 只有 `createdAt`/`reviewedAt` 两个时间列。「两次间隔 ≥1 天」在现 schema **无法判定**
- **若不补 schema，落地时最省力的近似是「PATCH 后 reviewedAt 非空」→ 点一次已复习就放烟花**，诱导强度与被否掉的"删除即庆祝"几乎相同
- **处置**：①新建 `mistake_reviews(id, mistakeId, userId, at)` + 索引 `(mistakeId, at)`；②`PATCH /api/mistakes` 每次插一行并同步维持 `mistakes.reviewedAt`（兼容现有读取）；③`PATCH` 响应新增 `celebrate: boolean`，由**服务端**按「同一 knowledgePoint 上存在两条 review 且跨自然日」计算，**前端不得自行判定**；④DELETE 响应恒 `celebrate:false`；⑤**删除「再练一题判定通过」路径**——它需要 §6.3 已确认不存在的 correctness 信号
- **断言两条**：a) 连续删除全部错题后无 `.celebrate-pop`；b) **同一天连续两次 PATCH 后 `celebrate` 必须为 false 且页面无 `.celebrate-pop`**（这条才测得到真正的诱导路径）

### P4 · 教师端三步向导

「选模板 → 填 1-2 个必填 → 结果预览 + 微调折叠」；**安全/隐私相关项（可见范围、是否留痕、模型来源）不得进折叠区**。

---

## 6. 生图资产

**管线已验可用**：gpt-image-2，36-77s / 张。已生成 3 张（形象候选、空态-知识库、完成态-错题清零）。

### 6.1 主形象必须重做

初稿的猫头鹰 + 学位帽 + 书本 + 命名「导师」+ 表情反应，两重问题：①**IP 近似**——「猫头鹰 + 教育 + 表情反应」是某知名语言学习产品吉祥物的核心识别组合，而方案又明说要取其表情反应机制；②**符号冲突**——持证教师造型的 AI 形象，视觉上主张的正是方案在文字上要否认的（它是老师、它靠谱、它不会错），而**视觉在低龄段的权重远高于 10.5px 灰色免责行**。

处置：**换母题**（避开猫头鹰/绿色/表情反应组合），去掉学位帽、书本、「导师」命名，改为**同伴/工具型**定位。形象任何出现处恒显一行**不可折叠**短句「我是 AI 助手，不是老师」。

### 6.2 形象退出 chat

**只出现在 `/student/home` 与 `/student/growth`**（§3.3 不可共现）。

### 6.3 表情三态绑**页面上真实存在**的事件

`/api/chat` 返回体只有 `kind / source / durationMs / references / id`，**无 correctness 字段**；而 v2 改绑的 `streaming` / SSE `done` **在这两个宿主页面上永不发生**（home 与 growth 都不发起 SSE），形象会恒停 idle。改为：

**`idle` / 「记下了」（`POST /api/mistakes` 返回 200 后）/ 「陪你再看一遍」（`PATCH /api/mistakes` 返回 200 后）**

两条硬规则：**前端不得从文本内容推断正确性**；**形象只在身份 provider 就绪后渲染，首帧不渲染**（避免首帧 fail-closed 到 L1、水合后又出现的"闪一下"）。

### 6.4 资产登记册

第一轮指出「生成方 = 审核方」的利益冲突正确，但 v2 一步跳到"外部机构书面确认"跨度过大：**一道无人能关的门，只会让趣味化被无限期挂起，或让门被绕过**；而且它卡住的是整个 V4，三步向导跟资产合规毫无关系却被连坐。定为：

1. **REGISTRY 七项自查**：`app/public/art/REGISTRY.md` 逐张登记——生成模型与版本 · 完整 prompt · 日期 · 许可证/商用条款出处 · **七项合规打勾**（授权链 / 已知 IP 近似 / 真人相似性 / 年龄呈现 / 性别·肤色·民族刻板印象 / 宗教与政治符号 / AIGC 标识）· **以及 §1.2 要求的该图主色（≤3 个）**
2. **审核方分离降级为可自持形式**：由另一次独立会话/另一角色做**盲审**，记录审核人标识与日期
3. **「校方书面确认」从验收门移出**，改列为《交付验收报告》「已知限制」中的**上线前置条件**
4. **默认路径**：在拿到确认前**不生成任何拟人化形象资产**，L0 插画只用纯几何/无人形图案；`.celebrate-pop`、EmptyState、问候语等**不含形象的趣味化照常交付**
5. **V4 拆为 V4a（向导 + 能力边界文案，不受资产门约束）与 V4b（拟人形象资产）**

**其余纪律**：统一风格词根 → 压 WebP ≤150KB → 存 `app/public/art/`；**每页装饰性栅格图 ≤2 张**（§1.2）；**插画不得承载唯一信息**，每张必须有文字等价物与 `alt`。

---

## 7. 安全与诚实红线（不可交易项）

### 7.1 危机求助浮标：豁免一切简约规则

> **重要更正**：v2/v3 曾把「危机对话框被 ChatTopbar 弹窗遮住」定为 P1 生命安全缺陷。**该结论不成立**（见 §2.1）——它源于只比较 z 数值而忽略层叠上下文。真实问题是**阶梯不穷举、靠 DOM 顺序侥幸、契约只写在注释里**（P2，易复发）。本节的修复项照做，但「不得后移」的理由改挂在 §2.2-②③ 上。

1. 建立 §4.3 **七层全登记**的 z 阶梯 + 完备性检查（`rg` 命中必须在阶梯表中有对应令牌，否则 gate 失败）
2. **层级归位**：`ChatTopbar:277 → --z-toast`、`ChatTopbar:282 → --z-app-modal`、`OnboardingGuide:97 → --z-app-modal`、`CommandPalette:78 → --z-app-modal`、`GuardianShell:68/80/102 → 三个具名令牌`、跳过链接 → `--z-skip-link:105`
3. **加根本判据**：安全层的任何祖先不得引入层叠上下文（`transform`/`filter`/`backdrop-filter`/`opacity<1`/`will-change`/`contain`）
4. **弹层互斥 —— 方案已选定为 (a)，不留二选一**：
   > 新建 `lib/store/useOverlayStore.ts`，导出 `register(id, close)` / `unregister(id)` / `closeAll()`；`ChatTopbar` 在 `exportNotice`/`copyDraft` 的 `useEffect` 里注册 `() => setExportNotice(null)` / `() => setCopyDraft(null)`；`OnboardingGuide`、`CommandPalette` 同理；`useSafetyStore` 打开求助时先调 `closeAll()`。
   > **已否决方案 (b)**（`body[data-safety-modal]` + CSS `display:none`）及理由，保留备查以免下轮重提：①它依赖的 `data-app-overlay` 属性全仓库不存在，需在四处逐一添加，改造量与 (a) 相同，"更省事"的优势不存在；②`display:none` 会把打开状态的 `role="dialog" aria-modal="true"` 弹窗从渲染树摘掉，造成**焦点丢失与 aria-modal 语义损坏**——那是 a11y 回归，不是互斥
5. **press 豁免做成显式机制**：`.press-soft` 只挂 Button 组件；`[data-no-press], [data-no-press] * { transform: none !important }`；给浮标、热线 `<a href="tel:">`、「请老师联系我」打 `data-no-press` **与** `data-safety-critical`（两属性解耦）
6. **press 判据**：仅用于 ≥44×44 的非安全类控件（现网大量控件是 `min-h-[40px]`，需一并提到 44）
7. **几何**：chat 列加 `padding-bottom: var(--safety-fab-reserve, 96px)`，或 <md 时浮标改到左下；断言五档下浮标与 Send/Stop 不相交且可点
8. **`z-order.spec.ts` 五条路径**（copyDraft / exportNotice / 命令面板 / 新手引导 / **GuardianShell 夜间遮罩**），每条断言用 **`elementFromPoint(热线按钮中心) === 热线按钮`**（而非比较 z 值——比较 z 值正是本轮误报的根因）

### 7.2 不可折叠白名单（单位 = 命题 + 必现文案片段）

v2 的断言口径「在 DOM 且非 `aria-hidden`」**结构上无法失败**：摘要 span 确实在 DOM，但学生看到的是被 `truncate` 截成一半的句子。**一条能通过的假断言比没有断言更危险**。

白名单命题（`lib/safety/whitelist.ts`，含 id、必现片段、宿主路由，节点带 `data-safety-critical`）：
安全求助入口 · AI 身份提示 · 隐私说明 · 降级/来源标注 · 诚信引导消息 · **联网能力边界声明** · **上传能力边界声明** · **「我是 AI 助手，不是老师」**（宿主 = home/growth，与拟人形象同生共死）

**渲染态五重断言**：①`toBeVisible()` ②`innerText` 含约定完整片段 ③`scrollWidth <= clientWidth + 1`（未被截断）④`getBoundingClientRect()` 在 1440×900 与 390×844 首屏内 ⑤对比度 ≥4.5:1、非 `aria-hidden`、祖先无 `display:none/height:0`

**并且**：`collapsible.tsx` 增加运行时守卫——接收 `data-safety-critical` 子树时开发环境直接 `throw`。

> §7.2 在现网 chat 页**已被违反**（§2.2-③），V-1a 必须先修复再谈瘦身。

### 7.3–7.6

- **7.3** 温柔文案不延伸到安全事件：自伤/霸凌线索需要清晰直接，不是柔化
- **7.4** 学生端 API 响应不得含 rank 字段（不是前端隐藏，是不进 payload）
- **7.5** 语音输入（若启用）：默认不留存原始音频、只存转写文本，说明恒显不可折叠
- **7.6** 动效负面清单：位移 ≤24px、**同屏无限循环 ≤2**（运行时实测口径）、禁 rAF 依赖、禁 scroll-jacking；`prefers-reduced-motion` 下庆祝与 shimmer 全部降级。**现存违规**：`/explore` LoadingState 同屏 9 个 `animate-pulse`，列入 V0（改为单个骨架容器 + 一条 shimmer）

### 7.7 UI 不得显示未经后端确认的安全/合规判定（纯禁止条款）

v2 删掉伪造面板的同时给了个"建设性替代"——用 `done` 事件的 `kind/source` 渲染「本次未触发校内安全策略」。**该替代本身仍是编造**：已自验 `route.ts:202` 的 `kind = finalSource === "remote" ? "normal" : "degraded"`，**只表达降级与否，不含任何安全判定**；而 `classifySafety` 是关键词分类器，没命中只意味着没命中词表。用它渲染绿色结论比现状**更具误导性**（看起来像被本轮对话验证过）。

1. UI 只允许陈述**已发生的动作与来源事实**（回答来自哪个模型 / 是否走兜底 / 引用了几份校内资料），**不得对内容做任何合规或安全性判定，即便有后端字段也不得推断**
2. **路由级断言**：`/chat` 全页在任何会话状态下都不得出现「未检出 / 已通过 / 已合规 / **未触发**」等字样（「未触发」也进禁用词表，堵住换皮）
3. 未来若确要做安全反馈，前置条件写死为「后端 `done` 事件新增独立 `safetyVerdict` 字段且其语义经过一次误报/漏报实测」，在此之前**不得立项**

### 7.8 不可共现清单

v2 的断言「以危机关键词发起请求后 DOM 中无 `[data-delight]`」守的是**已经没有守护对象的页面**（形象已退出 chat），而真正剩下的共现路径无人看管：安全求助浮标全站常驻，`/student/home` 页脚还主动引导学生去点。

1. 任何趣味组件订阅 `useSafetyStore.open` **与** 本轮 `kind`，二者任一为真即 `return null`（**非 CSS 隐藏**）
2. **会话级规则**：本会话内一旦出现过 `kind ∈ {crisis, care}`，该会话剩余时间内 chat 相关 delight 一律不恢复
3. **断言**：a) `/student/home` 程序化触发 `openHelp` 后 DOM 中无 `[data-delight]`；b) 危机关键词路径同上（回归冗余）；c) 未登录访问任意学生页 `[data-delight]` = 0
4. **防漏标**：新增趣味组件必须走 `lib/delight/policy.ts` 的早返回；`policy.ts` 的导出被 import 的位置数，与带 `data-delight` 的组件数，在 V5 人工对账一次

> **实现成本低于预期（已自验）**：`MessageBubble.tsx:133-192` 已存在同类机制——收藏、👍/👎 挂 `!isSafety`，重新生成挂 `!isPolicy`。§7.8 是把既有隐式约定提升为显式红线并扩展到 delight 层。

---

## 8. 分阶段路线与验收门

### 8.0 最小可交付切片（MVS，3 天 = 0.5 + 2.5）

> 保证 3 天内用户能**同时**看到"更干净"和"更暖"。

| 天 | 内容 | 验证 |
|---|---|---|
| **D0（0.5 天）** | **新建 `app/tests/gate/` + 最小 `.mjs` runner**（只需 fetch + 字符串断言，约 80 行）+ `package.json` 加 `"gate": "node tests/gate/run.mjs"` | 自身可跑通 |
| **D1** | §2.2 **②③** 修复（删除伪造安全面板、披露提升为常驻两行）+ z 阶梯常量化与七层登记 | `npm run gate` 追加 3 条断言 |
| **D2** | §5 P1a 呈现层（删三个死 ToolButton + 保留提示词库 + 动作区 5→2 + 去容器 + 降级整句提示 + 两条能力边界句） | 同上 + 人工三档视口核对 |
| **D3** | §5 P3（错题卡动作收敛 + chip 折叠 + 删 streak 火焰 + 问候语 + **EmptyState 文字版**）+ **中性思考态** | 同上 |

> **D0 不可省**：MVS 原本写「在现有 harness 上追加断言」，但那套 harness 只存在于会话 scratchpad（§2.2-⑦）——走 MVS 的人第一天就会发现无处追加。D0 与 §11-2「不建第二套体系」不冲突，它建的正是那套 `.mjs` 的入库版。

**MVS → 完整路线接续表**

| MVS 产出 | 在完整路线中的状态 |
|---|---|
| D0 的 `.mjs` runner | **可直接复用**，V-1b 在其上扩 Playwright 单文件 |
| D1 的 ②③ 修复 + z 登记 | **已完成可跳过**；但 §2.2-④（`--accent` 重复定义）**需补做**（推迟到 V0；已知副作用：`hsl(var(--accent))` 消费者拿非法值，MVS 期间不修） |
| D2 呈现层 | **已完成可跳过**；若 D2 期间用了临时 z 字面量而非 §4.3 令牌，**需重做** |
| D3 学生端 | **已完成可跳过**；但**服务端 `celebrate`、插画资产需补做**（MVS 只交付文字版空态与中性完成态） |

MVS **不含**：`ui-audit.mjs`、Playwright、REGISTRY、生图资产、令牌全量迁移、数据层迁移、停止生成。

### 8.1 完整路线

| 阶段 | 内容 | **用户可见的加法** | 验收门 |
|---|---|---|---|
| **V-1a 缺陷修复**（0.5 天）<br>**不得后移** | §2.2 **②③** + ④ + z 阶梯七层登记；三条最小断言挂 `.mjs` harness | —（安全修复） | `npm run gate` 全绿 |
| **V-1b 跑道与埋点**（1.5 天）<br>**可与 V1a 并行** | ①T7 harness 与单测**迁入 `app/tests/`** + `gate` script；②**仅**为渲染态断言引入 Playwright 单文件（`safety.spec.ts`：z-order 五路径用 `elementFromPoint`、白名单五重、浮标与 Stop 不相交）；③`ui-audit.mjs` + 双档夹具 + 基线 JSON；④`chat_messages.firstDeltaMs` 埋点；⑤`mistake_reviews` 表 + `PATCH` 返回 `celebrate` | — | `npm run gate` |
| **V0 令牌迁移**（2.5 天） | `--accent-focus/tint`、z 阶梯令牌化、半径阴影两档、字重三档、`lib/delight/policy.ts` + `no-restricted-imports`、`SessionIdentityProvider`、**播种 primary 与 senior 两个学生账号**；改写 111 处渐变 + 裸 hex + SVG `stopColor`；**数据层迁移（§4.2.1，含备份）**；修 `/explore` 骨架 | — | `npm run gate`；**全文件扫描**守卫 0 命中；`gradientKey` 正向断言 = 0；三层色门达标；同屏 infinite ≤2；**四行 delight 断言表**（未登录=0 / primary>0 / junior>0 / **senior=0**）；`/prompts` 5 张卡片前后截图 |
| **V1a chat 呈现层**（1 天） | §5 P1a 全部 + 中性思考态 | ✅ 中性思考态、降级整句人话、提示词库入口保留 | 每消息常驻控件 ≤2；首屏三档预算（≤8/≤10/≤16）；白名单五重；流式态 sr-only status 文本数恒为 1 |
| **V1b 停止生成**（1.5-2 天，单独一轮） | Send↔Stop 原位形变 + abort 时序 | ✅ 可中断 | §5 P1b 六条断言<br>*（本项推翻《交付验收报告》第 110 条"不动 chat"——理由：中断入口是学生端唯一功能性缺口且基础设施已就绪；代价是单独一轮 + 专属回归）* |
| **V2 dashboard**（1.5 天） | 12→6 映射表落地、色板/字阶收敛、DOM 顺序、教师端补披露；**并行启动 V4b 资产盲审** | — | p95 达标 且 p50 不回退；**Tab/Accordion 触发器数不得增加**；单列阅读顺序；教师端核心动作首屏 ≤1 击 |
| **V3 学生端**（1.5 天） | §5 P3 全部（含**删 streak 火焰**）+ 庆祝（消费服务端 `celebrate`）+ growth 空态插画 1 张 | ✅ 错题本更干净、庆祝、空态插画 | 同日两次 PATCH 后 `celebrate=false` 且无 `.celebrate-pop`；连续删除后无 `.celebrate-pop`；reduced-motion 降级；**禁机制词表清零，豁免仅限 `Leaderboard.tsx` 的 peerBand 行号区间** |
| **V4a 向导与边界**（1 天） | 三步向导 ×3、两条能力边界文案 | ✅ 教师三步向导 | 向导键盘可达、焦点管理正确 |
| **V4b 拟人形象**（1 天，**可被卡住而不阻塞其他**） | 换母题重做、三态绑 mistakes 事件、REGISTRY 七项 + 盲审 | ✅ 同伴形象 | REGISTRY 七项全绿 + 盲审归档；`[data-delight]` 不与安全态共现（含 `openHelp` 路径） |
| **V5 全站验收** | 密度复测 + T7 门 + 图门 + 真浏览器矩阵 | — | T7 全绿（本方案断言注册为第 19-25 条）；三档视口密度达标；**人工回看 `whitelist.ts` 全部 commit**；**`policy.ts` import 数与 `data-delight` 组件数对账** |

**总工期**：约 **12 天**。**若预算不足，走 §8.0 的 3 天 MVS**，其余按 V-1b → V3 剩余 → V0 → V1b 的顺序增量补齐。

### 8.2 本方案覆盖的**后端**改动边界

**在范围内（5 项）**：
1. SSE `event: phase`（仅 `kb-retrieved` 完成态陈述 + 未来的 `switching-to-local`）
2. `chat_messages.firstDeltaMs` 埋点
3. `mistake_reviews` 表 + `PATCH /api/mistakes` 返回服务端计算的 `celebrate`
4. `prompts.gradient`/`agents.gradient` 迁枚举 + zod 收口（§4.2.1，含备份与回滚列）
5. `SessionIdentityProvider` 所需的 stage 透传（服务端已具备，只需接线）+ 播种两个学生账号 + streak 相关的 `db.ts:1933/1971` 改写

**明确 out-of-scope**：
- **附件上传后端**。学生上传的是作业照片——含人脸、姓名、住址、同学信息；要做通至少需要存储、类型与大小校验、内容安全门（本项目对生图已有"安全门先于外部 API"的先例）、配额、审计、保留期与删除路径，还要与既有 `image_jobs` 异步管线和错题本打通。**需独立立项（暂记 V6）。** 正因如此，§5 P1a 删除死按钮时**必须**补上那句能力边界声明并纳入白名单
- 页面级曝光埋点（`page_views`）——仅在插画扩张时才需要
- `safetyVerdict` 字段（§7.7 已写死前置条件）

---

## 9. 证据分级（诚实声明）

- **一手（我直接验证）**：全部代码结论（行号可复核，含 §2.2 十条缺陷）、生图管线可用性（36-77s/张）、`linear-gradient` 111 处 / 裸 hex 556 处出现 / 其中 15 处在内联位置
- **静态读码，未经真浏览器合成验证**：**z 层级与层叠上下文的全部结论**（§2.1、§2.2-①、§4.3）——正是这一类结论在 v2/v3 里造成了一次 P1 误报。以 V-1b 的 `elementFromPoint` 断言实际跑过为准
- **口径不合格、待重测**：§1 初稿密度表（空态 + 未固定视口 + 渐变盲）
- **一手（调研员抓取）**：landing.love 页面、Khan Wonder Blocks 公开稿、ClassDojo 设计系统官方稿、Duolingo 角色系统官方博客、Khanmigo 家长页
- **二手综述**：希沃三步备课、智学网排名下线、小猿口算批改形态、Duolingo streak 机制拆解、2026 UI 趋势、各 AI 产品对话界面细节（land-book 本轮抓取失败）
- **利益冲突声明**：初稿把「形象已目检合格」列为一手证据，但生成方与审核方同为我——已撤回，改为 §6.4 盲审流程
- **跨文档一致性核对**：本方案的"仓库测试现状"结论已与《交付验收报告》§2.1/§5.3 交叉核对并更正（v2 曾夸大为"零执行者"）

---

## 10. 修订轨迹

| 轮次 | 结果 |
|---|---|
| 初稿 | 9 章，三路调研 + 4 页密度实测 |
| 第一轮（攻初稿） | **27 问 / 18 must-fix**。推翻三处地基：度量口径不合格、阈值算术不可达、断言无执行者。挖出现网缺陷 4 条 → **v2** |
| 第二轮（攻修订本身） | **24 问 / 16 must-fix，判出 6 处过度修订**。推翻 v2 自造矛盾 5 处（色相族门不可失败 / 零 primary 账号 / provider 无 stage 且默认方向反了 / DB 渐变 / 庆祝条件 schema 不可判定）。新增缺陷 5 条 → **v3** |
| 第三轮（终审） | **21 问**，含 **5 处事实性错误**。最重的一条：**v2/v3 把一条不复现的 z 遮挡误定为 P1 生命安全**；另揭出「我一边禁 streak 机制、一边给承载它的文件开整文件免检章」的红线自我作废路径 → **v4（本文档）** |

## 11. 被否决与被退回的方案（保留记录，避免下轮重提）

| # | 曾经的方案 | 结论 |
|---|---|---|
| 1 | V-1 把「跑道 + 白名单 + 缺陷修复 + 度量」捆成一道"不得后移"的门 | **退回**：拆 V-1a（0.5 天，真不得后移）/ V-1b（可并行）。"不得后移"只保留给诚实与安全缺陷 |
| 2 | 断言"仓库零测试执行者"，据此建全新跑道 | **更正**：已有 harness 但未入库。改为迁移 + 仅为渲染态断言引入 Playwright 单文件 |
| 3 | 资产上线门 = 校方书面确认 | **退回**：一道无人能关的门只会让趣味化被无限期挂起或门被绕过。降级为 REGISTRY 七项 + 独立盲审 + 默认路径 |
| 4 | `DelightBoundary` 包装层 + 自定义 eslint plugin | **退回**：为 4 个可枚举组件建通用框架。改用纯函数 + 早返回 + 现成 `no-restricted-imports` |
| 5 | 空态插画须过"按学段周曝光阈值" | **退回**：埋点全不存在。改为必经路径先投两张，阈值降为扩张前置 |
| 6 | 思考态须过"SSE 首包中位数 ≥600ms" | **退回**：中位数恰好砍掉"偶尔很慢时"的全部反馈，而情绪真空只发生在慢的那一侧。删除该门，中性提示无条件交付 |
| 7 | 弹层互斥用 `body[data-safety-modal]` + CSS `display:none` | **否决**：依赖不存在的 `data-app-overlay` 属性（改造量与方案 a 相同），且 `display:none` 会造成焦点丢失与 aria-modal 语义损坏 |
| 8 | 「危机对话框被 ChatTopbar 弹窗遮住」定为 P1 生命安全 | **撤销**：`ChatTopbar` 根节点的 `relative z-40` + `backdrop-blur-md` 创建层叠上下文，其后代 z-130/120 结构上不可能盖住根上下文的 z-100/110。重新定级为 P2「阶梯名存实亡」 |
| 9 | `Leaderboard.tsx` 整文件豁免禁机制词表 | **否决**：该文件同时承载被禁的 streak 与被豁免的 peerBand，豁免粒度是文件、违规粒度是代码块。改为按行号区间的命题级豁免，并把 streak 列为待删除项 |
| 10 | `--z-skip-link: 115`（高于安全对话框 110） | **更正为 105**：在修复"非安全层压过安全层"的同时不得亲手再造一个 |

*相关文档：《前端升级优化·角色化重构方案路线书》《后端开发与真实测试·完整路线文档》《交付验收报告》《内测问题清单·滚动汇总》。*

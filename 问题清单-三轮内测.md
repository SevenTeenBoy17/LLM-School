# EduAI Prism「鲁班7号」三轮内测 · 问题清单

> 内测方式：agent 分轮测试 + 每轮独立**监工 agent** 抽测复核（亲自重跑 ≥2–3 条结论，防偷懒/省步骤/编造）。
> dev: http://localhost:3400（node:sqlite）。账号：teacher/Teacher@123 · student/Student@123 · admin/Admin@123 · research/Research@123。
> 环境限制：preview 截图超时（纯视觉项 needsVisual 延后）；多 agent 大扇出常被限流→小扇出(≤2–3)串行。

## 轮次
- **R1 后端运行时实测**（curl 真打 :3400：鉴权/RBAC/chat 四分支/history 绕过/边界/落库/脱敏）+ 监工抽测。
- **R2 前端检查**（a11y/语义/文案/空载错态 代码审查，分片 agent）+ 监工核验。
- **R3 整合回归**（四角色全量回归 + 问题清单逐条对抗复核）+ 监工终审。

## 问题清单（P0/P1/P2 → 动作 → 验收）
（各轮结果在下方追加；监工 verdict 与抽测复现一并记录）

### R1 · 后端 — ✅ CLEAN（监工 verdict=pass）
- 测试 agent：18 条真实 curl 步骤，全部符合预期，findings=0。覆盖：四角色 login 200 · RBAC 页面 307/API 403 · chat 四分支(normal/scaffold/care/crisis) · **history 绕过仍 crisis 硬拦截** · 401/400(坏JSON)/400(超长) · 审计+工单真实落库(计数递增证明) · 科研 masked stu-N vs 管理员明文 双标对照。
- 监工 agent：coveredAll=true，**亲自重跑 13 条**（login/RBAC/chat 四分支/history 绕过/脱敏双标/错误码/递增落库）全部与自报一致，confirmedFindings=0，verdict=**pass**。
- 监工提示（非缺陷·测试卫生）：审计/工单绝对计数在共享 live 库上不可复现（append-only 实时落库致快照漂移）；后续断言宜用「触发前后 delta」。
- **结论：后端零缺陷，监工核验通过，无偷懒/造假。**

### R2 · 前端 — 监工 verdict=pass（15 条确认，rejected=0，无重大漏报）
2 分片审查 agent 实读 59 文件；监工抽查 11 处 file:line 全 CONFIRMED、驳回 0、无编造。确认 **15 条代码可见 a11y 缺陷**（皆 needsVisual=false）：
- **P1** 登录账号/密码 input 无可访问名（仅 placeholder）——全站唯一入口：`login/page.tsx` NeuInput。
- **P2(11)**：登录「忘记密码?/注册」无 href `<a>` 键盘不可达；知识库检索测试 input 无名 + Send 按钮无名；模型广场 hero 搜索无 aria-label；会话历史搜索框无 aria-label；提示词 收藏/复制 图标按钮无名；提示词变量 `<label>` 未关联 input；上传对话框 `<Label>` 未关联 Select；models 卡 / permissions 行 role=button 内嵌真实 `<Button>`（嵌套可交互 + models「设置」未 stopPropagation 会冒泡触发 setActive）。
- **P3(3)**：探索/知识库视图/提示词分类 筛选按钮选中态仅颜色无 aria-pressed；角色/模型选中态无 aria-pressed/aria-current；chat 侧栏搜索无结果静默空列表（无空态+无 aria-live）。
- 动作：折叠低风险项（aria-label/aria-pressed/label 关联/空态/stopPropagation）；role=button 嵌套结构项谨慎处理避免键盘回归。
- **已修（14/15，9 文件）**：登录 input aria-label(P1) + 忘记密码/注册→真实 button(P2) · 知识库测试 input+Send aria-label · 会话历史/模型广场 搜索 aria-label · 提示词 收藏/复制 aria-label + 变量 label htmlFor/id 关联 · 上传对话框 SelectTrigger aria-label · models 设置 stopPropagation(去冒泡误选)+aria-pressed · explore/knowledge视图/prompts分类/permissions角色 filter aria-pressed · chat 侧栏搜索空态(aria-live)。
- **验收**：tsc `OK` · eslint(9 文件)`OK` · code-review-graph 130/362/errors=[] · /login=200、teacher login=200、login SSR 含 2 个 aria-label(账号/密码 已渲染)。
- **延后 1/15（P2·记录在案）**：models/permissions 外层 `role=button` 内嵌真实 `<Button>` 的「嵌套可交互」ARIA 纯度问题——已做安全部分(stopPropagation+aria-pressed)，完全去嵌套需结构重构且会丢键盘行/卡选择(回归风险)，留作专门重构。

### R3 · 整合回归 + 监工终审
- **四角色全量回归（我实测 curl）**：教师 dash/class/chat/prompts/knowledge/hub=200、admin/audit=307、api/class=200、api/audit=403 ✓；学生 learn/explore/chat=200、class=307、api/class & tickets=403、chat 四分支(normal/scaffold/care/crisis)全对 ✓；管理 analytics/audit/permissions/models + api/audit + tickets=200 ✓；科研 masked=true、names_hidden=true、admin=307 ✓。**R2 的 14 处 a11y 改动未引入任何运行时回归。**
- **监工终审 = PASS（14/14 核实）**：独立 Reality Checker agent 逐文件 Read+Grep 确认 14 条修复全部落在指定 file:line（login 320-326/384 · knowledge 46-47/178/182 · ChatSidebar 44/81-83 · hub 65 · prompts 38/128/145-147/171 · UploadDialog 132/152 · models 44-48/79 · explore 145 · permissions 73），0 假报/0 漏改；延后项(models/permissions role=button 嵌套)如实标注、非偷懒；无重复属性/语法破坏；交叉核验 prompts 变量 key 唯一→htmlFor/id 无冲突。verdict=主 agent 未偷懒、改动属实。

## 三轮内测结论
- **R1 后端**：clean（0 缺陷），监工重跑 13 条 pass。
- **R2 前端**：监工确认 15 条代码可见 a11y 缺陷；**折叠 14**（1 P1 + 11 P2 + 2 P3 等），1 条结构性去嵌套(P2)如实延后。
- **R3 回归+终审**：四角色全量回归全绿无回归；监工 14/14 核实修复属实，verdict=pass。
- **静态门**：tsc `OK` · eslint(9 文件)`OK` · code-review-graph **130/362/errors=[]**。
- **未纳入（诚实）**：纯视觉项（对比度数值/焦点描边可见性）因 preview 截图超时延后。

## 复检轮（围绕遗留问题优化 + 再内测）· 2026-07-02
- **优化**：唯一遗留可修项 P2「role=button 内嵌真实 Button」**已彻底去嵌套**（2 文件）：
  - `admin/models/page.tsx`：外层去 role=button/tabIndex/onKeyDown → 纯 div(onClick 鼠标便捷层)；「选择模型」动作收敛到包住模型名的真实 `<button type="button" aria-pressed={active===m.id} onClick=stopPropagation+setActive>`，键盘/读屏经此可达。
  - `admin/permissions/page.tsx`：行外层去 role=button/tabIndex/onKeyDown → 纯 div(onClick)；「打开用户」经真实「编辑」`<Button aria-label={\`编辑 ${u.name} 的权限\`} onClick=stopPropagation+setActiveUser>`。
- **验收**：全库 `role="button"` grep = **0 命中**；tsc `OK` · eslint(2 文件)`OK` · code-review-graph **130/362/errors=[]** · 四角色回归全绿（models/permissions 200、chat 分支、RBAC 307/403 完好）。
- **诚实**：models 模型列表为**客户端 hydrate 后渲染**（SSR HTML 不含 ChatGPT/aria-pressed），故 SSR curl 看不到该 button——属渲染时机，非缺陷；交互 DOM 的最终视觉确认受 preview 截图超时所限（同 needsVisual 边界）。源码 + 静态门已确证改造正确。
- **再内测·监工复检 = PASS**：独立 a11y agent 逐行核验——两处外层已彻底移除 role=button/tabIndex/onKeyDown（全文件 role="button"=0）；models 选择动作由真实 `<button aria-pressed onClick=stopPropagation+setActive>`（模型名）承载、permissions 由「编辑」`<Button aria-label=含用户名 onClick=stopPropagation+setActiveUser>` 承载，`keyboardPathOk=true`，`regressions=无`。residualNits（模型名 button 热区偏小、选中态/焦点态视觉分离）判为**可选增强·非阻断**，本轮不追改（避免过度工程）。
- **本轮结论**：内测遗留唯一可修项已彻底优化 + 监工复检 pass；三轮内测代码可见缺陷**全部清零**。仅剩「需渲染/像素核实」项受 preview 截图超时所限延后。

---

// Canonical fidelity entry: v6 retains the 640-bit / 48-bit drift budget,
// nonblank pixels, 600 KB capture floor, 44px controls and real API assertions.
// Fake peers, fake inventory and QA-only state menus are replaced by real fixtures.
import "./manor-v6-ui.mjs";
